# 결정 로그 (decisions)

형식: 날짜 / 결정 / 맥락 / 근거 / 재검토 조건. 스펙과 달라진 것, 보류한 것만 적는다.

## 2026-08-31 — 지도 SDK: 카카오 → 네이버(NCP 신규 Maps)
- **맥락**: 카카오맵 무료 쿼터가 "계정당 첫 번째 활성화 앱 한정" 정책으로 확인됨. 개발자 계정에 카카오맵 사용 중인 기존 앱이 있어 새우맵은 두 번째 앱 → 무료 쿼터 없이 비즈월렛(유료) 필요.
- **결정**: 네이버 클라우드 신규 'Maps' 상품의 Web Dynamic Map 사용. 지오코딩도 VWorld 대신 네이버 Geocoding으로 통일.
- **근거**: 신규 Maps는 월 600만 건 무료 + 초과 0.1원/건 (구 AI NAVER API의 무료 종료와 별개 — 2025-06-23 NCP 공지 확인). 우리 예상 규모(월 수십만 로드)의 10배 여유. 11/30까지 NCP 크레딧 20만원이 초과분 방어. 데이터·플레이스 링크·길찾기·검색 API(API HUB)가 네이버 생태계라 콘솔 통일.
- **운영 조건**: NCP 콘솔에 월 사용량 알림 400만 건 설정(차단 아님). React 래퍼가 얇으므로(react-naver-maps) SDK 사용 시 context7로 공식 문서 확인.
- **재검토 조건**: 네이버 Maps 무료 정책 변경 시, 또는 월 로드 400만 건 접근 시.
- **유지**: 카카오 로그인·카톡 공유는 지도 SDK와 무관하므로 카카오 유지.

## 2026-09-01 — TypeScript 6.0.3 (7.0 대신)
- **맥락**: TS 7.0.2가 최신이지만 typescript-eslint 8.69.0이 TS 7.0을 아직 미지원 (>=7.1 추적 중).
- **결정**: TypeScript 6.0.3 사용. Next.js 16.3.3과 호환 확인됨.
- **재검토 조건**: typescript-eslint이 TS 7.x 지원 시 업그레이드.

## 2026-09-01 — Vercel 비용 방어 정책
- **맥락**: DAU 2천 서비스가 월 $86 청구된 실사례 분석 (Threads @kindainvestor). 원인은 트래픽이 아니라 **ISR 남용**: `revalidate: 60` × 동적 URL 9,649개 → 재생성마다 Function Duration + ISR Writes 이중 과금. 부가 요인: next.config `headers()`의 Cache-Control이 페이지 `revalidate`를 덮어써 s-maxage=60 고정(두 번 배포하고 발견), 크롤러 트래픽, Observability 과금.
- **우리 방어선 (Phase 5~6에서 지킬 것)**:
  1. **페이지 캐시는 on-demand 우선**: 쓰기(제보·다녀왔다면·리뷰)가 우리 앱을 거치므로 `revalidateTag`/`revalidatePath`로 변경 시에만 재생성. 주기 revalidate는 보조로 3600s 이상. **`revalidate: 60` 같은 분 단위 금지** — 우리 데이터는 분 단위 신선도가 필요 없다.
  2. **next.config `headers()`에 페이지 경로 Cache-Control 금지** — 페이지 revalidate를 무력화한다. 폰트·정적 자산(`/fonts/*` 등)만 허용.
  3. URL 규모 자각: /place/[id] 452 + /gu/[name] 25 ≈ 477개 (그 사례의 1/20). on-demand면 재생성은 실제 데이터 변경 횟수에 비례.
  4. Observability Plus 켜지 않는다.
  5. **Hobby 플랜 유지** — 한도 초과 시 과금이 아니라 정지라 요금 폭탄이 구조적으로 불가능. Pro 전환 시 Spend Management 지출 상한을 반드시 같이 설정.
  6. 폰트는 dynamic subset (아래 결정).
- **재검토 조건**: Pro 전환 시, 또는 Phase 5 SSR 페이지 추가 시 이 목록 재확인.

## 2026-09-01 — 호스팅: Vercel Hobby 유지, Cloudflare 이전은 트리거 기반
- **맥락**: Vercel 요금 폭탄 실사례 조사 — Cara $96k(1주 유저 65만, 함수 5,600만/일), DDoS $23k, 크롤러 8.4TB $1.5k, 봇 95% 트래픽 과금 등. 공통점: 전부 **Pro 이상 + Spend Management 미설정**. Hobby는 초과 시 과금이 아니라 정지라 폭탄 불가능.
- **결정**: PMF 확인까지 Vercel Hobby 유지. 단 이전 비용을 싸게 유지하는 패턴 준수 — Vercel 전용 기능 미사용, 사진 외부 스토리지(R2 예정), on-demand ISR (전부 OpenNext 호환 패턴).
- **Cloudflare(Workers + OpenNext) 트레이드오프**: 장점 = egress 무료(대역폭 폭탄 원천 차단), 요청당 과금($5/월에 1,000만 req), WAF·봇 방어가 본업, 스펙의 R2·Turnstile과 궁합. 단점 = OpenNext 어댑터 레이어(Next 신버전 시차·런타임 갭 디버깅), ISR 캐시 직접 구성(KV/R2/DO), 프리뷰 DX 열세.
- **이전 트리거** (하나라도 해당 시 재검토): ① Pro 전환 필요 시점(Hobby 정지 경험 또는 상업화) ② 봇·크롤러 트래픽 유의미 ③ 사진 트래픽 본격화(R2 이전과 동시 진행이 자연스러움).
- **Pro로 가게 되면**: 전환 당일 Spend Management 상한 설정이 선행 조건 (기본값이 무상한 — 사례들의 공통 원인).

## 2026-09-01 — git 워크플로: 모든 변경 PR 경유, push는 승인 필수
- **맥락**: Phase 0 동안 커밋 12개가 전부 main 직push, PR 0개 — Codex PR 리뷰 설정이 발동 불가능한 모순. 사용자 지적으로 규칙화.
- **결정**: 커밋은 로컬 자유(세이브 포인트) / push·PR·머지는 사용자 승인 필수 / 모든 변경 짧은 브랜치→PR (main은 branch protection으로 기계 차단, CI `check` 통과 필수) / 코드 PR은 Codex 리뷰 경유, 문서만 PR은 셀프 머지 OK.

## 2026-09-01 — 자동 배포: GitHub Actions, 배선은 Phase 1 첫 PR 때
- **맥락**: 자동 배포 타이밍 논의. 배포를 1회만 해본 시점의 배선은 하네스 선완성("미리 완성하지 않는다" 위반).
- **결정**: 방식만 확정 — 기존 ci.yml 확장: main push → check 잡 통과 후 `opennextjs-cloudflare deploy` / PR → `opennextjs-cloudflare upload`(프리뷰 버전). 배선은 Phase 1 첫 PR과 함께. 그때까지 `pnpm deploy` 수동.
- **배선 결과 (Phase 1, 2026-09-01)**: PR → `pnpm run upload --preview-alias preview` (opennextjs-cloudflare upload가 미지 인자를 wrangler `versions upload`로 전달함을 `--dry-run`으로 확인) → 고정 URL `https://preview-saeu-map.saeu-map.workers.dev`, PR에 코멘트. main push → `pnpm run deploy`. 시크릿 미등록 시 잡 실패 대신 스텝 스킵(+notice). CI에선 `pnpm deploy`가 pnpm 내장 명령과 겹치므로 항상 `pnpm run`.
- **프리뷰 URL을 고정 별칭 하나로 한 이유**: NCP Maps는 등록된 Web 서비스 URL에서만 인증되고 와일드카드가 없다. PR별 URL이면 매번 콘솔 등록이 필요 → 별칭 `preview` 하나만 등록. 동시 PR이면 마지막 업로드가 덮어씀(1인 개발이라 수용). **확인(2026-09-01)**: 프로덕션 URL 등록만으로 `preview-saeu-map.saeu-map.workers.dev`에서도 인증 통과 — NCP의 "서브 도메인은 대표 도메인만 입력" 규칙이 `*.saeu-map.workers.dev`를 커버한다. 추가 등록 불필요.
- **Workers Builds(대시보드 git 연동) 배제 이유**: CI(테스트·gitleaks)를 우회하고 배포됨.
- **필요 시크릿** (배선 시점에 사용자가 등록): `CLOUDFLARE_API_TOKEN`("Edit Cloudflare Workers" 템플릿) + `CLOUDFLARE_ACCOUNT_ID` → GitHub repo secrets, `NEXT_PUBLIC_NCP_CLIENT_ID` → repo variable. **등록 완료(2026-09-01)**. 토큰은 공백 없이 넣고 저장 전 `curl -H "Authorization: Bearer $T" https://api.cloudflare.com/client/v4/user/tokens/verify`로 `active` 확인할 것 — 공백 섞인 값은 6111 에러로 프리뷰 잡이 실패한다.

## 2026-09-01 — 호스팅 변경: Cloudflare Workers 선(先)채택 (당일 "Vercel Hobby 유지" 결정 뒤집음)
- **맥락**: 스파이크 결과 OpenNext 어댑터가 Next 16.3.3을 정확히 지원, 자동 마이그레이션·로컬 workerd 렌더·배포까지 당일 완료. 이전 비용이 최저점(페이지 1개, ISR·이미지 없음)이고, 사용자의 Vercel 계정은 다른 프로젝트들과 Hobby 무료 한도(계정 단위 100GB)를 공유 중이었음.
- **결정**: 처음부터 Cloudflare Workers(OpenNext)로 배포. Vercel은 쓰지 않는다. 프로덕션: https://saeu-map.saeu-map.workers.dev (Phase 7에서 새우맵.kr 연결 예정).
- **요금 안전**: Workers Free는 한도 초과 시 과금이 아니라 에러(하루 10만 요청). Cloudflare 폭탄 사례는 R2 Infrequent Access 오해($9~10)가 대부분 — **R2는 Standard 등급만 쓴다**.
- **운영 수칙**: ① Next 업그레이드 전 @opennextjs/cloudflare 호환(peerDeps) 확인 ② 배포 전 `pnpm preview`(workerd, :8787)로 런타임 확인 ③ ISR 도입 시(Phase 5~6) R2 incremental cache 바인딩 설정 필요 (지금은 미설정 — 정적이라 무관)
- **재검토 조건**: OpenNext가 Next 메이저를 4주 이상 못 쫓아오거나, 런타임 갭 디버깅이 반복될 때.

## 2026-09-01 — Pretendard dynamic subset 전환 (단일 2MB woff2 폐기)
- **맥락**: PretendardVariable.woff2 단일 파일 2MB를 전 방문자가 다운로드하는 구조였음. DAU 2천 가정 시 폰트만 월 60~120GB 전송 → Hobby 무료 한도(100GB) 위협.
- **결정**: pretendard 패키지의 dynamic subset(unicode-range 92분할)으로 전환. 화면에 쓰인 글자 범위의 조각만 다운로드 — 방문당 ~100KB, 약 95% 절감. `/fonts/*`는 immutable 캐시 헤더.
- **트레이드오프**: next/font 최적화(preload) 포기. FOUT은 font-display: swap + 시스템 폴백으로 수용.
- TanStack Query — 서버 컴포넌트 구조라 클라이언트 fetch 없음. 재검토: 지도 뷰포트 단위 로딩 도입 시.
- 가격 뱃지·가격 지수 — kg 단위 표본 20곳 미만. 재검토: 시즌 중 표본 충족 시.
- 다크 모드(야장모드) — 라이트 우선, 토큰 두 벌만 준비. 재검토: 런칭 후.
- 무인 장시간 루프(/goal 야간 실행 등) — 정지 조건·비용 상한 설계 후. 재검토: 11월.

## 2026-09-01 — Phase 1 지도 메인: 스펙에 없던 UI 결정 6건
- **위치 폴백**: 첫 로드 시 조용히 `geolocation` 요청. 허용 → 내 위치 기준 거리 표시·"가까운순" 정렬, 서울 근교면 지도도 이동(줌 14). 거부·실패·미지원 → 카드에 **구만** 표시(거리 숨김), "가까운순"은 **지도 중심 기준**. 근거: 지도 중심 거리를 "거리"로 보여주면 사용자 위치와 무관한 숫자라 오해를 부른다.
- **목 날짜 상대 이동**: lib/data.ts(목 레이어)에서 JSON의 최신 체크인 날짜가 오늘(KST)이 되도록 모든 날짜를 같은 일수만큼 이동. date-only 값은 KST 달력일로 읽어 UTC ISO로 출력(컨벤션 일치). `isNew`는 정적 플래그 대신 `createdAt` 7일 이내로 파생(spec 5). 6개월 무활동 표본 2곳(p004 철수네포장마차, p115 프로간장새우 옥수)은 1월로 고정. Supabase 교체 시 이 블록 삭제.
- **`needsReview: true` 숨김**: 새우 메뉴 파싱 실패로 검수 대기인 가게는 노출하지 않는다(목 1곳 p108). 검수 후 false면 자동 노출.
- **비서울 노출**: 시드의 서울 밖 가게(김포 등 31곳)도 지도에 보인다 — spec 1 "시드 452곳으로 열고". `/gu/[name]`(Phase 5)는 서울 25개 구만.
- **이벤트 카드 링크 `/test`**: 라우트는 Phase 7. 그때까지 404 허용(설정값이라 갈아끼움). 닫기는 메모리 상태만(규칙 4) — 새로고침 시 재노출, 지속은 서버 단계에서.
- **카드 탭 후 정렬 고정**: 카드 탭으로 지도를 옮긴 직후의 `idle`에서는 "지도 중심" 정렬 기준점을 갱신하지 않는다(탭한 카드가 손가락 밑에서 이동하는 것 방지). 사용자가 직접 드래그하면 다시 갱신.
- **재검토 조건**: 위치 폴백은 사용자 피드백 시, 목 관련은 Phase 6 교체 시 자동 소멸.

## 2026-09-01 — 메인 페이지는 요청 시 렌더 (`await connection()`)
- **맥락**: `app/page.tsx`가 요청 시 API를 안 써서 빌드 시 정적 프리렌더됐다. 그러면 목 날짜 이동·"이번 주"·"○일 전"이 **배포 시각에 고정**되고, `loading.tsx`도 안 보인다. 리뷰(플랜 단계)에서 발견.
- **결정**: `connection()`으로 요청 시 렌더. `now`(ISO)는 서버가 만들어 클라이언트에 props로 내려주고, 클라이언트는 렌더 중 `new Date()`를 부르지 않는다(Workers UTC vs 브라우저 KST 하이드레이션 불일치 + react-hooks 7 purity 룰).
- **spec 6 "핀 목록 캐시 필수"와의 관계**: 목 50곳·정적 JSON이라 지금은 비용 0. Phase 5~6에서 on-demand 재검증(위 Vercel 비용 방어 정책 1번 원칙 그대로)으로 설계할 때 이 결정을 캐시 전략으로 대체한다.

## 2026-09-01 — 패키지 결정 (Phase 1)
- **supercluster 8.0.1 핀**: 마커 클러스터링. 9.0.0이 2026-08-10 출시된 메이저 직후 → 정책대로 한 마이너 대기. `lib/cluster.ts` 래퍼로 감싸 naver 전역 없이 테스트. extent 256(네이버 타일 256px)·radius 60·maxZoom 16.
- **@iconify/react 제거 → @iconify/tailwind4 + @iconify-json/ci**: 기존 것은 미사용이었고 기본 동작이 api.iconify.design 런타임 fetch라 "외부 도메인 금지" 취지와 충돌. 새 방식은 빌드 타임 CSS mask, 쓴 아이콘만 포함. 사용법 `<span class="icon-[ci--search-magnifying-glass]" />`. (docs/plans/phase0-scaffold.md의 @iconify/react 표기는 이 결정으로 대체)
- **eslint-import-resolver-typescript 추가 (하네스 결함 수정)**: boundaries 린트가 `@/` 별칭 import를 external로 오인해 **Phase 0 내내 한 번도 발화하지 않았다**(절대 규칙 1이 기계 강제되지 않고 있었음). 리졸버 추가 + v7 문법(요소=폴더, `lib/data.ts`는 `boundaries/files` 카테고리) + `lib/mock`은 data.ts만 import 가능. 위반 파일을 일부러 만들어 에러 발화를 확인한 뒤 완료 처리 — "하네스는 발화 검증이 완료 조건" 규칙의 사례.
- **바텀시트 직접 구현** (vaul 미채택): 항상 열린 비모달 + Phase 2 드래그 확장·스와이프 닫기까지 제어 필요, vaul 1.1.2는 2024-12 이후 무업데이트. 스냅 위치는 CSS 변수(SSR 첫 렌더부터 정확), JS는 드래그 놓을 때 판정만. 뷰포트 높이 ≤639px이면 half=collapsed.
- **vitest**: tsconfig `jsx: preserve` 때문에 Vite 8이 tsx 테스트를 거부 → vitest 설정에 `oxc.jsx.runtime = automatic` (plugin-react 불필요). `globals: false`라 RTL cleanup을 setup에서 직접 등록.

## 2026-09-01 — "스모크 통과"의 정의
- **맥락**: roadmap Phase 0·1 완료 조건의 "스모크"가 정의돼 있지 않았다(Phase 0 플랜은 "build 성공 = 통과"로 읽었고, ci.yml엔 스모크 스텝이 없었음). Phase 1 갭 스윕에서 "모호"로 보고됨.
- **결정**: 스모크 = ① CI `check` 잡에서 OpenNext 빌드 후 **workerd(`wrangler dev`)를 실제 기동해 `/`가 200 + "새우맵"을 돌려주는지** 확인(2026-09-01부터 ci.yml 스텝) + ② 배포 전 `pnpm preview`로 사람이 한 번 보는 것. 빌드 성공만으로는 런타임 갭(OpenNext 어댑터)을 못 잡는다는 게 이유.
- **재검토 조건**: Phase 5 SSR 라우트 추가 시 스모크 URL 목록(/place/[id], /gu/[name]) 확장.

## 2026-09-02 — Phase 1 UI 리디자인: 버틸까 디자인 언어·토큰 방식 채택
- **맥락**: Phase 1은 기능·테스트·갭 스윕까지 끝났지만 지도 위에 상단바·검색·카운터·이벤트·탭·칩 6개가 따로 떠 "AI가 만든 티"가 났다. 다른 프로젝트 **버틸까**(디자이너 협업, 같은 지도+검색+칩+상시 시트 구조)의 Figma 디자인을 새우맵에 맞춰 이식했다. 참고: Figma "버틸까" `MYU1gTAVOqauq9fUgAz53G` — 지도 홈 596:23173 · 구 상세 176:1140 · Components 156:1204 · Type 156:1175 (Figma Desktop MCP `127.0.0.1:3845`, 이 프로젝트엔 미등록 — 다음 화면부터 `claude mcp add --transport http figma-desktop http://127.0.0.1:3845/mcp`). 기능·상태 로직은 그대로, 표현 계층만 교체. 플랜: docs/plans/phase1-redesign.md.
- **토큰 방식(디자이너 협업 대비)**: `@theme static` + Figma 변수 컬렉션 미러링 — **Primitive**(red/coral/teal/gray 램프, 텍스트 스타일 `text-body-m-medium` 등 12종, radius-6~20·max, shadow-float/upper/marker/fab, spacing-safe-*) 위에 **Semantic**(bg/fg/line/brand/toast). 값은 새우맵 것(쿨 그레이 #191F28 계열, 새우 레드 램프) — 버틸까 색을 복사하지 않음. 기본 팔레트·라운드·그림자·텍스트 스케일은 `initial`로 지워 토큰 밖 값이 클래스로 존재하지 않게 했다. 컴포넌트는 임의값(`text-[13px]`, `#hex`, `rounded-[8px]`) 금지. 토큰 표는 docs/design.md가 단일 출처, 다크는 Semantic만 두 번째 벌.
- **규칙 변경(spec 7·CLAUDE.md 스타일)**: ① 액센트 — "채운 레드 버튼은 화면당 한 곳([+ 제보])", 틴트(활성 칩·"새로 제보됨" 라벨)와 클러스터 마커의 레드는 허용. ② 라운드 — 12px 통일 → 6(미니칩)/8(입력·컨트롤)/12(카드·트랙·토스트)/20(시트)/pill(칩·FAB). ③ 그림자 — 헤어라인 원칙은 유지하되 지도 위에 뜨는 요소(검색 블록·칩·FAB·마커)와 시트 상단(upper)에만 옅게 허용. ④ 타이포 — 자간 -0.02em·행간 1.4를 body 기본으로.
- **배치 변경(design 화면 1)**: 지도 위엔 검색 블록·칩 행 두 층만. "새우맵" 워드마크는 화면에서 제거(sr-only h1만 남겨 CI 스모크의 "새우맵" 문자열·접근성 유지). [+ 제보]·현위치는 시트 가장자리 위 FAB 줄(시트와 함께 이동, full에선 숨김). 시즌 카운터는 시트 헤더 **부제**로, 제목은 **보고 있는 지역 + N곳**("마포구 일대 12곳"/"서울 전체 421곳" — `lib/places.ts areaLabel`: 뷰포트 안 가게의 구 분포만 사용, 0곳 "이 지역" / 전체 60%↑ 또는 구 8개↑ "서울 전체" / 1구 그 구 / 그 외 최다 구+"일대". 지오코딩·외부 API 없음). 이벤트 카드는 시트 본문 첫 요소. 정렬은 드롭다운 → **세그먼트 컨트롤**. 탭(전체/구이/회)과 칩은 같은 pill로 한 줄. 시트 collapsed 88 → 98px(제목·부제까지 보이게).
- **마커**: 클러스터 = 레드 원 44px + 가게 수 / 개별 핀 = 36px 원에 썸네일 + 카테고리색 링, 썸네일 없으면 카테고리 색점 플레이스홀더(새우 아이콘 에셋 대기). 신규 점선·무활동 반투명·선택 확대+잉크 링 규칙 유지. 중간 거리 별도 상태 없음 — supercluster가 뭉침→개별로 자연 전환. `Place.thumbnailUrl`(우리 경로만 — innerHTML 삽입 전 `safeThumbnailUrl` 화이트리스트, 규칙 3의 마지막 방어선)을 선반영하고 목 2곳(p018·p019, 성동구 이웃)에 합성 SVG 샘플만 넣었다. 실제 사진은 업로드 Phase.
- **카테고리 = 드롭다운 칩 (2차 피드백)**: 탭 3개(전체/구이/회)가 토글 칩(새로 들어온 집·찜한 곳)과 같은 줄에 나란히 놓이니 성격이 달라 어색했다 → 버틸까 "기간 ▾" 칩처럼 **[전체 ▾] 드롭다운 하나**(전체/소금구이/생새우회, 선택하면 칩이 틴트로 "소금구이 ▾"). 라벨은 구이→**소금구이**, 회→**생새우회**(`TAG_LABELS` — 상세 등 다른 화면도 같은 라벨. 데이터 태그 grill/raw는 그대로). 드롭다운 목록은 body 포털(fixed 좌표)이라 칩 행 전체를 한 스크롤 컨테이너에 두고 같이 넘긴다(고정해 두면 뒤 칩이 잘려 보인다는 피드백). 열린 채 스크롤하면 닫힌다. 트레이드오프: 카테고리 전환에 탭 1회 추가 — 전환 빈도가 높다고 확인되면 세그먼트로 재검토.
- **사이드 필터 칩 3개 추가 (3차 피드백)**: 토글 칩 행을 [머리버터구이] [라면] [볶음밥] [새로 들어온 집] [찜한 곳]으로 — spec 4.1 "최대 5개" 상한에 도달. 사이드가 앞(로그인 없이 바로 쓰는 필터). 문서 전 구간에서 사이드는 표시 요소로만 있었지만 `Place.sides`·`matchesChips`(AND)가 이미 있어 필터 승격 비용이 작았다. 용어는 **"곁들임" → "사이드"**로 통일(spec·design·코드 카피·aria-label). 사이드 칩이 켜진 채 0곳이면 "이 동네엔 아직 없어요 · 제보"는 오해라 `EmptyKind: "filter"` — "조건에 맞는 집이 없어요" + [필터 해제]. `matchesChips`·빈 상태 분기는 `switch` + `assertNever`로 바꿔 칩·빈 상태 종류가 늘면 컴파일 에러로 잡히게 했다. 목 49곳 분포: 라면 24 · 머리버터구이 10 · 볶음밥 5(전부 라면 동반) · 없음 22. 재검토: 칩이 6개째 필요해지면 사이드 3개를 "사이드 ▾" 다중선택 드롭다운으로 접는다.
- **시트 헤더·카드 리디자인 (4차 피드백, 시니어 디자이너 패스)**: 부제의 굵은 숫자 3개와 회색 박스 2개(이벤트 카드 + 정렬 세그먼트)가 "AI 느낌"의 원인이라 진단. ① 정렬은 세그먼트 → **헤더 오른쪽 텍스트 트리거 "가까운순 ▾"**(카카오맵·버틸까 SortTrigger 문법, 접힌 상태에서도 보임, `DropdownChip appearance="text" align="end"` 재사용). ② 시즌 카운터는 **캡션 한 줄** "● 오늘 6건 확인됐어요 │ 이번 주 47곳" — 라이브 점(green-500, motion-safe 펄스), 구분은 가운데 점 대신 세로 헤어라인, "최다 확인" 세그먼트 삭제(긴 상호 잘림·가치 낮음; `SeasonStats.topPlace`는 유지). ③ 이벤트는 **브랜드 틴트 배너 한 줄**. ④ 카드는 **왼쪽 64px 썸네일 + 텍스트**(캐치테이블·카카오맵·당근 리스트 문법). 사진 없으면 마커와 같은 플레이스홀더(가라앉은 배경 + 카테고리 색점)라 마커↔카드가 같은 가게임을 암시. 카테고리는 색점 대신 메타 텍스트("소금구이 · 생새우회 · 마포구 · 1.2km"), 단위는 칩 대신 회색 텍스트. 시트 collapsed 98 → 88px. ⑤ FAB 줄은 **왼쪽 현위치(40px 흰 원, 표적 아이콘) / 오른쪽 [＋ 제보]**. 표적(크로스헤어) 아이콘은 coolicons에 없어 인라인 SVG로 근접 대체(에셋 목록).
- **이벤트 행·카드 메타 정리 (5차 피드백)**: 이벤트 배너의 em dash(—)와 "› ×" 동거가 AI 티라는 피드백 → 실제 앱의 공지 행 문법(아이콘 타일 + 제목/부제 두 줄 + × 하나, 행 전체 링크). `EventCard.description`(설정값) 추가로 카피를 두 줄로 나눠 기호 연결을 없앴다. 카드에서 대표메뉴·가격 줄 삭제(훑어보기 밀도, 가격은 상세) → 메타 "850m · 마포구 · 소금구이". **거리 기준 변경(플랜 결정 1 갱신)**: 위치 거부 시 거리 숨김 → 내 위치 없으면 **지도 중심 기준**으로 표시("가까운순" 기준점 `origin`과 동일해 순서와 숫자가 항상 일치, 지도를 옮기면 갱신). **평점·리뷰는 카드에 보류**: 리뷰가 거의 없어 "★ –"가 신뢰를 깎고 새우맵의 신호는 확인 신선도. 상세와 같은 규칙(리뷰 3개 이상)일 때만 카드 우측에 "★ 4.5 (12)"를 붙이는 조건으로 기록. 카피 규칙: UI 문장을 em dash·가운데 점으로 잇지 않는다.
- **추가·미채택**: 현위치 버튼은 모바일 스펙에 없었지만 추가(기존 geolocation 재사용, 명시 요청이라 서울 밖이어도 이동, 실패 시 안내만). 버틸까의 하단 탭바는 스펙에 없어 미채택.
- **문서 처리**: 규칙·스펙(CLAUDE.md 스타일, spec 4.1·7장, design 공통 블록·화면 1, roadmap)은 즉시 v2로 정정. design.md 화면 2~9 프롬프트는 v1 초안으로 남기고 배너로 표시 — 각 화면 착수 시 v2 언어로 재작성 후 구현(지금 고쳐도 착수 때 또 바뀌므로).
- **재검토 조건**: 정렬 세그먼트는 써보고 어색하면 드롭다운으로 회귀. 임의값 금지는 위반 2회 시 ESLint 승격. 새우 마커 아이콘 에셋이 오면 플레이스홀더 교체. 지역 라벨 규칙(60%·8구)은 실사용 피드백 시.

## 2026-09-02 — Phase 2 상세: 화면 2 v2 재작성과 스펙에 없던 결정
- **맥락**: roadmap 절차대로 design 화면 2를 v2 언어로 재작성(Figma 버틸까 구 상세 `176:1140`·Components `156:1204` 문법: 핸들 → 제목 20/부제 16 → 섹션 제목 16 semibold → 8px 가라앉은 띠 → 작은 아웃라인 [수정] 28px → 잉크 채움 주 버튼 → 알림 행 = 아이콘 타일 + 두 줄)한 뒤 구현. 플랜 docs/plans/phase2-detail.md.
- **미리 정한 것(사용자)**: 채운 레드 = [다녀왔다면] 레드 pill 한 곳(상세가 열린 동안 FAB 줄을 숨겨 화면당 1을 유지). 길찾기 잉크 채움, 공유·찜 아웃라인. 상단 사진 = 카드·마커와 같은 `thumbnailUrl`. 평점은 리뷰 3개 이상일 때만 별점. 용어 사이드·소금구이·생새우회, UI 문장에 em dash·가운데 점 연결 금지.
- **`/place/[id]` 얕은 라우트 선행(roadmap Phase 5 → Phase 2)**: [공유] 버튼이 실제 링크를 가지려면 라우트가 필요. `app/place/[id]/page.tsx`가 같은 지도 화면을 해당 가게 열린 상태로 렌더(서버에서 상세까지 불러 `initialDetail`로), 열기/닫기는 **이벤트 핸들러 안에서** `history.pushState`/`back`/`replaceState`(Next 16 네이티브 History 통합 — 서버 왕복 없음. 마운트 effect에서 호출하면 AppRouter 패치 전이라 popstate가 리로드함). id 출처는 `detailId` 상태 + `location.pathname`(`useParams`는 `/` 트리를 보고). SSR 메타·OG·진짜 404는 Phase 5. **없는 id는 200 + not-found UI + noindex** — 루트 `app/loading.tsx`(Suspense) 때문에 스트리밍되어 상태 코드를 바꿀 수 없다(Next 문서 loading.md "Status Codes"). 실측(2026-09-02, workerd `pnpm preview`): `/` 200, `/place/p018` 200(상호 렌더), `/place/nope` 200 + not-found 카피 + `<meta name="robots" content="noindex">`(Next 자동 주입). 새로고침 후 닫기는 replace라 `/` 엔트리가 하나 남을 수 있음(무해).
- **영업시간 = 메모 문자열** `Place.hoursNote: string | null`: 제보 플로우(spec 4.3-4)가 자유 메모 입력이고 크롤 데이터에 구조화 영업시간이 없다. "영업 중" 초록 판정은 없음(spec 4.2-5 정정). 목 5곳 샘플.
- **요약 상태 정의**: 같은 본문을 50dvh(최소 300px)로 열어 위에서 잘림(스펙 순서 그대로). 시트는 `mode`(list/detail)로 내용만 전환하고 `SheetSnap` 3종은 유지, 닫기는 4번째 스냅이 아니라 `resolveRelease` 콜백(half에서 아래 플링 또는 halfPx − 100px 아래에 놓음). 본문 드래그: half는 스크롤 잠금 + `touch-action: none`(body에), full은 `pan-y` + non-passive `touchmove`로 맨 위에서 아래로 끌 때만 선점(`setPointerCapture`만으로는 터치 스크롤 선점을 못 막음). 목록 본문은 `hidden`으로 유지해 스크롤·스냅 복원.
- **빈 사진 = 입력 행**(2차 자기 검토): 목 50곳 중 48곳이 사진이 없어 160px 빈 타일은 AI티 + 요약 공간 낭비 → 5번 영업시간 빈 상태와 같은 입력 행 문법(카메라 타일 + "첫 사진을 올려주세요" / "네이버에서 사진 보기 ↗" + ›). 닫기 ×는 상호 행 오른쪽(사진 위 오버레이는 사진 없을 때 자리가 없음). 확인 줄의 절대 날짜는 상대 표기와 중복이라 삭제. 확인 후 pill은 비활성 아웃라인 대신 틴트 "확인했어요".
- **데이터 호출 방식(목)**: 컴포넌트가 `lib/data.ts`를 클라이언트에서 직접 호출. 목 JSON 약 8.5KB gzip이 클라이언트 번들에 들어감(Phase 6 Supabase 교체 시 소멸 — anon 키가 NEXT_PUBLIC_ 허용 목록에 있어 원래 브라우저에서 도는 모양). Server Action은 isolate 전역 캐시를 사용자 간 공유 상태로 만들고 테스트에서 실패 주입이 불가해 미채택. `checkIn`·`getPlaceDetail`은 `now` 필수 인자(클라이언트에서 `Date.now()` 기본값 금지). 실패 시뮬레이션 delay 400ms·10%(`Math.random`). 찜은 클라이언트 메모리 Set(탭 단위, Phase 4에서 사용자 단위).
- **미구현 플로우 입구 8곳**(첫 사진·영업시간·수정 제안·메뉴 알려주기·리뷰 남기기·정보 수정 제안·신고·사장님이신가요?): 자리만 두고 토스트 "준비 중이에요"(FAB [제보]와 같은 톤). 전부 캡션 급이거나 빈 행 전용.
- **사이드 표기**: 있음 = 아웃라인 + 체크, 없음 = 가라앉은 배경 회색(버틸까 Disabled 칩 문법). 필터가 아니라 사실이라 틴트 안 씀. 별 색 = 잉크. coolicons `star`는 라인 별뿐이라 채움/빔이 색으로 구분되지 않아 인라인 SVG 채운 별(`components/ui/icons/star-icon.tsx`)로 대체(에셋 목록).
- **길찾기 딥링크**: `nmap://route/public?dlat&dlng&dname&appname`(NCP "지도 앱 연동 URL Scheme") + 모바일에서 1.5초 안에 앱이 안 열리면(visibilitychange 없음) 웹 폴백. 웹 길찾기 URL은 공식 문서가 없어 네이버 지도팀이 포럼에서 안내한 좌표 표시 링크 `https://map.naver.com/?lng&lat&title`을 쓴다(데스크탑은 처음부터 이 링크, 새 탭). "네이버에서 사진 보기"는 `naverPlaceUrl` 호스트 화이트리스트(`m.place.naver.com`·`map.naver.com`) 통과 시만 렌더.
- **완료 조건의 목업 기준**: 별도 목업 없이 재작성 블록 항목을 gap-sweeper가 전수 대조 + 버틸까 Figma 문법 비교(roadmap Phase 2 문구 정정).
- **재검토 조건**: 요약 높이 50%는 사용감 피드백 시. 수정 입구는 각 Phase 연결 시 재배치. 진짜 404·OG는 Phase 5. 리뷰 사진(`Review.photoUrl`)은 Phase 4 폼에서.

## 2026-09-03 — 공유 링크 직접 진입의 지도 시작점
- `/place/[id]`로 직접 들어오면 지도는 **그 핀·줌 14**(현위치 줌과 동일)에서 시작하고, 지도가 뜨면 핀을 요약 시트 위 가시 영역 중앙으로 `panTo`(카드·마커 탭과 같은 목표). 위치 권한이 있어도 핀이 우선 — 위치는 거리 정렬 기준으로만 쓴다. 이유: 서울 전체(줌 12)로 시작하면 공유받은 핀이 화면에 없다(gap-sweeper 2026-09-03). 줌은 새 상수 대신 기존 현위치 줌을 재사용.
- 요약 상태에서 사진 있는 가게는 1~4 + 5(영업시간) 걸침으로 design 문구를 실측대로 정정. 행 여백을 줄여 버튼 줄까지 넣는 건 디자인 결정이라 하지 않았다(필요하면 별도 결정).

## 2026-09-03 — 화면 2 리디자인: 확인 줄 해체·다중 사진·스크롤 체이닝
- **맥락**: Phase 2로 구현한 상세가 만족스럽지 않다는 사용자 판단. 원인은 색·라운드가 아니라 **리듬의 균일함**이었다 — `아이콘 20 + 14 medium + 12 캡션 + 우측 버튼`이 확인·주소·영업시간 세 행 연속으로 반복되고, 섹션 제목이 전부 16 semibold, 여백이 전부 `py-3`. 플랜 docs/plans/phase2-detail-redesign.md.
- **화면 2 로컬 디자인 규칙**(design.md 화면 2에 같은 표. 다른 화면에서도 맞는 게 확인되면 공통 블록으로 승격):
  1. **아이콘은 액션에만** — 정보 앞 장식 아이콘(핀·시계·체크) 금지. 이 화면에서 아이콘이 붙는 곳은 길찾기·공유·찜·＋·×·사이드 체크뿐. (**정보 블록은 같은 날 예외가 됐다** — 핀·시계는 행 종류를 가리키는 인덱스이자 접힌 주소의 정렬 기준이다. 아래 "정보 블록 미세 조정" 항목. 체크·별 같은 순수 장식은 그대로 금지.)
  2. **행 리스트 대신 정보 블록** — 관련된 값은 붙이고 그룹 사이만 벌린다. 같은 생김새의 행이 세 번 반복되면 화면이 표처럼 읽힌다.
  3. **위계는 회색 계층으로** — fg → fg-secondary → fg-tertiary. 섹션 안을 보더로 나누지 않는다(메뉴 행 헤어라인만 예외: 값이 두 열이라 정렬선이 필요).
  4. **섹션 제목에도 위계** — "사이드"는 칩이 곧 내용이라 12 회색 라벨로 낮춘다.
  5. **여백 리듬은 불균등하게** — 사진 아래 12 / 상호 블록 20 / 정보 블록 16 / 버튼 줄 16.
  6. **빈 상태는 한 줄 + 인라인 액션** — "아직 ~가 없어요 / 첫 ~가 되어보세요" 2연타와 일러스트 금지.
  7. **숫자만 살린다** — 가격·확인 횟수·별점은 semibold + tabular, 단위·날짜·메타는 fg-tertiary로 후퇴.
- **뒤집은 결정**(전부 2026-09-02 항목):
  - *본문 드래그: half는 스크롤 잠금 + `touch-action: none`* → **스크롤 체이닝**. 요약에서도 본문은 스크롤 컨테이너로 두고(`overflow-y: auto` 유지), 터치 제스처만 non-passive `touchmove`가 스냅에 따라 선점한다(half는 어디로 끌든 시트가 먼저, full은 맨 위에서 아래로 끌 때만). 이유: half에서 본문을 잠그면 **휠·키보드 사용자가 본문에 닿을 경로가 아예 없다**(접근성 결함). 체이닝은 Material 3 `BottomSheetBehavior` 기본이자 iOS `prefersScrollingExpandsWhenScrolledToEdge` 기본값이고, 네이버지도·카카오맵·구글맵이 전부 이 동작이다.
  - *채운 레드 = [다녀왔다면] 한 곳, 길찾기 잉크 채움* → **채운 레드 = [길찾기]**. 확인 액션이 기여 블록의 아웃라인으로 내려가 화면당 1은 그대로다.
  - *닫기 ×는 상호 행 오른쪽* → **시트 헤더 오른쪽**(detail 헤더 26 → 44px, `SHEET_DETAIL_HEADER_PX`와 `--sheet-header-h` 동시 정정). 상호와 경쟁하지 않고 사진 유무·스크롤 위치와 무관하게 늘 같은 자리다. ⚠︎ 헤더는 pointerdown에서 즉시 `setPointerCapture`하므로 이 버튼은 `onPointerDown`에서 `stopPropagation`해야 click이 헤더로 리타겟되지 않는다(jsdom이 못 잡는 항목 — Playwright 검증).
  - *빈 사진 = 입력 행(56px)* → **112px 정사각 스트립의 빈 상태**. 사진이 있든 없든 행 높이가 같아 사진 유무로 아래가 밀리지 않는다. 카피와 "네이버에서 사진 보기 ↗"는 그대로.
  - *"○일 전 확인 · 다녀왔다면 →" 확인 줄*(구 spec 4.2-3) → **해체**. 신선도는 상호 블록의 셋째 줄 캡션("어제 확인 · 확인 4회"), 액션은 리뷰 앞 **기여 블록**("여기 다녀오셨나요?" + [✓ 다녀왔어요] [리뷰 남기기]). 확인 줄이 주소·영업시간과 똑같은 정보 행 생김새인데 안에 액션 pill이 들어 있고 위아래 헤어라인으로 갇혀 더 도드라졌다. **"다녀왔다면"은 조건절이라 버튼 문구가 아니다 → [다녀왔어요]로 확정**(문서 전역 정정).
  - *정보 행 3연속(아이콘 + 두 줄)* → **정보 블록 1개**(위 규칙 1~3).
- **가로 제스처는 시트 것이 아니다**(2026-09-03 Playwright 실측 후 추가): 본문 touchmove 선점이 요약에선 무조건, 전체에선 dy가 0이라 "아래로"로 읽혀 **사진 스트립이 터치로 영영 안 넘어갔다**. `[data-pan-x]` 안에서 시작한 제스처는 가로가 우세하면 드래그 후보를 버리고 브라우저에 넘긴다(`touch-action: pan-x`만으로는 preventDefault를 못 이긴다). 세로로 끌면 스트립 위에서 시작해도 체이닝은 그대로.
- **spec 4.2 순서 재번호**: 10항목 → 9항목(구 4·5 주소·영업시간이 정보 블록 하나로, 구 3 확인 줄이 7 기여 블록으로 내려감). 코드 주석의 항목 번호도 같이 정정. 과거 로그의 "spec 4.2-N" 표기는 그 시점 번호 그대로 둔다.
- **다중 사진 데이터 모양**: `Place.photoUrls: string[]`가 원본, `thumbnailUrl`은 `photoUrls[0] ?? null` 파생값(마커·카드는 무변경). 목 JSON은 `"thumbnail": "…"` → `"photos": [...]`, 경로는 기존 `safeAssetPath`(절대 규칙 3 가드) 통과분만.
- **수정 입구는 두 문법으로만**(spec 4.2 "수정 입구는 필드마다" 원칙 자체는 유지): 값이 **없으면** 그 필드 자리가 눈에 띄는 인라인 입구, 값이 **있으면** 섹션 오른쪽에 옅은 [수정](12 fg-tertiary 텍스트 버튼, `components/place-detail/edit-button.tsx`, 호출 3곳). 작은 아웃라인 버튼 4개를 나란히 두면 규칙 2가 말하는 균일함이 그대로 되돌아온다.
- **[리뷰 남기기] 중복 제거**: 기여 블록이 갖고, 리뷰 빈 상태는 "아직 리뷰가 없어요" 한 줄뿐(같은 화면에 같은 액션을 두 번 두지 않는다).
- **재검토 조건**: 요약 높이 예산(사진 112 + 상호 + 정보 + 버튼 줄)은 320에서 버튼 줄이 잘리면 타일 112→104로. 로컬 규칙 표는 화면 3~9 착수 때 재사용해 보고 공통 블록 승격 판단.

## 2026-09-03 — 절대 날짜는 연도까지
- **결정**: 화면에 찍는 절대 날짜는 전부 `2026.08.27` 형식. 연도 없는 `8.27`(`formatKstShortDate`)은 **삭제**했다.
- **이유**: 사용자가 실제 화면을 보고 "9.3 이렇게만 하니까 이상하다"고 지적. 리뷰·사진은 몇 년이 지나도 남는데 `9.3`만으로는 **올해 것인지 3년 전 것인지 구분이 안 된다**. 지도 데이터의 신뢰는 "언제 것이냐"에 걸려 있어 이 모호함은 비용이 크다.
- **범위**: `review-section.tsx` 리뷰 날짜, 화면 7 등록일, 앞으로 붙는 사진 업로드일. **상대 시간은 그대로** — "어제 확인 · 확인 4회"처럼 신선도를 말하는 자리는 연도가 오히려 방해다.
- **뒤집은 결정**: 2026-09-02 design.md의 리뷰 행 `"8.27"` 표기.

## 2026-09-03 — 사진 확대 뷰어·사진 신고·10장 상한
- **맥락**: 리디자인한 상세를 실제로 보고 나온 요구 — 사진이 작다 / 여러 장이면 어떻게 되는지 모르겠다 / 탭하면 크게 보고 싶다 / 익명 업로드라 이상한 사진이 올라올 수 있으니 신고와 올린 날짜가 필요하다. 플랜 docs/plans/phase2-photo-viewer.md.
- **스트립 112 정사각 → 176×128 가로형**(≈4:3). 정사각으로는 접시가 잘려 음식이 안 보였다. 4:3 정확값 170.67px은 임의값이라 토큰 유틸 `w-44`(176)로 내렸다 — 실제 비율 11:8, 눈에 안 보이는 차이. 요약 높이 예산의 여유가 32 → 16px로 줄지만 390×844에서 4번 버튼 줄까지 보이는 수용 기준은 유지된다(320에서 잘리면 타일 높이를 112로 내린다 — 폭이 아니라 높이를 줄인다).
- **빈 상태 ＋ 타일만 176 → 112 폭**(2026-09-03 저녁, 320 실측 후): 타일이 176이면 320에서 오른쪽 안내 열이 49px밖에 안 남아 "첫 사진을 올려주세요"(113px)가 네 줄로 접힌다. **불변식은 "행 높이가 같다"이지 "폭이 같다"가 아니다** — 높이 128은 그대로 두고 폭만 좁혔다(280 − 112 − 12 − 12 − 16 = 128 ≥ 113). 사진이 있는 스트립의 타일과 끝 ＋ 타일은 176 그대로(빈 상태와 나란히 보이는 일이 없다).
- **사진 상한 10장**(`MAX_PLACE_PHOTOS`, lib/data.ts). UI 상수가 아니라 도메인 규칙이라 데이터 계층에 두고 변환에서 `slice`로 막는다. 근거: 한 가게에 필요한 건 음식·간판·내부 정도고, 익명 업로드라 상한이 없으면 도배가 가장 싼 공격이다.
- **데이터 모양 `photoUrls: string[]` → `photos: Photo[]`**(`{ id, url, uploadedAt }`). 업로드 날짜를 보여주려면 배열 원소가 문자열일 수 없다. 이름을 그대로 두면 필드가 거짓말이 되므로 개명했다(참조처 7곳). **`thumbnailUrl = photos[0]?.url ?? null` 파생은 유지** — 카드·마커는 무변경. 목 단계의 `id`는 `{placeId}-p{n}` 파생이고 Phase 6에서 DB uuid로 바뀐다.
- **뷰어는 네이티브 `<dialog>` + `showModal()` + body 포털**. 이유 둘: (1) `.saeu-sheet`가 `transform: translateY()`라 시트 안의 `position: fixed`는 뷰포트가 아니라 시트를 기준으로 잡힌다 — 전체 화면이 안 나온다. (2) top layer는 z 경쟁(시트 z-20, 드롭다운 z-30, 토스트 z-10) 자체를 없애고 포커스 트랩·`inert`·Escape·포커스 복원을 공짜로 준다. 대가는 jsdom에 `showModal`이 없어 `vitest.setup.ts`에 스텁 4줄.
- **좌우 넘김은 CSS scroll-snap, JS 제스처 없음**. 바텀시트가 겪은 pointer-capture 리타겟·non-passive touchmove 함정은 전부 "세로 시트 vs 가로 스트립" 경합에서 나온 것인데, 포털된 단독 표면에는 그 경합이 없다.
- **뒤로가기**: 뷰어를 열 때 `history.pushState({saeuDetail:true, saeuPhoto:true}, "", location.pathname)` — **URL은 바꾸지 않는다**(바꾸면 `use-map-screen`의 `PLACE_PATH` 재동기화가 깨지고, 사진 인덱스는 아직 공유할 자원이 아니다). ⚠︎ **`openDetail` 멱등 가드가 이 설계의 전제다** — pathname이 그대로라 뷰어를 닫을 때 popstate가 같은 id로 `openDetail`을 다시 부르고 `setSnap("half")`가 실행돼 **전체로 펼쳐 둔 시트가 요약으로 튄다**. 같은 id면 no-op으로 막는다.
- **뒤집은 결정 — 신고는 "준비 중이에요" 토스트가 아니다**(2026-09-02 미구현 입구 목록). **사진 신고만** 사유 선택 + 목 저장까지 실제로 동작한다(가게 신고·리뷰 신고는 그대로 토스트). 이유: 신고는 다른 미구현 입구와 성격이 다르다 — 나머지는 "아직 못 만든 편의 기능"이지만, 익명 업로드 이미지에 대한 신고는 **없으면 안 되는 안전장치**다. 사유 4개(부적절한 사진/다른 가게 사진/광고·도배/기타), **행 탭이 곧 제출**이라 확인 버튼이 없다 — 고르고도 안 낸 상태를 만들지 않는다(네이버·인스타 신고 시트의 문법).
- **실패 토스트는 뷰어 안에 따로 렌더한다.** 기존 토스트는 z-10이라 top layer 아래에 무조건 가리고, 신고 실패 시에는 뷰어를 닫으면 안 된다(무엇이 실패했는지 사라진다). 성공만 뷰어를 닫고 기존 토스트를 쓴다. 타이머 호출자가 셋이 되면 `lib/use-notice.ts`로 승격한다.
- **새 토큰 4개**: `bg-immersive`(common-100) · `fg-on-immersive`(common-0) — 딤·몰입 표면 Semantic 자리가 비어 있었다. 여기에 `bg-immersive-raised`(gray-800) · `fg-on-immersive-secondary`(gray-400)를 더했다(아래 항목).
- **뷰어 chrome 재조정**(같은 날, 실제 화면을 보고): 날짜·[신고]가 12 맨 텍스트라 **작고 떠 있었다** — [신고]의 실측 탭 영역이 20×17px로 44px 기준에 한참 못 미쳤다. 날짜는 14 medium + 회색 " 등록"(그냥 날짜만 있으면 무슨 날짜인지 모른다), [신고]는 아이콘 16 + 14 medium의 **44px 알약**, 상단 ✕·카운터도 같은 알약 표면으로 통일. 근거 둘: (1) 탭 영역 44px은 타협 대상이 아니다, (2) 흰 접시 사진이 뒤에 오면 맨 흰 아이콘·글자는 배경과 같은 색이 된다 — 몰입 표면의 컨트롤에는 자기 배경이 필요하다(인스타·네이버 뷰어의 문법). 알약 색은 브랜드 레드가 아닌 gray-800이라 "채운 레드는 화면당 한 곳" 규칙과 무관하다.
- **뒤집은 결정**: 같은 날 위에서 정한 뷰어 상·하단 "12 텍스트" 표기(design.md 변형 (e) 두 줄).

## 2026-09-03 — 상세 요약 높이 50% → 30%(최소 270px)

- **결정**: 상세 시트의 기본 스냅을 `max(50dvh, 300px)` → **`max(30dvh, 270px)`**. 요약에서 보이는 것은 사진 스트립 + 상호 블록(상호·카테고리·**"3일 전 확인"**)까지고, 정보 블록·버튼 줄은 본문 스크롤이나 전체 펼침으로 본다.
- **이유**: 사용자 피드백 — "지도를 너무 가리는 느낌". 지도 화면에서 시트가 화면 절반을 먹으면 **정작 지도가 보조가 된다**. 2026-09-02 항목의 재검토 조건("요약 높이 50%는 사용감 피드백 시")이 그대로 발동한 경우다.
- **하한 270px은 임의값이 아니다**: 헤더 44 + 사진 144 + 상호 블록 = 확인 캡션 바닥 **257px**(320·390, 긴 상호 포함 실측 동일 — 상호는 1줄 truncate) + 바닥 여백 13. 30dvh만 쓰면 844에서 253px이라 캡션이 4px 잘리므로 `max()`의 하한이 실제로 이기는 값이다(1000px 이상 뷰포트에서만 30%가 이긴다).
- **뒤집은 결정**: 2026-09-02 "요약 상태 정의 = 50dvh(최소 300px)", 2026-09-03 "요약에서 4번 버튼 줄까지 보이는 것이 수용 기준"(사진 176×128 항목). **사진 타일 크기는 그대로 둔다** — 높이 예산이 더는 버튼 줄에 묶여 있지 않으므로 128을 112로 내릴 이유가 사라졌다. 320×568에서 [길찾기]가 접히던 것도 이제 결함이 아니라 정의된 동작이다.
- **주 액션이 접히는 대가**: [길찾기](채운 레드)가 요약에서 안 보인다. 받아들이는 근거는 (1) 상세를 연 직후의 판단 재료는 사진·상호·신선도이고, 길찾기는 그 판단 **뒤**에 오는 액션이며, (2) 본문이 스크롤 컨테이너라 요약에서도 손가락 한 번이면 닿는다(스크롤 체이닝). 네이버지도·카카오맵도 첫 스냅은 이름·거리·신선도까지다.
- **재검토 조건**: 길찾기 도달률이 문제가 되면 요약 높이가 아니라 **요약 상태의 고정 액션 바**(시트 하단에 겹치는 줄)를 검토한다 — 높이를 되돌리지 않는다.

## 2026-09-03 — 카테고리 라벨 소금구이 → 새우구이

- **결정**: 화면 라벨 `TAG_LABELS.grill`을 **"새우구이"**로. 데이터 태그 이름(`grill`/`raw`)과 카테고리 색(코랄/틸)은 그대로.
- **이유**: 사용자 요청. "소금구이"는 조리법 하나(소금 위에 굽기)를 가리키는데, 이 태그가 실제로 묶고 있는 건 대하·왕새우 소금구이부터 버터구이·양념구이까지다 — 라벨이 카테고리보다 좁았다.
- **뒤집은 결정**: 2026-09-02 "라벨은 구이→소금구이, 회→생새우회"(카테고리 드롭다운 항목). **생새우회는 그대로 둔다.**
- **바꾸지 않은 것**: 가게·메뉴 이름 안의 "소금구이"(대하소금구이·왕새우소금구이 등)는 크롤링 데이터라 손대지 않는다. 라벨만 바뀌었고 데이터는 그대로다.

## 2026-09-03 — 빈 사진 상태 최종: 전폭 빈 상태 블록 + 두 줄 카피 (같은 날 3안 폐기)

- **결정**: 사진 0장이면 좌우 20 여백의 **전폭 블록**(높이 128, bg-sunken, 라운드 12) 하나 — ＋(24) / "아직 사진이 없어요"(14 medium) / "첫 새우를 올려주세요"(12 fg-tertiary)를 세로 가운데, **블록 전체가** 사진 올리기 표적. **"네이버에서 사진 보기"는 사진 유무와 무관하게 리뷰 끝 한 자리**로 통일했다(빈 상태 옆자리에서 뺐다 — 규칙이 둘일 이유가 없다).
- **버린 3안**(전부 같은 날, 화면을 보고 받은 피드백):
  1. **112 타일 + 오른쪽 텍스트 열 + chevron** — "빈 회색 썸네일이 달린 리스트 행"으로 읽혔다("애매하다"). 표적이 행 전체라 안쪽 네이버 링크와 겹쳤다.
  2. **56px 전폭 바**(카메라 + 문구 + chevron) — 무사진 94%를 근거로 높이를 줄였지만 비례를 잃었다("세로로 짧고 가로로만 풀로 한 게 별로"). 요약에서 주소 줄까지 보이던 이점은 여기서 포기.
  3. **176 ＋ 타일 하나 + 오른쪽 네이버 링크** — 타일 오른쪽에 담기지 않은 흰 공간이 남아 미완성으로 읽히고, 320에선 링크가 두 줄로 접혔다.
- **카피 규칙(정정) — 컨테이너가 정한다**: 버튼 라벨은 **액션 라벨**(스트립 안 ＋ 타일 = "사진 추가". 네이버·카카오 "사진 등록", 구글·당근 "사진 추가"), 빈 상태 블록은 **상태 한 줄 + 요청 한 줄**이 표준 문법이다. 그래서 design.md의 「"아직 사진이 없어요" 카피 금지」를 **뒤집는다** — 금지의 진짜 대상은 *작은 행에 문장을 우겨넣는 것*이었지 빈 상태의 문장 자체가 아니다. "첫 새우를 올려주세요"는 새우맵의 첫 마스코트 말장난(사진=새우, 올리다=업로드/굽다)이고, 앞으로 다른 빈 상태 카피도 이 톤에 맞춘다.
- **밋밋함 해소는 색이 아니라 선으로**(캐치테이블 "AI가 요약했어요" 카드 참고 요청): 회색 채움 → **채움 없음 + 옅은 브랜드 헤어라인**(`line-brand` = red-50) + ＋ 아이콘만 brand-fg. 새 Semantic 토큰 `line-brand` 1개 추가(토큰 표 갱신).
- **그라데이션 보더·배경은 채택하지 않았다**: ① 캐치테이블에서 그 그라데이션은 장식이 아니라 **"AI가 생성한 내용"이라는 표식**이고 한 화면에 한 장뿐이다. 우리 빈자리에 쓰면 아무 의미도 없는 채로 가장 시끄러운 요소가 된다. ② 무사진이 94%라 그 시끄러운 블록이 상세의 기본 첫인상이 되고, 화면당 하나여야 할 채운 레드 [길찾기]와 시선이 경합한다. ③ 디자인 언어의 "그라데이션 배경 금지"(2026-09-02)를 뒤집을 근거가 못 된다. 실제로 시트에 얹어 4안(회색 채움 / 흰 바탕+브랜드 헤어라인 / 틴트 채움+보더 / 그라데이션 보더+틴트)을 렌더해 비교한 결과다.
- **채운 틴트(red-10)도 기각**: 신규 핀 공지 행이 이미 채운 틴트라 무사진 신규 가게에서 틴트 두 덩이가 겹쳐 보인다. 무게는 공지 > 빈자리 순서를 지킨다.
- **불변식 복구**: 행 높이는 두 상태 모두 144(pt 4 + 128 + pb 12). 요약에서 확인 캡션까지 320·390 모두 여유 13px(실측).
- **비용**: 무사진이 94%인 지금은 요약에서 바로 눌리던 "네이버에서 사진 보기"가 전체 펼침 + 스크롤 뒤로 내려갔다. 사진이 쌓이면 사라질 비용이라 규칙 단순화(링크는 한 자리)를 택했다.

## 2026-09-03 — 상세 정보 블록에 최근접 지하철역 + 호선 배지

- **결정**: 화면 2 정보 블록 첫 줄에 `(3)(8) 가락시장역 2-1번출구에서 90m`를 넣는다. 캐치테이블 상세의 문법을 가져오되 **거리는 역 중심이 아니라 가장 가까운 출구까지** 잰다. 역이 멀거나 없으면(`STATION_NEARBY_MAX_M` = 800m) 그 줄을 안 그리고 주소만 남긴다. 도로명 주소는 `fg-secondary`로 한 단계 낮춘다 — "어느 역 근처냐"가 서울에서는 도로명보다 먼저 읽히는 정보다.
- **거리 기준을 출구로 잡은 근거(실측 50곳)**: 역 중심 기준 중앙값 255m → 출구 기준 **218m**. 출구가 **중앙값 53m·평균 61m 짧고**(최대 234m: 삼각지역 439m→205m) 46곳이 짧아졌다. 사람이 걷기 시작하는 지점은 역 중심이 아니라 출구다. 출구 번호는 50/50 확보됐다.
- **정확도(직접 물어서 답한 값)**: ① 역 좌표는 Wikidata(CC0, 독립 소스) 대조 편차 중앙값 21m·평균 31m, 100m 이내 46/47, 최대 145m(옥수). ② 가게 좌표는 독립 지오코딩으로 건물이 특정된 4곳에서 1·4·4·24m(동피랑 1m, 동강해물찜 4m — 상호까지 일치). ③ "어느 역이냐"는 1위↔2위 격차 중앙값 406m(최소 44m)로 좌표 오차보다 훨씬 커서 안 흔들린다. **그래도 직선거리라 실제 도보는 1.2~1.4배**이므로 캐치테이블의 1m 단위("361m")를 따르지 않고 기존 `formatDistance`의 10m 반올림을 쓴다 — 좌표 오차 ±30m에 맞는 해상도이고 화면 1 카드 표기와도 같다.
- **임계값 800m는 데이터가 아니라 코드(`lib/data.ts`)가 갖는다**: JSON에는 사실(역·출구·미터·노선)만 굽고 컷은 파생에서 한다. 값을 바꿔도 재생성이 필요 없고 픽스처로 두 분기를 테스트할 수 있다. 목 기준 47/50이 걸리고 까치산 980m·고촌 1,022m(김포시 — "지방" 케이스)·화곡 1,087m가 주소만 남는다.
- **출처는 OpenStreetMap / Overpass (ODbL, 키 불필요)**. 역 621 + 정차점 5,856 + 출구 2,757개(≈3.6MB)를 클라이언트 번들에 올리지 않고 `scripts/add_nearest_station.py`가 `places.json`에 결과만 굽는다(+414줄). 좌표·주소와 같은 성격의 시드 시점 외부 파생 사실이고, Phase 6에선 서버 쓰기 시점에 채우는 컬럼이 될 자리다. **후속 과제**: 실서비스 전 공공데이터(국토부 역사 표준데이터, 공공누리 1유형)로 재생성 — 스크립트의 fetch 함수 3개만 교체.
- **실측으로 깨진 접근 4개(다시 시도하지 말 것)**:
  1. **호선을 역 `ref`(역번호)에서 뽑기** — 최근접 50곳 중 32곳에 `ref`가 없다. 노선 릴레이션을 거쳐야 50/50이 된다.
  2. **릴레이션 멤버를 `node(r)["railway"="station"]`로 받기** — 한국 노선 릴레이션의 멤버는 역 노드가 아니라 승강장 정차점(`railway=stop`)이라 전부 걸러져 빈 결과가 나온다. 멤버를 통째로 받아 **역이름 일치 + 400m 이내**로 잇는다.
  3. **Wikidata P81을 노선 출처로 쓰기** — 운행 노선이 아니라 "경부선·용산선" 같은 시설 노선이 나오고 색도 항목마다 불일치. 좌표 교차검증에만 쓴다.
  4. **역 이름으로 중복 제거** — 같은 이름 다른 위치가 실재한다(신촌 2호선↔경의중앙선 **704m**, 수서 360m, 청량리 327m). 노드를 그대로 두고 최근접을 고르면 이름이 저절로 맞는다.
- **데이터 정제 규칙 2개**: 출구 `ref`에 `"엘리베이터"`·`"한강진역 2번출구"` 같은 오염 값이 섞여 있어 `^\d{1,2}(-\d)?$`만 채택한다(아니면 역 중심 거리로 폴백). 역명이 이미 "역"으로 끝나는 경우(`서울역`)에 "역"을 또 붙이지 않는다. 괄호 이름 `총신대입구(이수)`는 공식 표기라 그대로 둔다.
- **표시 문법**: 노선색 16px 원(흰 숫자 12/600 — 처음 20px로 넣었다가 같은 날 축소, 아래 "정보 블록 미세 조정" 항목) + 역명 + 출구 + 거리. 환승역은 **숫자 호선을 전부** 나란히 놓고(서울 최대 3개) 개수를 고정하지 않는다. **숫자가 없는 노선(수인·분당, 김포 골드라인 — 목 4/50)은 배지 없이 역명만** 쓴다: 원 안에 넣을 숫자가 없고 한글 약자("수")는 관습이 아니며, 색 원만 그리면 의미를 못 읽는다. **지하철 아이콘은 coolicons에 아예 없다**(`ci` 715개 중 train/subway/metro/rail 0건) — 배지 자체가 노선 표지라 근접 대체도 두지 않는다. (정보 블록 앞의 핀·시계 아이콘 열은 같은 날 추가했다 — 아래 "정보 블록 미세 조정" 항목.)
- **보라색 금지 규칙의 예외**: 5호선 #794698은 우리가 고른 색이 아니라 **사실 표기**라 그대로 쓴다. 16px 배지 안에서만이고 브랜드 레드 1개 원칙·카드 색 띠 금지는 그대로다. 토큰도 Semantic 역할 없이 Primitive `subway-1`…`-9` 9개로 닫는다(숫자 호선만 배지를 그리므로 더 늘지 않는다).
- **배지는 `aria-hidden`, 호선은 텍스트로 읽힌다**: 9호선 #A49D87 위 흰 숫자는 대비 2.4:1이다. 색은 보조 신호고 정보는 텍스트가 갖는다. (수단은 `sr-only` 조각 → 헤더 버튼 `aria-label`로 같은 날 교체 — 아래 "정보 블록 미세 조정" 항목.)
- **한 줄 폭 규칙과 출구 생략 폴백**: 320에서 이 줄이 쓰는 폭은 265px(Playwright 실측). 넘치면 **출구만 떼서** `동대문역사문화공원역에서 990m`로 줄인다 — 출구 없는 형태는 서울 최장 역명 + 배지 3개(실측 259.4px)에서도 들어간다. 판정은 브라우저 측정이 아니라 문자 폭 근사(`estWidth`)로 해서 Vitest로 검증한다. **근사는 실측보다 0.8~3.4px 작게 나오므로 예산을 265가 아니라 259로 잡는다**(플랜은 과대추정이라고 적었는데 실측이 반대였다 — 265 그대로면 `총신대입구(이수)역 5번출구에서 320m` 추정 265 / 실측 265.8이 통과했다가 잘린다). 뷰포트마다 다르게 판정하지 않고 320 기준 하나로 통일한다: 390에서만 출구가 나타났다 사라지면 더 이상하다. 현재 목 50곳은 전부 출구를 유지하고(최악 가산디지털단지 255), 마지막 안전망으로 `truncate`를 건다. (같은 날 핀·chevron이 44px을 먹으면서 예산은 **217**로 재도출됐고 가산디지털단지 2곳이 폴백에 들어갔다 — 아래 "정보 블록 미세 조정" 항목.)
- **도로명을 `fg-secondary`로 낮췄다**: 서울에서는 "어느 역 근처냐"가 도로명보다 먼저 읽힌다. 역 줄(fg) → 도로명(fg-secondary) → 지번(fg-tertiary) 3단이고, 간격은 역↔도로명 2로 "여기가 어디냐" 한 묶음이다. 복사 버튼은 역 줄이 아니라 도로명 행에 붙는다 — 역 줄과 같은 flex 행에 넣으면 `items-start`가 버튼을 역 줄 옆으로 올려 "역을 복사"로 읽힌다. (같은 날 주소를 드롭다운으로 접으면서 [복사]는 펼친 패널 안으로 들어갔다 — 아래 "정보 블록 미세 조정" 항목.)
- **Overpass 운영 메모**: 태그 조합을 서버에서 union하면 504가 난다(역 질의는 통째로 받아 파이썬에서 거른다). 지하철·경전철과 일반철도 릴레이션은 한 질의에 넣으면 타임아웃 — 두 번으로 쪼갠다. 스크립트는 `--cache`로 응답을 재사용하고, 재실행 결과가 바이트 단위로 같다(idempotent).

## 2026-09-03 — 상세 정보 블록 미세 조정: 아이콘 열·16px 배지·접히는 주소·지번 축약

캐치테이블 상세 스크린샷을 다시 대조하고 같은 날 넣은 역 줄을 네 군데 고쳤다.

- **"아이콘은 액션에만" 규칙을 정보 블록에서 뒤집는다**(design.md의 그 줄도 같이 고쳤다). 위치 그룹 앞에 핀(`ci--location`), 영업시간 앞에 시계(`ci--clock`) 16px을 세운다. 근거 두 개: ① 값이 4종(역·도로명·지번·영업시간)으로 늘면서 "이게 어떤 값이냐"를 회색 계층만으로는 못 구분한다 — 아이콘은 장식이 아니라 **행의 종류를 가리키는 인덱스**다. ② 접힌 주소가 핀 아래로 들여쓰기되려면 **정렬 기준**이 필요하다(아이콘 열 22px = 16 + gap 6). 값 앞의 체크·별 같은 순수 장식 아이콘은 여전히 금지다.
- **배지 20px → 16px(`size-4`)**. 20px은 14px 글자의 행 높이(19.6px)를 꽉 채워 배지가 탭할 수 있는 버튼처럼 보였다. 16px이면 글자보다 확실히 작아 "역 이름에 붙은 표지"로 읽힌다. 숫자는 12/600 그대로다(원이 작아져도 두 자리가 들어갈 일은 없다 — 숫자 호선은 1~9).
- **주소는 기본 접힘, 역 줄이 그 disclosure의 헤더다.** 도로명+지번이 상시 두 줄을 먹을 이유가 없다 — 주소는 길찾기·복사할 때만 쓰는 값이고, 서울에서 먼저 읽히는 위치 정보는 역이다. 역 줄 오른쪽에 chevron ⌄(펼치면 180° 회전, Tailwind v4에서 `rotate-180`은 `transform`이 아니라 `rotate` 프로퍼티라 계산값은 `rotate: 180deg`로 확인한다), 펼치면 도로명·지번·[복사]가 핀 아래로 들여쓰기돼 나온다.
- **역이 없으면(800m 밖) chevron도 없고 주소가 그냥 보인다.** 헤더와 본문이 같은 disclosure는 만들지 않는다 — 누를 대상이 없는데 접는 것은 상태만 늘린다. 이때 도로명도 `fg-secondary`를 유지한다: 정보 블록에서 진한 값(fg)은 "가장 먼저 읽히는 하나"(역 줄)뿐이고, 같은 필드가 케이스마다 다른 무게를 갖는 게 더 나쁘다.
- **"주소가 왜 두 개냐"는 표기 문제였다**(도로명·지번 둘 다 필요한 값이라 지우지 않았다). 원인 두 개를 각각 고쳤다: ① 둘이 항상 같이 보였다 → 드롭다운으로 접었다. ② 지번이 `"서울 송파구 가락동 600"`처럼 시·구까지 달고 나와 바로 위 도로명과 접두어가 겹쳤다 → **동·번지만 남긴다**(`shortJibun`: `(동|가|리|읍|면)\d*가?$`로 법정동을, `^산?\d+(-\d)?$`로 번지를 찾는다). 뒤에 붙은 건물명·층(`"… 400-1 금강리빙스텔 2층 팔팔수산"`)은 지번이 아니라 크롤링 잔재라 같이 자른다. **형식을 못 읽으면 원문을 그대로 둔다** — 모르는 형식을 잘라 틀린 주소를 만드는 것보다 길게 두는 게 낫다. 복사되는 값은 손대지 않은 도로명 원문이다.
- **한 줄 예산 259 → 217 재도출.** 핀(16+6)과 chevron(16+6)이 44px을 먹는다: 265(320 실측 콘텐츠 폭) − 22 − 22 − 4(폭 근사의 관측 최대 과소분 3.4 올림) = **217**. 그 결과 8자 역명 + 배지 2개가 걸려 **가산디지털단지역 2곳**(p261·p268)이 출구를 뗀 폴백으로 내려갔다 — 전수 계산으로 확인한 유일한 2곳이고 최종 폭 216/217이다. 10자 역명 + 배지 3개(동대문역사문화공원역)는 **폴백도 예산을 넘어** `truncate`가 받는다: 서울에 그 조합이 이 역 하나뿐이라 폴백 사다리를 더 만들지 않는다. 실측(320/390/430)에서 표시되는 47곳 전부 한 줄이고 잘림 0건이다.
- **호선 낭독은 `sr-only` 조각이 아니라 헤더 버튼의 `aria-label`로 한다.** 접근 이름 계산(dom-accessibility-api)은 자식 요소의 기여를 각각 trim한 뒤 **구분자 없이 이어 붙인다** — `sr-only` "9호선" + 라벨이 `"9호선송파나루역 2번출구에서 370m"`으로 읽혔다(span 안에 공백을 넣어도 trim된다). 버튼에 `aria-label={["3호선","8호선", 라벨].join(" ")}`을 주면 문장으로 읽힌다. 배지는 `aria-hidden` 그대로.
- **스켈레톤에 주소 자리를 두지 않는다**: 기본이 접힘이라 실제 화면도 아이콘 열 + 값 두 줄(역·영업시간)이다.

## 2026-09-03 — 상호 블록은 3줄 유지 (구 삭제·확인 캡션 승격 검토 후 기각)

주소를 드롭다운으로 접은 직후 "`생새우회 · 송파구`에서 구를 빼고 그 자리에 `3일 전 확인 · 확인 0회`를 올리자"를 검토했고 **현재 형태를 유지**한다.

- **구를 남긴다**: ① 주소를 접은 뒤로 상세에서 행정구역을 말하는 자리는 이 줄뿐이다(`place-header.tsx` 한 곳. 하단 링크 줄에도 없다) — "주소에 어차피 나온다"는 근거가 같은 날 사라졌다. ② 구는 이 제품의 1급 축이다(`/gu/[name]`, 시트 헤더 "마포구 일대 12곳", 화면 1 카드도 구를 쓴다). ③ 공유 링크로 바로 들어오면 지도 컨텍스트가 없고, 역명으로 동네를 아는 것은 서울 사람 기준이다. 역 줄은 "얼마나 가깝냐", 구는 "어느 동네냐"로 다른 값이다.
- **확인 캡션을 14 fg-secondary로 올리지 않는다**: ① 무확인이 흔한 단계라 "확인 0회"를 승격하면 첫인상이 불안이 된다 — 신선도는 조용히 말한다(design 화면 2 항목 2). ② 분류·시간·횟수 세 종류를 한 줄에 점으로 이으면 회색 3단(fg/secondary/tertiary)이 죽는다. 그 3단이 정확히 이 세 값을 나누는 장치다. ③ "확인 횟수는 tabular만, 굵기는 올리지 않는다"와도 충돌한다.
- **줄 수를 줄이는 지렛대는 여기가 아니다**: 요약 스냅에서 버튼 줄을 올리고 싶으면 19px 캡션이 아니라 사진 블록 높이(128)가 지렛대다. 그건 이미 3안을 버리고 정한 값이라 다시 열지 않는다.
- **열어 둔 변형 1개**(요청 시): 둘째 줄을 분류만 남기고 구를 메타 줄로 내리기 — `생새우회`(14) / `송파구 · 3일 전 확인 · 확인 0회`(12). 줄 수는 그대로지만 둘째 줄이 순수 분류가 되고 위계도 안 깨진다.
- 카드(`송파구 · 생새우회`)와 상세(`생새우회 · 송파구`)의 순서 차이는 의도로 둔다: 목록은 지역부터 스캔하고, 상세는 이미 그 가게 안이라 분류부터 읽힌다.

## 2026-09-03 — 사진 뷰어는 dialog를 열고 나서 첫 슬라이드를 잡는다 (Codex PR #5 P1·P2)

- **`showModal()`과 첫 위치 지정은 한 layout effect 안에서 이 순서로.** 열기가 passive effect(`useEffect`)에, 위치 지정이 `useLayoutEffect`에 나뉘어 있으면 React가 layout effect를 먼저 흘린다. 그때 dialog엔 `open`이 없어 `display:none`이라 트랙의 `clientWidth`가 0이고 `scrollLeft = 0 × n = 0`이다. 스크롤이 안 났으니 `syncIndex`도 안 돌아 **화면은 1번 사진인데 카운터·등록일·신고 대상만 고른 사진**을 가리켰다(스트립 2번째 이후 전부).
- **이 회귀는 dev에서 안 보인다.** App Router는 StrictMode가 기본이라 effect를 두 번 돌리고, 두 번째 layout effect는 첫 passive effect(=`showModal()`) **뒤에** 실행돼 `clientWidth`가 잡힌다. effect 순서 버그는 `pnpm build && pnpm start`로만 판정한다. 계측(`scrollLeft` setter + `showModal` 후킹)으로 순서를 직접 봤다: 수정 전 `set 0(cw 0, open false) → showModal`, 수정 후 `showModal → set 780(cw 390, open true)`.
- **같은 포트에 남은 이전 빌드 청크를 브라우저가 재사용한다.** 고친 뒤 같은 포트에서 다시 재니 버그가 그대로 나왔는데 번들 원본엔 수정이 들어 있었다 — `rm -rf .next` + **새 포트**로 재야 맞는 값이 나온다. 프로덕션 재검증은 포트를 바꿔서 한다.
- jsdom은 `clientWidth`가 항상 0이라 이 버그를 구조적으로 못 잡는다(테스트 208개가 전부 통과한 채 나갔다). 검증은 Playwright + 프로덕션 빌드로: p025의 3·10·1번, p018의 3번에서 **보이는 슬라이드 = 누른 번호**.
- **지번 축약 폴백 보강**: 동은 찾고 번지를 못 찾으면(`경기 김포시 고촌읍`, `123번지` 표기) 끝까지 자르지 않고 **원문 그대로** 반환한다 — 바로 위 항목의 "형식을 못 읽으면 원문"이 반쪽만 구현돼 시·도가 조용히 사라지고 있었다. 목 49곳엔 해당 케이스 0건. 기존 테스트가 이름은 "원문 그대로"인데 단언은 `"고촌읍"`이라 **버그를 정답으로 고정**하고 있었다 — 문서가 맞고 코드·테스트가 틀린 경우다.

## 2026-09-04 — 제보 플로우(화면 3) v2: 구는 경계 판정, 주소는 비워 두고, 단계마다 CTA 하나

Phase 3 착수. design 화면 3을 v1 초안에서 v2(화면 2와 같은 언어)로 재작성하며 정한 것. 계획은 `docs/plans/phase3-report.md`.

- **구(gu)는 핀 좌표를 서울 25구 경계로 판정한다.** 저장은 사용자가 확정한 핀 좌표뿐이고 도로명·지번은 `null`로 둔다(`Place.addressRoad`가 nullable이 된다). 상세 정보 블록에는 "주소를 알려주세요" 입구가 서고(영업시간 빈 상태와 같은 문법) Phase 6 정보 수정 제안에서 채운다. 경계 데이터는 `southkorea/seoul-maps`(Apache-2.0, 통계청 2013 시군구 GeoJSON 원본 해상도)를 `scripts/fetch_gu_boundaries.py`로 받아 Douglas-Peucker 20m로 줄여(2,820점, 60KB, 동적 import) `lib/gu-boundaries.json`에 굽는다 — 출처·라이선스는 스크립트와 JSON 머리에. 같은 저장소의 simple 버전(1,374점)은 시청 앞이 종로구로 나올 만큼(≈300m) 거칠어 쓰지 않는다. 라이선스가 불명확하면 최근접역 스크립트와 같은 OSM Overpass(ODbL)로 바꾼다.
- **주소 검색(네이버 지오코더, `submodules=geocoder`)은 핀을 옮기는 보조다.** 결과는 제안 목록이 열린 동안만 컴포넌트 상태에 있고 선택·닫기·언마운트에 버린다. Place·파일·DB에 쓰지 않는다(CLAUDE.md 규칙 2). 지오코더 결과에서 옮겨간 핀 좌표는 사용자 입력이다.
  - **근거(2026-09-08 원문 확인 — 그동안 규칙만 있고 출처가 없었다)**: 네이버 클라우드 플랫폼 *AI·Naver API 서비스 이용약관*의 서비스별 조건 Maps API (2) — "**고객은 Maps API의 결과 데이터를 … 별도로 저장해서는 안되며, … 데이터베이스화하여 이용해서도 안됩니다. … 모든 Maps API의 결과 데이터는 값을 리턴 받는 즉시 1회 자신의 서비스에서 사용하는 것만 허용**"(제5조 ⑧에도 "사전 동의 없이 결과 데이터를 복제·저장·가공·배포 금지"). 카카오도 같다 — 데브톡 공식 답변(2026-08-18): "**당사가 제공하는 API 의 응답 결과는 저장하여 사용하실 수 없습니다**", 저장 가능한 건 **사용자가 직접 입력한** 장소명·주소뿐. 즉 "핀 좌표 → 주소 자동 채움"은 네이버·카카오 **양쪽 다 금지**이고, 다른 서비스가 그렇게 보인다면 (a) 공공 데이터 출처를 쓰거나 (b) 저장 없이 화면에만 그리거나 (c) 위반이다.
- ~~서울 밖 핀은 막는다~~ → 같은 날 보완에서 **전국 허용**으로 바뀜(아래 "제보 보완" 항목). 한국 밖(바다)만 막는다.
- **채운 레드는 단계마다 하단 CTA 하나.** 단계가 곧 화면이다. 제보 중엔 FAB 줄과 지도 위 두 층(검색 블록·칩 행)을 숨긴다. 중복 의심의 [이 가게예요][다른 가게예요]는 둘 다 아웃라인.
- **4단계 CTA는 하나.** 빈 채로면 "건너뛰고 등록", 하나라도 넣으면 "등록하기". v1의 "큰 [건너뛰고 등록] + 작은 [입력하고 등록]"은 큰 버튼이 입력을 버리는 함정이라 폐기. spec 4.3-4 문구를 정정한다.
- **뒤로가기는 단계 하나.** 플로우는 히스토리 엔트리 하나(`{saeuReport: true}`, URL `/`). popstate에서 한 단계 내리고 다시 push, 1단계에서는 닫힘. 완료 시 엔트리를 평범하게 바꾸고, 기존·신규 상세로 넘어갈 땐 `openDetail(id, "report")`가 replace해 back 한 번에 목록.
- **2단계 요약 높이는 40%, 최소 330px.** 상세의 270에는 ‹ 줄·제목·캡션·주소 입력·CTA(308px)가 안 들어간다. 제보 모드는 아래로 튕겨도 닫히지 않는다(입력 유실 방지).
- **사진은 파일 선택 + 미리보기**(최대 10, 이미지만). 목 제출은 파일을 버린다(저장소 Phase 6). 4단계 사진 타일은 화면 2 스트립의 ＋ 타일 문법.
- **단위 칩은 [1kg][500g][한판][반판][마리][단위 없음].** 마리는 수 입력이 딸린다(`count`, `unit_raw "10마리"`). 大中小(`size`, 목 150줄 중 2)·인분(`serving`)은 크롤 표기로만 두고 제보로 늘리지 않는다. 가격은 필수(양의 정수). 구이 줄 → `grill`, 회 토글 줄 → `raw`.
- **중복 의심 후 "다른 가게예요"면 `Place.duplicateSuspectOf`에 후보 id**(관리자 큐용, UI 미표시). 1단계 자동완성은 두 글자부터, 상호 정규화 포함 매칭, 닮은 순 5행.
- **완료 공유는 상세의 공유를 `lib/share.ts`로 추출해 재사용**(navigator.share → 링크 복사). 카카오 전용 버튼·OG는 Phase 5. "리뷰도 남겨볼래요?"는 Phase 4까지 "준비 중이에요".
- **react-hook-form은 쓰지 않는다.** 깔려 있지만 미사용이고, 필드 6개는 컨트롤드 state + zod `safeParse`(사진 신고 시트와 같은 패턴)면 된다. 새 패키지 없음.
- **지도 에러 상태에선 제보 입구가 토스트로 막힌다**("지도를 불러오지 못해 제보할 수 없어요") — 2단계가 지도다.
- **(구현 중 정정) 완료 화면에서도 제보 엔트리 표식을 유지한다.** 계획은 등록 성공 시 `replaceState(null)`이었지만, 표식을 두면 완료에서의 뒤로가기(popstate → 닫힘)와 헤더 ✕(back → 닫힘)가 같은 길이고 중복 엔트리가 안 남는다. 상세로 넘어갈 때는 그 엔트리를 `/place/[id]`로 교체한다.
- **(구현 중 정정) 중복 의심 서브 패널의 ‹는 핀 화면으로 돌아온다**(히스토리 조작 없음). 브라우저 뒤로가기는 여전히 한 단계(1단계)다.
- **환경 메모: 네이버 지오코딩은 NCP 애플리케이션에 Geocoding API가 켜져 있어야 한다.** 현재 키는 `maps.apigw.ntruss.com/map-geocode/v2/geocode-js` 403 → SDK 콜백 status 500 → 화면은 "주소를 찾지 못했어요"(4상태의 실패). 콘솔(AI·NAVER API > Application > 새우맵 > Maps)에서 Geocoding을 체크하면 코드 변경 없이 동작한다. 핀 드래그만으로도 제보는 끝까지 된다.
- **(갭 스윕 뒤 확정) 제보 모드는 본문 드래그 체이닝이 없다.** 입력 폼이라 텍스트 선택·스크롤이 우선이고, 전체로 펼치는 길은 핸들뿐이다. design 3-2 문장을 이에 맞췄다.
- **(갭 스윕 뒤 확정) 제보 중 마커는 칩·탭·검색어 필터를 우회해 전부 그린다** — 중복 후보가 필터에 가려지면 "핀과 후보가 둘 다 보이게"를 못 지킨다. 클러스터 탭도 마커 탭처럼 무시한다.
- **(스코프) spec 4.3 엣지 "이전 가게"의 기존 핀 이전 처리·리다이렉트는 Phase 6 관리자 큐로.** 화면 3에는 대응 UI가 없고(제보는 새 핀만 만든다), 기존 핀을 옮기거나 합치는 건 관리자 판단이다. roadmap Phase 6에 항목을 추가했다.
- **StrictMode 함정 1회 기록**: 주소 검색의 `mounted` ref 가드가 dev 이중 effect로 false에 굳어 응답이 버려졌다(Playwright에서 로딩 고착으로 발견, jsdom 테스트는 통과). 비동기 결과 가드는 요청 순번(seq)만 쓴다. 같은 실수가 다시 나오면 CLAUDE.md 규칙으로.


## 2026-09-04 — 제보 보완: 탭으로 핀 이동, 핀 자리 기존 가게, 전국 허용, 몇 마리 입력 위치

Phase 3를 눌러 본 뒤 나온 지적 4건(계획 `docs/plans/phase3-report-followup.md`).

- **지도 탭 = 핀 이동**(2단계에서만). 44px 핀을 손가락으로 집어 끄는 것보다 "여기" 탭이 쉽다. 지도는 움직이지 않고(탭한 자리가 곧 보이는 자리) 드래그는 미세 조정용으로 남는다. 데스크탑 `click`·터치 `tap` 둘 다 받는다(같은 좌표라 두 번 와도 무해).
- **핀 자리 30m 근접 검사(상호 무관)**를 [여기가 맞아요]에 더한다. 상호 유사 150m 검사가 먼저, 없으면 30m 안 가장 가까운 가게. 이름을 다르게 적고 핀을 기존 가게 위에 놓는 경우가 그전까지 그대로 중복이 됐다. 같은 패널에 제목만 "핀 자리에 이미 등록된 가게가 있어요". [다른 가게예요]면 역시 `duplicateSuspectOf`. 30m = 같은 건물·옆 점포 — 수산시장처럼 붙은 다른 점포는 "다른 가게예요"로 통과한다. 같은 거리면 목록 순서가 우선(반복 확정에서 같은 후보가 나온다).
- **2단계에서 기존 마커를 탭하면 그 가게로 중복 의심 패널**(제목 "이미 등록된 가게예요", 핀은 그대로). 이 패널의 [다른 가게예요]는 핀 화면으로 돌아온다(3단계로 건너뛰면 핀 확정 검사를 안 거친 좌표가 등록된다). 탭은 명시적 질문이라 이미 "다른 가게예요"라고 답한 가게도 다시 탭하면 다시 묻는다 — "다시 묻지 않는다"는 확정 경로에만 적용(갭 스윕 모호 2건 결정). 이미 있는 마커가 보이면 그걸 누르는 게 자연스럽다. 1·3·4단계는 무반응 그대로.
- **전국 허용.** 경계 데이터를 두 파일로: 서울 25구(원본 해상도, DP 20m, 60KB)를 먼저 보고, 서울 밖일 때만 전국 226개 시군구 단순화본(`southkorea/southkorea-maps` KOSTAT 2013, Apache-2.0, 180KB)을 동적 import. 라벨은 서울 "마포구", 그 밖 `"{시군구}({시도 약칭})"`("김포시(경기)", "해운대구(부산)", "창원시 진해구(경남)") — 크롤러의 김포 표기와 같고 동구·남구 같은 중복 이름이 구별된다. 시도 약칭은 KOSTAT 코드 앞 두 자리 표. 한국 밖(바다)만 "한국 안의 위치만 제보할 수 있어요".
- **전국 정밀도는 낮다.** 서울 밖 단순화본은 시군구 경계 근처 수백 m가 옆 시군구로 갈 수 있다(원본 55MB는 클라이언트에 실을 수 없다). 라벨은 "어느 동네냐"용이고 Phase 6 관리자가 고친다. API 호출 증가는 없다(전부 클라이언트 연산).
- **[마리]의 "몇 마리" 입력은 칩 행 아래 TextField**(폭 128, 접미 "마리", 바로 포커스). 칩 행 오른쪽 끝에 두니 가로 스크롤 밖이라 보이지 않았다. 단위 칩 행은 **줄바꿈**으로 — 마지막 칩이 잘려 숨는 것이 발견성을 해쳤다.
- CLAUDE.md의 "서울 새우구이 지도"는 제품 정체성이라 그대로 둔다. 시드는 서울(+김포 1곳), 제보만 전국을 받는다.

## 2026-09-04 — Codex PR #6 코멘트 5건: 늦은 비동기 완료 가드, 규칙 2 문구, 경계 프리페치

사람이 선별한 처리(CLAUDE.md "Codex 코멘트는 사람이 판단"): #2·#3·#5 고침, #1 무시 + 규칙 문구 정정, #4는 라벨 대신 프리페치.

- **#2·#3(P1) 고침 — 늦게 끝난 비동기가 떠난 화면을 움직였다.** 2단계 [여기가 맞아요]의 경계 파일 로드 중 ‹·✕를 누르면 완료 뒤 `onConfirm`이 1단계에서 3단계로 튀거나 닫힌 플로우를 다시 열었고, 4단계 등록(목 400ms) 중 ✕로 닫으면 성공 콜백이 `done`을 다시 열어 `created` 없는 빈 시트가 됐다. StepLocation은 alive ref로 결과를 버리고(effect 본문에서 true, cleanup에서 false — 위 StrictMode 함정의 원인은 본문 리셋이 없던 것), `goToReportStep`은 플로우가 닫혀 있으면(`reportStepRef === null`) 무시한다. 등록 성공의 `onCreated`는 그대로 둔다 — 목에 이미 써진 가게는 목록·마커에 보여야 한다(spec 5 즉시 노출). 같은 종류 3회째(주소 검색 mounted 가드 포함)라 CLAUDE.md 컨벤션으로 승격. 공용 `useAlive()` 훅은 호출자가 한 곳뿐이라 만들지 않는다(사다리 5).
- **#5(P2) 고침.** 마운트 시 위치 요청은 권한 프롬프트 뒤 늦게 응답할 수 있어(브라우저는 프롬프트 시간을 timeout에 세지 않는다) 2단계에서 핀을 맞춘 뒤 지도를 현 위치로 `morph`해 버렸다. 제보 중이면 위치만 기억하고 지도는 옮기지 않는다.
- **#4(P2) — 라벨 대신 프리페치.** 확인 중 버튼이 라벨 그대로 비활성만 되는 지적은 맞지만 design (e) "이 화면은 서버 읽기가 없다"를 유지하고 대기 자체를 없앤다: 2단계 진입에 핀 시작 좌표로 `getGuOfPoint`를 한 번 불러 경계 파일을 받아 둔다(모듈 캐시). 서울 밖으로 옮긴 핀은 확정 때 전국 파일(180KB)을 한 번 더 받는다 — 드물어 감수.
- **#1(P1) 무시 + 규칙 2 문구 정정.** 주소 검색 제안 목록을 컴포넌트 상태에 두는 건 위 v2 결정대로 허용(목록을 그리려면 렌더 중 임시 상태가 불가피). CLAUDE.md·AGENTS.md 규칙 2가 "상태"라고만 써 Codex가 문자 그대로 읽었다 → "파일·DB·Place·전역 상태 금지, 화면에 그리는 동안의 임시 상태는 허용"으로 두 파일 정정.

## 2026-09-04 — 실기기 뷰포트 보정: DevTools 844px는 거짓, viewport-fit 누락

Phase 3 머지 뒤 프리뷰를 폰에서 보니 검색·칩 아래 ~ 바텀시트 위의 "보이는 지도"가 개발자도구보다 훨씬 좁았다. 시트가 `40dvh`라 **시트 상단 y좌표로 실제 뷰포트를 역산**할 수 있어 스크린샷 3장(네이버 인앱·크롬 시크릿·사파리)에서 숫자를 뽑았다.

| | 시트 상단 | 실제 뷰포트 | 상단 스택 | 보이는 지도 |
|---|---|---|---|---|
| DevTools 390×844 | 506 | 844 | 72 (고정) | 438px |
| 사파리 | 421 | 702 | 72 | 353px (−19%) |
| 네이버 인앱 | 419 | 694 | 72 | 353px |
| 크롬 시크릿 | 394 | 656 | 72 | 324px (−26%) |

- **DevTools 기기 에뮬레이션은 브라우저 UI를 시뮬레이션하지 않는다.** 실기기는 656~702px만 준다. 뷰포트가 142px 줄 때 시트는 비례라 57px만 줄고 상단 스택 72px은 고정이라 안 주므로 손실을 보이는 지도가 전부 흡수한다. 그 위에 칩 행(~70)·FAB 줄(~60)이 또 고정으로 얹혀 아무것도 안 가린 지도는 368 → 254px(−31%). 지금까지의 320/390/430 확인이 전부 이 잘못된 높이 위에서 이뤄졌다 — 검증 뷰포트에 높이를 박는다.
- **`viewport-fit: cover`가 없었다.** `env(safe-area-inset-*)`는 cover 없이는 **항상 0**을 반환한다. `globals.css:155-159`의 `--spacing-safe-*` 토큰 4개와 이를 쓰는 7곳(`place-sheet.tsx:191`, `step-frame.tsx:63`, `footer-links.tsx:10`, `photo-viewer.tsx` 2곳, `photo-report-sheet.tsx:30`, 로딩 2곳)이 전부 12px 폴백으로 죽어 있었다. `app/layout.tsx`에 한 줄. 사용처는 이미 다 맞게 걸려 있어 고칠 게 없었다.
- **검색 바를 칩과 같은 떠 있는 pill로.** 불투명 흰 블록(`bg-bg … shadow-float`, 고정 72px)이 지도를 완전히 덮고 있었다. 껍데기를 `rounded-8 bg-bg-sunken` → `rounded-max border border-line bg-bg shadow-float`로 바꿔 칩과 같은 층·같은 문법(pill + 헤어라인 보더 + float)으로 통일하고, 래퍼에서 `bg-bg`·`shadow-float`·`pb-3`을 뺐다(간격은 부모 `gap-2.5`가 담당). 지도가 `absolute inset-0`이라 pill 뒤로 이어져 그려진다. **390×702 실측: 보이는 지도 353 → 421px (+19%).**
- **`SearchBar`는 `map-screen.tsx` 한 곳에서만 쓰여 새 컴포넌트가 필요 없었다.** `rounded-8 bg-bg-sunken` 입력 껍데기를 복붙한 나머지 4곳(`address-search.tsx:69`, `step-name.tsx:78`, `ui/text-field.tsx:25`, `photo-picker`)은 **시트 안 입력이라 그대로** 둔다 — 지도 위에 뜨는 건 검색 바 하나뿐이다.
- **로딩 스켈레톤 2곳(`app/loading.tsx`, `app/place/[id]/loading.tsx`)을 같이 바꿨다.** 상단 블록 마크업이 3개 파일에 복제돼 있어 안 맞추면 스켈레톤 → 실제에서 화면이 튄다. (시트 요약 높이를 `--sheet-detail-half` 한 곳에서 관리하는 것과 같은 이유)
- **용어 정정**: 살아 있는 문서의 "검색 블록" → "검색 바"(CLAUDE.md, spec 4.1, design 공통 블록·화면 1·화면 3). decisions·plans의 과거 기록은 로그라 그대로 둔다. design 화면 3의 "390×844에서 지도가 500px 남는다"는 DevTools 값이라 폐기 — 실기기 702px에서는 40%(281)가 아니라 **330px 하한이 걸려** 372px이다.

- **지도 위치는 컨테이너 정중앙이 아니라 "보이는 지도의 한가운데"에 맞춘다.** SDK는 `defaultCenter`를 컨테이너 정중앙(702px 기준 y=351)에 놓는데, 상단 두 층과 시트에 가려 실제로 보이는 띠는 110~421이라 한가운데가 **265** — 85~105px 어긋났다. 그래서 서울 중심이 시트 쪽으로 치우치고 위쪽 절반은 북한산·성북구 빈 땅이었다(스크린샷 3장에서 서울시청이 한가운데보다 90~100px 아래). 390×702 실측: **시청 y 355 → 275**(한가운데 265).
- **같은 결함이 네 곳에 있었다** — `initialCenter`(첫 화면), 위치 첫 로드·현위치 버튼(`morph`), 클러스터 탭(`morph`). 전부 이미 있는 `visibleStripCenterY`를 거치게 통일했다(세 번째 호출자가 넘었으므로 `stripCenterY()` 헬퍼로 추출 — 사다리 5). `morph` → `focus`: `focus`는 줌을 즉시 바꾸고 pan만 애니메이션한다. 줌 애니메이션 중에는 투영이 옛 줌이라 screenY 계산이 어긋나기 때문이며(map-view.tsx 주석), 제보 핀이 이미 쓰던 경로다. **클러스터 탭의 줌이 스냅으로 바뀌는 건 감수** — 펼쳐진 마커가 시트 아래로 들어가는 것보다 낫다.
- **`panTo`에 `animate:false`(= `setCenter`)를 더했다.** 첫 페인트에서 지도가 뜨자마자 105px 미끄러지면 안 된다. `/place/[id]` 직접 진입은 그대로 애니메이션(핀으로 날아가는 게 그 인터랙션의 요점). 늦게 오는 위치 응답이 호출 시점의 시트 상태를 보도록 `snapRef`/`modeRef`를 뒀다(`reportStepRef`와 같은 패턴 — 클로저 값은 낡는다).
- 테스트 지도 페이크에 `setCenter`가 없어 전 케이스가 `TypeError`로 죽었다(실제 SDK엔 있다). 페이크에 추가.

- **`window.innerHeight`와 `dvh`가 갈릴 것이라는 가설은 틀렸다 — 고치지 않는다.** CSS는 `dvh`, JS는 `window.innerHeight`로 소스가 둘이라 키보드에서 어긋날 것으로 봤으나, 390×702에서 `innerHeight === visualViewport === dvh === svh === lvh === 702`로 완전히 일치했다. iOS와 Android Chrome(108+) 모두 가상 키보드 기본이 `resizes-visual`이라 **둘 다 안 줄어든다**. 소스 통일은 이득 없이 변경 위험만 있어 보류.
- **대신 진짜 위험은 따로 있다(백로그, 실기기 확인 필요)**: 시트가 `position: fixed; bottom: 0`인데 iOS에서 키보드가 뜨면 레이아웃 뷰포트가 안 줄어 **하단 CTA가 키보드 뒤로 들어간다**. 제보 1·3·4단계와 주소 검색이 그 자리고, Phase 4 리뷰 작성 폼이 같은 자리를 다시 만든다. 필요하면 `interactiveWidget: "resizes-content"` 또는 `visualViewport` 구독.

- **cover 단독으로는 보이는 지도가 거의 안 는다.** dvh가 safe-area만큼 커지지만 `40dvh` 시트와 `pt-safe-top-or-3` 상단이 같이 커져 상쇄된다(iPhone 14 Pro 추정 349 → 358px). 효과는 검색 블록을 플로팅으로 바꿀 때 난다 — 지도가 `absolute inset-0`이라 상태바·홈 인디케이터 뒤까지 그려지므로 421 → 477px. 인앱 브라우저의 cover 동작은 사파리와 다를 수 있어 실기기 확인이 완료 조건.

## 2026-09-04 — Codex PR #7 코멘트 2건 + 칩 행 세로 이동

사람이 선별한 처리(CLAUDE.md "Codex 코멘트는 사람이 판단"): #2 고침, #1 무시 + 백로그.

- **#2(P2) 고침 — `viewport-fit: cover`는 가로도 넓힌다.** 노치폰 가로모드에서 좌우 인셋이 47~59px인데 상단 두 층은 고정 `px-5`(20px), FAB 줄은 `inset-x-5`, 시트는 edge-to-edge라 컨트롤이 노치 밑에 깔린다. `--spacing-safe-left/right`와 `-or-5` 토큰을 더해 **지도 위에 뜨는 UI와 시트 '내용'만** 되민다 — 지도(`absolute inset-0`)는 full-bleed, 시트 배경·라운드도 edge-to-edge 유지(`padding-inline`을 `.saeu-sheet__header`·`__body`에). 토큰을 59px로 덮어써 검증: 포트레이트 20/0px → 가로모드 59/59px, 시트 배경 폭·지도 폭은 390 그대로.
- **#1(P2) 무시 + 백로그 — 거리 기준점이 눈에 보이는 중심과 다르다.** `origin`(`use-map-screen.ts:199`)이 `viewport.center`(캔버스 정중앙 y=351)를 쓰는데 사용자가 보는 한가운데는 띠 중앙(265)이라 줌 12에서 약 2.6km 어긋난다. **다만 이 86px 어긋남은 이 PR 전에도 똑같이 있었다** — 중심 보정은 각 화면 위치에 오는 좌표를 바꿨을 뿐 간격은 그대로다(Codex가 신규 회귀로 본 건 오독). 고치려면 화면 y → 좌표 역투영이 `MapHandle`에 필요하고, 같은 줄의 서울 밖 `origin` 건은 사용자가 "그대로 둔다"고 결정했다(위 항목). 선재 결함으로 백로그.
- **칩 행이 세로로도 움직였다.** `overflow-x: auto`만 주면 CSS가 `overflow-y`를 `visible` → **`auto`로 강제**한다. 여기에 `.hit-44::after`(`inset: -6px 0`)가 아래로 삐져나와 1px이 세로 스크롤이 되고, `touch-action: auto`라 세로 드래그를 스크롤 제스처로 먹었다. `overflow-y: hidden` + **`touch-action: pan-x`** — 시트 본문이 `pan-y`를 쓰는 것과 같은 idiom(가로 스크롤러에는 `pan-x`를 명시한다). 가로 스크롤은 그대로.
- **알아 둘 것(안 고침)**: `.hit-44::after`의 **위쪽 6px은 스크롤 컨테이너가 이미 잘라내고 있다** — 스크롤 가능 영역은 끝 방향(아래·오른쪽)만 잡히고 시작 방향 오버플로는 클립되기 때문이다. 즉 칩의 실제 터치 타겟은 44px이 아니라 컨테이너 높이 40px이다. 이 PR 전부터 그랬고, 되살리려면 칩 행에 `py-1.5`가 필요한데 상단 스택이 8px 두꺼워져 지도를 그만큼 먹는다. 판단 보류.

## 2026-09-04 — 키보드가 뜨면 시트가 그 위에 앉는다 (visualViewport)

실기기에서 제보 1단계 입력에 포커스하면 **[새로 등록하기] CTA와 캡션만 남고 위쪽이 전부 빈 흰 화면**이 됐다(사파리·네이버 인앱 스크린샷 2장). 타이핑 중인 입력 필드·제목·진행바가 사라진다.

- **원인**: 시트가 `position: fixed; bottom: 0; height: 92dvh`인데 키보드가 떠도 레이아웃 뷰포트(`innerHeight`·`dvh`)는 안 줄고 visual viewport만 줄어든다. 시트는 키보드 뒤에 남고, iOS가 포커스된 입력을 보이려고 visual viewport를 아래로 스크롤(`offsetTop`)하면서 시트 위쪽 내용이 화면 밖으로 밀려났다.
- **`window.visualViewport` 구독 하나로 iOS·안드로이드를 같이 덮는다.** `max(0, innerHeight - vv.height - vv.offsetTop)`을 `--kb`로 `:root`에 쓰고(`components/ui/use-keyboard-inset.ts`), `.saeu-sheet`가 `bottom: var(--kb)`로 키보드 위에 앉고 `--sheet-full: calc(92dvh - var(--kb))`로 그만큼 낮아진다. 안 낮추면 위가 화면 밖으로 넘친다. `height`·`transform`이 둘 다 `--sheet-full`을 쓰므로 스냅 계산은 그대로 맞는다.
- **뷰포트 meta의 `interactive-widget`은 쓰지 않는다.** Chrome 108+·Firefox 132+ 전용이라 **Safari에 없고**(WebKit 259770 오픈), 켜면 Chromium에서만 레이아웃 뷰포트가 줄어 **`h-dvh`인 지도까지 리사이즈된다** — 제보 2단계(주소 검색 + 지도)에서 타이핑마다 지도가 튀는 안드로이드 전용 부작용을 새로 만드는 셈이다. 안드로이드 기본값도 `resizes-visual`이라 증상이 iOS와 같으므로 visualViewport 하나면 양쪽이 다 고쳐진다.
- **높이는 `--kb`가 아니라 `--vvh`(보이는 창의 높이)로 정한다 — 첫 수정이 틀렸다.** 처음엔 `--sheet-full: calc(92dvh - var(--kb))`로 했는데, **iOS가 visual viewport를 끝까지 밀면 `offsetTop`이 키보드 높이와 같아져 `--kb`가 0이 된다**(795 − 459 − 336 = 0). 그때도 창은 키보드만큼 낮은데 시트만 92dvh로 남아 위로 272px 넘치고, 넘친 만큼 제목·입력이 화면 밖으로 밀린다 — **실기기에서 증상이 그대로였다.** `bottom`은 `--kb`가 맞지만 높이는 창 높이 자체를 써야 한다: `--sheet-full: calc(0.92 * var(--vvh, 100dvh))`.
- 검증(`--kb`·`--vvh` 주입, 두 상태 모두): `offsetTop 0`(kb 336)과 `offsetTop 336`(kb 0) **양쪽에서** 시트가 보이는 창 안에 들어가고 진행바·제목·입력·CTA가 전부 보인다. 헤드리스엔 가상 키보드가 없어 **실기기 확인이 완료 조건**이다.
- **알아 둘 것**: `--kb > 0`이면 CSS(`dvh`)와 JS(`window.innerHeight` 기반 `sheetVisiblePx`)가 갈린다. 위 "두 소스는 일치한다"는 판단의 전제를 이 변경이 깬다. 스냅 계산은 드래그할 때만 쓰이고 타이핑 중엔 드래그하지 않아 이번엔 두지만, 소스 통일 백로그의 근거가 하나 늘었다.
- **안드로이드는 미확인.** 지금까지의 측정·스크린샷이 전부 아이폰 하나에서 나왔다. 코드는 양쪽을 덮지만 동작 확인 전까지는 추정이다.

## 2026-09-04 — 제보 3단계: 필수는 캡션에서 한 번만, 회 줄은 켜면 포커스

- **필수 표시는 캡션 한 줄로.** spec 4.3이 "메뉴명·가격·단위 칩 한 줄(필수)"이라 3단계는 **세 필드가 전부 필수**고 4단계는 전부 선택이다. 즉 필수/선택이 이미 **단계 단위로 갈려 있어** 필드마다 `*`를 붙이면 셋 다 `*`라 정보량이 0이다. 캡션을 "대표 메뉴 한 줄이면 돼요. 이름·가격·단위를 모두 채워주세요"로 고친다. (실기기에서 "다 필수인가? 그래서 `*`를 안 붙인 건가?"라는 질문이 나왔다 — 지금까지는 [다음]을 눌러 오류가 떠야 알 수 있었다.)
- **"새우회도 팔아요"를 켜면 새우회 메뉴명에 포커스.** 회 줄이 토글 아래에 생기는데 스크롤 밖이라 안 보였다. `[마리]` 칩이 "몇 마리" 입력에 포커스하는 기존 방식과 같게 맞춘다 — 포커스가 가면 브라우저가 스크롤도 같이 해 준다.
- **마운트 이펙트로 하지 않는다.** `report-panel.tsx`가 `switch (step)`으로 단계를 **언마운트**하므로, rawToo가 켜진 채 ‹로 3단계에 돌아오면 그때마다 포커스를 뺏는다. "방금 켰다"는 신호를 토글 `onChange`에서 ref에 세우고 이펙트가 소비한다. **state가 아니라 ref인 이유**: state면 이펙트에서 되돌려야 하는데 그건 연쇄 렌더라 린트가 막는다(`Calling setState synchronously within an effect`). 정리(cleanup)에서 만지지 않으니 StrictMode 이중 effect에도 안전하다 — 첫 실행이 소비하고 두 번째는 지나간다.

## 2026-09-04 — 제보 CTA 위 스크롤 페이드 ("그라데이션 배경 금지"의 예외)

실기기에서 "CTA 윗부분이 좀 잘리는 느낌"이라는 지적. 본문 스크롤 컨테이너와 CTA 줄이 flex 형제라 경계에서 내용이 **뚝 잘린다**.

- **CTA 줄 위로 20px 페이드**(흰색 → 투명)를 얹는다. 본문 children 래퍼는 `pb-3` → `pb-6`으로 늘려, 끝까지 스크롤했을 땐 페이드가 **빈 자리만** 덮게 한다 — 마지막 항목이 흐려지지 않는다.
- **"그라데이션 배경 금지"(2026-09-02)의 예외로 둔다.** 그 규칙과 2026-09-03의 그라데이션 보더 기각은 **장식**을 막는 것이다(의미 없이 가장 시끄러운 요소가 되는 것). 이건 스크롤 끝을 부드럽게 하는 **기능적 마스크**라 성격이 다르다 — 지하철 노선색이 "사실 표기"라 보라 금지의 예외인 것과 같은 결. 색은 배경색(`--color-bg`) 한 가지에서 투명으로 갈 뿐 새 색을 들이지 않는다.
- **literal blur(`backdrop-filter`)는 쓰지 않는다.** 시트가 불투명 흰색이라 흰 위에 흰을 블러해도 거의 안 보인다. 그리고 blur를 쓰려면 CTA가 본문 위로 떠야 해서 본문 하단 패딩을 CTA 높이에 맞춰야 하는데, CTA 높이가 단계마다 달라 측정이 필요해진다. 흰색 → 투명 페이드가 같은 "부드럽게"를 더 싸게 만든다.
- **위쪽 끝은 안 했다(백로그).** 같은 결함이 스크롤 컨테이너 위쪽에도 있어 스크롤 중에는 제목이 글자 중간에서 잘린다. 다만 위에 그냥 페이드를 얹으면 **스크롤이 맨 위일 때 `‹` 버튼이 흐려진다** — 스크롤 위치를 보는 조건부라 JS가 필요하다. 아래쪽은 `pb-6` 여유 덕에 조건이 필요 없었다.

## 2026-09-04 — 현위치 마커 (spec에 없던 기능)

지도에 현위치 **버튼**(FAB)만 있고 **마커**가 없었다 — spec·design 어디에도 없어 미정이었다. 실기기에서 "일부러 안 넣은 건가?"라는 질문이 나왔고 넣기로 했다.

- `userLocation`은 `use-map-screen.ts`에 이미 있었다. 마커만 없어서 `getUserLocationIcon` + `<Marker>` 하나로 끝난다.
- **16px 파란 점 + 3px 흰 링.** 탭 대상이 아니라(`clickable={false}`) 히트 박스를 키우지 않고, `zIndex` 5로 가게 마커(100~300)보다 아래에 깔린다.
- **파랑은 브랜드 팔레트가 아니라 "내 위치"의 사실 표기다.** 네이버·카카오·구글이 다 쓰는 관습이라 설명 없이 읽힌다. 지하철 노선색이 보라 금지의 예외인 것과 같은 결이고, `locator` semantic 토큰 하나로 **이 마커 한 곳에만** 쓴다. 브랜드 레드로 하면 클러스터 마커(레드 원)·제보 핀과 헷갈린다.
- **최종은 새우 모양.** 에셋이 없어 표준 파란 점으로 대체하고 아래 커스텀 에셋 목록에 올린다.

## 2026-09-04 — 제보 1단계 "찾는 가게가 없나요?" 캡션 제거

버튼인 줄 알고 눌러 봤다는 지적에서 시작했다. 실제로는 [새로 등록하기]를 설명하는 12px 회색 캡션인데(design 화면 3 v1의 "빈 상태는 한 줄 + 인라인 액션"), 검색 결과가 5행 떠 있어도 같이 보여 문맥이 어긋나고 버튼과 떨어진 별개 액션처럼 읽혔다.

- **"매치가 없을 때만 보이기"로 먼저 고쳤다가 그것도 폐기했다.** 그 조건에서는 **아직 아무것도 검색하지 않은 빈 입력 상태에서도** 캡션이 뜬다 — 패널을 막 열었는데 "찾는 가게가 없나요?"가 먼저 말을 거는 셈이라, 정작 필요한 순간(쳐 봤는데 없을 때)보다 엉뚱한 순간에 더 자주 보인다.
- **그냥 뺀다.** `새로 등록하기`라는 라벨이 이미 무엇을 하는지 다 말한다. 시트 세로가 빠듯한데(실기기 656~702px, 키보드가 뜨면 더) 캡션 + 여백이 28px을 먹는다. 코드 최소주의 사다리 1("이게 꼭 있어야 하나").
- **CTA는 매치 유무와 무관하게 늘 있다** — 매치가 있어도 "그중엔 없다"며 등록할 수 있어야 한다.
- design 화면 3의 규칙을 "빈 상태는 한 줄 + 인라인 액션" → **"빈 상태는 액션 하나"**로 고친다.

## 2026-09-04 — 위치 거부를 구분해 안내 + 현위치 FAB 활성 표시

`requestPosition`이 **거부·실패·미지원을 전부 `null` 하나로** 삼키고 있어 안내가 "위치를 가져올 수 없어요" 하나뿐이었다.

- **거부는 다시 눌러도 팝업이 안 뜬다** — 브라우저가 기억하기 때문이다. 그런데 안내가 "가져올 수 없다"라 재시도로 읽혀 **거부한 사용자가 버튼만 계속 누르게** 된다. `PositionError.code === 1`(PERMISSION_DENIED)을 가려내 "위치 권한이 꺼져 있어요. 브라우저 설정에서 허용해주세요"로 바꾼다. code 2·3(위치 못 구함·타임아웃)은 재시도할 만하므로 기존 문구 유지.
- 반환 타입을 `LatLng | null` → `{ok:true, point} | {ok:false, reason}`으로. 조용한 호출자(첫 로드·제보 2단계)는 `ok`만 본다.
- **현위치 FAB에 활성 상태를 준다.** 지도가 내 위치에 맞춰져 있는 동안 레드 틴트 배경 + 레드 아이콘(`aria-pressed`). 사용자가 지도를 밀면 풀린다 — `handleViewportChange`가 이미 프로그램 이동(1500ms 창)과 사용자 조작을 구분하고 있어 그 갈래에 얹었다.
- **채운 레드는 안 쓴다.** "채운 레드 버튼은 화면당 한 곳"(2026-09-02)이 [＋ 제보]에 걸려 있다. 칩 활성과 같은 틴트 문법(red-10 배경 + red-600)으로 위계를 낮춘다.
- 테스트 페이크의 `Marker`가 모든 마커를 `<button>`으로 그려 현위치 마커(`clickable={false}`)와 현위치 FAB의 접근 이름이 "내 위치"로 겹쳤다. 실제와 맞춰 `clickable === false`면 `<span>`으로 그린다.
- **아직 안 한 것**: 첫 로드에 조용히 위치를 묻는 건 그대로다(맥락 없이 팝업이 떠 거부율이 높다). 권한 상태로 FAB을 비활성화하는 것도 안 했다 — `navigator.permissions`가 필요하고, 거부 안내만으로 회복 경로는 생겼다.

## 2026-09-04 — 첫 로드에 위치를 묻지 않는다 + 첫 화면은 가게가 가장 몰린 곳

"잘못 눌러서 거부하면 어떻게 하나"에서 나왔다. **거부를 되돌리는 경로가 브라우저마다 4갈래**(iOS 사파리 주소창 아A → 웹사이트 설정 / 설정 › Safari › 위치 / 안드로이드 크롬 자물쇠 → 사이트 설정 / 인앱은 앱 권한이 먼저)라 안내로 풀 수 없다. **막을 곳은 거부 이후가 아니라 거부가 생기는 지점이다.**

- **첫 로드의 조용한 위치 요청을 없앤다.** 앱을 열자마자 맥락 없이 뜬 팝업은 반사적으로 거부된다. **현위치 FAB을 누를 때만** 묻는다 — 그때의 거부는 "지금 내 위치를 보고 싶지 않다"는 의도적 선택이라 사고가 아니다.
- **대신 첫 화면을 가게가 가장 몰린 곳으로 연다**(`densestPoint`, 반경 5km 안 가게 수 최대). 서울시청 고정보다 첫 화면에 보이는 가게가 많다 — 목 50곳 실측 **21 → 23곳**(줌 12 가시 띠 기준 계산으로는 12 → 18). 평균·중앙값이 아니라 밀도 피크인 이유는 **전국 제보를 받기 시작해** 서울 밖 한두 건이 평균을 끌고 가기 때문이다. 반경 5km는 줌 12 가시 영역(11.8×9.4km)에 내접한다.
- 첫 중심은 **서버가 준 목록으로만** 계산한다(`initialPlaces`) — 제보로 목록이 늘어도 흔들리지 않는다. O(n²)지만 등거리 근사라 삼각함수가 없고 한 번만 돈다.
- **거리·정렬은 그때까지 지도 중심 기준**이다. spec 4.1이 이미 규정한 동작("거리는 내 위치, 없으면 지도 중심 기준")이라 새 예외가 아니다.
- 위치가 없어도 앱은 온전히 동작한다: 거리·정렬은 지도 중심, 제보 핀은 지도 중심 → 서울 중심으로 떨어진다. **못 쓰게 되는 건 현위치 버튼 하나뿐이다.**
- 거부 안내 문구는 "브라우저 설정에서 허용해주세요"로 **유지**한다. 경로가 4갈래라 구체적으로 쓰려면 UA 판별이 필요하고, 틀린 안내가 없는 안내보다 나쁘다. 안드로이드는 아직 확인도 못 했다.

## 2026-09-04 — Codex PR #7 재리뷰 4건: 시트 높이 기준을 하나로

키보드 수정(`--vvh`) 뒤 재리뷰. 4건 다 유효해 전부 고쳤다 — **①은 그 수정이 만든 회귀다.**

- **① 스냅 px 하한이 시트 높이를 넘어 시트가 앵커 위로 솟았다.** `--sheet-full`은 `0.92 × --vvh`로 줄어드는데 `--sheet-report-half`의 `330px` 하한은 안 줄어, 보이는 창이 359px보다 작으면 `translateY = full - visible`이 음수가 됐다. **제보 2단계에서 지도가 아예 안 보였다**(재현: 창 322px → translateY −34, 시트가 화면 위로 8px 삐져나가고 지도 띠 0px). 390×702에 키보드 336이면 여유가 **7px뿐**이라 한글 추천 줄만 켜져도 넘어간다. `--sheet-visible`을 `min(..., var(--sheet-full))`로 clamp해 해결. **전체(full) 스냅은 원래 안전했다**(visible = full이라 항상 0) — 그래서 1·3·4단계는 멀쩡했고 `half`인 2단계만 깨졌다.
- **② 드래그 계산이 레이아웃 뷰포트 기준이라 키보드만큼 부풀려졌다.** `startVisible = innerHeight - rect.top`인데 시트 바닥은 `innerHeight - kb`에 있다. 제보 half를 아래로 끌면 `full`로 튀고 full은 접기 어려웠다. **시트의 모든 높이를 `--sheet-vh`(= `sheetViewportHeight()`) 하나로 통일**: CSS의 `40dvh`·`30dvh`도 `0.4 × --sheet-vh`로 바꿔 JS와 같은 값을 본다. `sheetVisiblePx`에 `layoutHeight` 인자를 더해 짧은 뷰포트 판정(`@media (max-height: 639px)`)만 레이아웃 높이로 남겼다. 위 "두 소스는 일치한다"던 판단은 이 변경으로 **소스가 하나가 되어** 정리됐다.
- **③ 절대 위치는 부모 padding이 안 밀어준다.** 시트 닫기 ✕가 `absolute right-2`라 `.saeu-sheet__header`의 `padding-inline`이 무효였다(절대 오프셋은 containing block의 **padding box** 기준). 가로모드 노치가 오른쪽이면 닫기 버튼이 그 밑에 깔린다. `--spacing-safe-right-or-2` 토큰을 오프셋 자체에. 검증: 인셋 59px 주입 시 8 → 59px.
- **④ 프로그램 이동 창(1500ms) 안에 민 경우 추적이 안 풀렸다.** `idle`로 사용자 조작을 가려내면 창 안의 팬을 놓치고, 이후 idle이 보장되지 않아 FAB이 계속 활성으로 남는다. **`dragstart`로 바꿨다** — 손가락이 끌 때만 오고 `panTo`·`morph`·`focus`는 안 낸다. 시간 창에 기대지 않으니 판정이 정확하다.

## 2026-09-04 — Phase 4 신규 패널·내 활동·로그인(화면 4·5) v2: 그릇·입구·목 세션
- **맥락**: roadmap Phase 4. design 화면 4·5 v1 초안을 v2 언어로 재작성하고 구현. 플랜 docs/plans/phase4-activity.md. Supabase는 아직 없어 세션·리뷰·찜 전부 `lib/data.ts` 메모리 목(새로고침 = 초기화, 규칙 4).
- **신규 패널 입구 = [새로 들어온 집] 칩**(사용자 선택). spec 4.4의 "지도 위 신규 N곳 버튼"은 v2 "지도 위 두 층만" 규칙과 충돌해 정정. 켜면 시트 헤더·행이 패널로 바뀌고(제목 "새로 들어온 집 N곳", 캡션 "아직 검증 전이에요…", 정렬 트리거 숨김) **뷰포트와 무관하게 7일 이내 전부를 최신순** — 심판대는 서너 곳이라 지도 안으로 자르면 늘 비어 보인다. 지도엔 신규 마커만(칩 필터가 그대로 한다).
- **맞아요 = 다녀왔어요**(`checkIn`, `checkedIds` 공유). 낙관 로직을 `useCheckIn` 훅으로 추출해 상세 기여 블록과 패널 행이 같은 코드를 쓴다. **달라요 = 사유 시트 + 목 저장**(사용자 선택): 위치 / 메뉴·가격 / 문 닫음 / 기타, 탭이 곧 제출(사진 신고 문법), `flagPlace()`. 관리자 큐는 Phase 6. "확인됨"은 체크 아이콘 + fg-secondary 텍스트 — v1의 틸 라벨은 카테고리색 규칙(마커·색점만)과 충돌해 폐기.
- **내 활동 = 시트 `me` 모드 + 검색 바 프로필 버튼**(사용자 선택). 별도 라우트 `/me`는 Phase 5 데스크탑 패널 전환과 이중 그릇이 돼 미채택. 열린 동안 마커는 활성 탭의 가게만(design 9 v1의 "찜 핀 하트 뱃지"는 이 방식으로 대신, Phase 5에서 재검토). 프로필 버튼은 지도 위 층을 더하지 않는다(카카오맵·네이버지도 문법). 익명이 누르면 로그인 시트 — 내 활동은 spec 5 "로그인 시" 기능.
- **로그인 시트·리뷰 폼은 `<dialog>`**: 시트 위에 얹히는 별도 표면이라 사진 뷰어와 같이 top layer. 로그인 시트는 바텀 모달(딤 + 라운드 20), 리뷰 폼은 전체 화면. 둘 다 히스토리 엔트리 하나(`saeuOverlay`) — 뒤로가기 한 번에 그 표면만 닫힌다. 리뷰 폼을 시트 모드로 하지 않은 이유: 상세 위에 열리는데 시트는 한 번에 한 모드뿐이고, 닫으면 상세가 스크롤·스냅 그대로 남아야 한다(뷰어와 같은 요구).
- **로그인 게이트는 Promise**: `requireLogin(reason): Promise<boolean>` — 이미 카카오면 즉시 true, 아니면 시트를 띄우고 성공/닫기로 resolve. 호출자가 그 뒤에 하려던 일(리뷰 폼·내 활동)을 이어 간다 = spec 5 "완료 후 쓰던 리뷰 화면으로 복귀". 늦은 resolve는 alive ref로 버린다(CLAUDE.md 컨벤션).
- **카카오 옐로 `kakao-500` #FEE500**: 카카오 브랜드 가이드의 사실 표기라 지하철 노선색·locator와 같은 결 — Primitive만 두고 Semantic 역할을 주지 않는다. 로그인 버튼 한 곳에만. 심볼은 커스텀 에셋 목록(그 전까지 `ci--chat`).
- **세션 목**: `Session {userId, provider, nickname}`, 익명이 기본. 카카오 로그인 시 익명의 찜·제보(`reporterId`)·체크인(`actor`)을 카카오 id로 승계(linkIdentity 흉내), 로그아웃은 새 익명(찜은 기기 한정이라 사라진다 — Supabase 익명 auth와 같은 결과). 탈퇴는 내 리뷰 소프트 삭제 + 찜 삭제 + 제보 작성자 해제. 목 카카오 유저 "새우헌터"(`u-kakao-1`) = 목 리뷰 2건의 작성자라 로그인 즉시 내 리뷰·본인 [수정][삭제]가 보인다.
- **리뷰 데이터**: `Review.id·authorId·editedAt?`, 삭제는 `deletedAt`(목록·평균에서 제외, 관리자에겐 보임 — spec 5). 등록은 카카오 세션 필수(익명은 reject), 확인일 갱신 + checkin(actor = userId). 사진은 1장(`photoUrl` 단수) 목은 버림. 본인 수정·삭제는 `authorId === session.userId`만. 삭제 확인은 행 안 인라인("삭제할까요? [삭제][취소]") — 모달 하나를 더 띄우지 않는다.
- **닉네임 수정 포함**(spec 5 "수정 가능"): 프로필 행 [수정] → 인라인 입력 2~12자.
- **익명 찜 3개째 넛지**: 토스트 한 번, 세션당(spec 5 "벽은 아님").
- **재검토 조건**: 신규 패널 범위(전체)는 신규가 20곳을 넘으면 뷰포트 우선으로 재검토. `me` 모드는 Phase 5 데스크탑에서 패널 전환으로 흡수. 리뷰 폼 키보드 동작은 실기기 확인 뒤(백로그 항목).
- **구현 중 보정(같은 날, Playwright 390×702·656·320×480)**: ① 리뷰 폼 헤더 ✕가 제목 위로 겹침 — `safe-left-or-2` 토큰이 없어 `left`가 안 먹었다 → 토큰 추가(safe-right-or-2와 대칭). ② 공유 링크로 직접 들어온 상세(`/place/[id]`, 표식 없는 엔트리) 위에서 프로필을 누르면 `replaceState`만 하고 닫기의 `back()`이 사이트 밖으로 나갔다 → 직접 진입이면 목록 엔트리로 바꾼 뒤 패널 엔트리를 얹는다(closeDetail의 직접 진입 처리와 같은 결). ③ 대표 메뉴 한 줄이 "생새우대하구이 한판 한판 42,000원"(크롤 메뉴명에 단위가 이미 있음) → 단위가 이름에 들어 있으면 한 번만(`primaryMenuLine`). ④ 신규 행 상태·액션 줄이 320에서 "검/증/전"으로 세로 접힘 → 버튼 묶음이 다음 줄로. ⑤ 리뷰 폼 실패는 토스트가 아니라 폼 안 오류 한 줄(top layer가 지도 화면 토스트를 가린다 — 사진 뷰어 신고와 같은 이유, design 화면 5 (b) 정정).
- **gap-sweeper 결과 반영(미구현 0·부분 4·모호 4)**: ① **내 활동 ✕가 히스토리 엔트리를 남겼다** — 시트의 닫기 버튼이 `onClick={onDismiss}`로 콜백에 MouseEvent를 넘겨 `source === "ui"` 판정이 깨졌고, 남은 `saeuMe` 엔트리 때문에 로그아웃·탈퇴 뒤 뒤로가기가 **빈 내 활동 시트**를 다시 열었다. 시트가 인자 없이 부르도록 고쳤다(닫기 함수 시그니처를 믿지 않는다). ② [탈퇴하기]의 `className` 덧칠이 cva 기본 유틸에 밀려 잉크색이었다 → 공용 버튼에 `danger` variant. ③ 찜 탭에 로딩·에러가 없었다 → 로드 상태를 세션 id 비교로 파생(effect 안 setState 금지 규칙 때문에 state 대신 파생). ④ 리뷰 폼 실패 문구는 이미 문서와 일치(에이전트가 옛 버전을 읽음).
- **모호 4건 확정**: ① **리뷰는 핀당 1개**(spec 5 스팸 4겹 2) — 내 리뷰가 있는 가게면 기여 블록의 [리뷰 남기기]가 **사라지고**(수정·삭제는 리뷰 행이 갖는다) 데이터 계층이 `already reviewed`로 막는다. 익명으로 폼을 열고 로그인한 경계에서만 폼 안 오류 한 줄로 알린다. ② **리뷰 수정은 별점·후기만** — 사진 교체는 Phase 6 저장소와 함께. ③ **내 활동은 닫으면 찜 탭으로 돌아온다**(design "기본은 찜"). ④ **신규 패널 빈 상태는 둘로 갈린다** — 신규가 0곳이면 "이번 주 새 제보가 없어요", 신규는 있는데 다른 조건이 걸러 0곳이면 "조건에 맞는 집이 없어요" + [필터 해제](칩·카테고리·검색어를 풀고 신규 칩은 남긴다). 화면 1 목록의 검색어 0곳은 기존대로 "이 동네엔 아직 없어요"다(범위를 넓히지 않았다).
- **security-reviewer 결과 반영(중간 1·낮음 4·정보 2)**: 탈퇴가 `checkins.actor`를 남기던 것 → `"deleted"`로 익명화(집계는 유지, Phase 6 `actor nullable + ON DELETE SET NULL`). 닉네임은 길이만 보던 것 → NFKC 정규화 + 한글·영문·숫자(단어 사이 공백 하나)만 — 폭 없는 공백·방향 제어문자로 빈 이름·남 흉내를 못 만들게. `toggleBookmark`가 존재하지 않는 가게 id도 받던 것 → 존재 검사(Phase 6 FK 자리). `submitReview`·`flagPlace`에 속도 제한 자리 주석 + spec 스팸 2번 목록에 리뷰 핀당 1·달라요 핀당 일 1 추가. 닉네임 저장 Enter 연타 가드. 정보 2건(사진 MIME은 서버 sharp가 방어선, 서버 모듈 전역 세션은 읽기뿐)은 주석으로만.

## 2026-09-05 — 심판대를 사용자 화면에서 뺀다 (Phase 4 되돌림)

- **맥락**: Phase 4를 다 만들고 프리뷰에서 눌러 보다가 "[새로 들어온 집] 리스트가 어디 있냐"는 질문이 나왔다. 확인해 보니 칩 행이 화면보다 147px 넓어 그 칩이 **화면 밖에 반쯤 걸려** 있었다(390 기준 x=336~443, [찜한 곳]은 아예 밖). 자리를 고치려다 더 근본적인 질문이 나왔다 — **심판대를 둬도 사람들이 참여할까.**
- **판단: 안 한다.** ① **판단 비용** — 거지맵 심판대가 돈 이유는 "이 가격이 맞냐"가 안 가본 사람도 사진만 보고 즉석에서 답하는 질문이라서다. "이 새우집이 실제로 있냐"는 가서 먹어봐야 안다. 형식만 복사하면 빈 목록이 남는다. ② **모수** — 서울 400곳에 주 신규 3곳, 그중 이용자가 다녀온 곳이 있을 확률은 사실상 0. 초기엔 신규가 0~2곳이라 늘 비어 있고, 빈 화면으로 가는 입구가 상시로 뜬다. ③ **운영자가 더 낫다** — 제보 알림 + 24시간 사후 확인(spec 5)이 더 빠르고 정확하며, spec 4.5 "사후 확인(새 제보)" 탭과 기능이 겹친다.
- **UI 위화감의 정체**: 칩 행의 나머지는 전부 "목록을 좁히는 필터"인데 [새로 들어온 집]만 **시트를 다른 화면으로 바꿨다**. 생김새가 같은데 하나만 동작이 다르면 예측이 안 된다. 자리를 옮기는 걸로는 안 풀리는 문제였다.
- **뺀 것**: 칩 [새로 들어온 집](`ChipKey`의 `"new"`까지), 신규 패널 컴포넌트·행·그 테스트, 시트의 패널 슬롯·헤더 분기·`EmptyKind: "new"`, `sortByCreatedDesc`. 코드는 커밋 `cb32d18`에 남아 Phase 6 /admin에서 꺼내 쓴다.
- **남긴 것**: 신규 표시(카드 라벨·점선 마커·상세 공지 행)는 *정보*라 그대로. 확인 참여는 상세 [다녀왔어요] 하나. **`flagPlace`와 사유 시트는 상세 맨 아래 [정보 수정 제안]으로 옮겼다** — 오류 신고는 참여와 성격이 다르다(잘못된 정보를 본 사람은 고치고 싶어 하고, 상세를 보는 중이면 판단할 능력도 있다). 준비 중 토스트로 막혀 있던 입구 하나가 실제로 동작하게 됐다.
- **부수 효과**: 칩이 4개로 줄어 390에서 전부 보인다(스크롤 없이). Phase 4 완료 조건의 "신규 패널" 항목은 관리자 이동으로 대체 기록.
- **되돌릴 조건**: 제보가 주 20건을 넘고 재방문 이용자가 생기면 참여형 검증을 다시 검토한다. 그때는 "판단 비용이 낮은 질문"(사진으로 되는 것)부터 찾는다.

## 2026-09-05 — Codex PR #8 코멘트 3건 (전부 P2, 셋 다 반영)

- **#1 로그인 게이트가 거짓 성공을 돌려줬다.** `resultRef`가 지난 성공(true)을 들고 있어서, 로그아웃 뒤 다시 게이트를 열고 **뒤로가기로 닫으면** popstate 경로가 `settle(false)`를 안 거치고 `finish()`를 직접 불러 그 true가 그대로 resolve됐다. 익명인데 리뷰 폼·내 활동이 열린다. 새 요청을 만들 때 `resultRef.current = false`로 초기화 — 한 줄. **규칙 승격**: 결과를 담아 두는 ref는 다음 요청 시작 때 초기화한다(CLAUDE.md 컨벤션).
- **#2 낙관 삭제와 재로드가 경합했다.** 내 리뷰를 지우는 400ms 동안 탭을 나갔다 오면 `getMyReviews()`가 아직 살아 있는 그 행을 다시 넣었고, 성공 경로에는 정리가 없어 지운 행이 남았다(실패하면 롤백이 한 벌 더 붙어 중복). 진행 중 id를 ref에 들고 ① 로드 결과에서 걸러내고 ② 성공·실패 양쪽에서 표식을 지우고 ③ 되돌릴 때 이미 있는지 확인한다. **규칙 승격**: 낙관적 업데이트가 진행 중인 항목은 재로드 결과에도 반영한다(CLAUDE.md 컨벤션). 목은 400ms라 재현이 어렵지만 Phase 6 실서버에서는 흔해진다.
- **#3 내 활동 마커에 옛 선택이 남았다.** `markerPool`은 활성 탭으로 걸렀는데 `items`가 `selectedPlace`를 조건 없이 덧붙였다 — 찜하지 않은 가게를 열었다 닫고 내 활동을 열면 그 마커가 남는다. `markerSelection` 파생을 두고 **내 활동에서는 마커 풀에 있을 때만** 덧붙인다. 상세가 열린 동안 필터와 무관하게 선택 핀을 유지하는 규칙(Codex PR #4)은 그대로다. 갭 스윕이 "스펙 외 구현"으로 기록했던 동작인데 버그가 맞았다.
- 셋 다 회귀 테스트를 붙였다(세션 게이트 재사용, 삭제 중 탭 왕복, 내 활동 마커). 테스트 373 → 377.

## 2026-09-07 — Phase 5 데스크탑 그릇(화면 6~9 v2): 단일 DOM + CSS 전환, 제보는 패널
- **맥락**: roadmap Phase 5. design 화면 6~9는 v1 초안(중앙 480 모달 + 240px 내장 지도, 패널 푸터 링크, 찜 핀 하트 뱃지, 새로 들어온 집 패널)이었다. 착수 규칙대로 v2로 재작성했다(플랜 docs/plans/phase5-desktop.md).
- **그릇은 CSS다**: 1024px(Tailwind `lg`, 64rem)부터 왼쪽 패널 400(`w-100`) + 오른쪽 전체 지도. DOM은 하나 — 모바일의 상단 스택(검색·칩)과 바텀시트를 `display: contents` 래퍼로 감싸고 lg에서만 그 래퍼가 flex 컬럼 패널이 된다. `.saeu-sheet`는 unlayered CSS라 유틸이 못 덮으므로 lg 재정의도 globals.css `@variant lg` 블록에 둔다(position static·transform none·flex 1). 데스크탑에만 있는 요소(브랜드 행 [＋ 제보]·줌 컨트롤·마커 툴팁)는 `useMediaQuery`(useSyncExternalStore, 서버 스냅샷 false)로 하이드레이션 뒤 붙고, 지도 기하(중심 y·fitBounds 마진·시트 드래그 무시)는 핸들러 안에서 `isDesktopViewport()`로 즉시 판정한다. 상수는 `lib/layout.ts` 한 곳.
- **제보는 모달이 아니라 패널**(사용자 선택): 2단계가 본 지도에서 핀을 찍는다. 중앙 모달이면 지도가 딤에 가리거나 두 번째 지도 인스턴스(핀·탭·중복 후보 배선을 모달 지도로 돌리는 비용)가 필요하다. 카카오맵 PC "장소 제안"·구글맵 "장소 추가"도 패널 + 본 지도다. **중앙 480 모달은 지도가 필요 없는 오버레이만**(로그인·리뷰 폼·사유·사진 신고·탈퇴) — `ModalSheet`·리뷰 폼 dialog에 lg 스타일만 더한다(라운드 16 토큰의 첫 용도). CLAUDE.md UI 완성 기준의 "플로우는 중앙 모달 480px"은 이렇게 정정.
- **호버 동기화**: 카드 hover → 마커 확대(`saeu-marker--hovered`), 마커 hover → 툴팁(상호 / 대표 메뉴 두 줄, React가 그린다 — 마커 innerHTML에는 여전히 이름을 넣지 않는다). `pointerType === "mouse"`만(터치 탭이 내는 에뮬레이션 hover 무시). 툴팁은 mouseout·dragstart·idle에 닫힌다.
- **추가·삭제**: 우하단 줌 [+][−] 추가(design 6 v1 항목, `MapHandle.zoomBy`), 현위치는 모바일 FAB과 같은 `LocateButton`. 패널 푸터 "새우맵 소개 · 사장님이신가요?"는 **뺐다**(라우트가 spec에 없다 — Phase 7 재검토). 찜 핀 하트 뱃지는 하지 않는다(Phase 4의 "활성 탭 가게만" 유지, 재검토 종결). 검색·칩은 상세에서도 보인다(네이버지도·카카오맵 PC 문법, 모바일과 같은 규칙).
- **재검토 조건**: 태블릿(768~1023) 전용 배치는 요청이 오면. 패널 폭 400은 카드·상세가 좁아 보인다는 피드백 시.
- **제보 CTA 색은 화면 3대로 단계마다 하나**(갭 스윕 모호 1건, 사용자 결정 같은 날): 화면 8 v2와 공통 블록에 썼던 "2단계 [여기가 맞아요]만 채운 레드, 나머지 잉크"는 오기다 — 단계가 곧 화면이라 각 단계의 바닥 CTA가 그 화면의 유일한 채운 레드이고, 같은 StepFrame 컴포넌트가 뷰포트별로 색이 달라지지 않는다. 코드는 그대로(`variant="brand"`), 문서만 정정.

## 2026-09-07 — 진짜 404는 proxy가 아니라 로딩 경계 제거로
- **맥락**: roadmap Phase 5 항목이 "진짜 404(proxy)"였고 Next 문서(loading.md "Status Codes")도 proxy를 권한다. 200이 나오던 원인은 루트 `app/loading.tsx`(Suspense)가 스트리밍을 시작해 `notFound()` 뒤에 상태 코드를 못 바꾸기 때문이다(decisions 2026-09-02).
- **결정(사용자 선택)**: 홈만 route group `app/(home)/`으로 옮겨 `loading.tsx`를 그 안에 두고, `/place/[id]`(기존 `loading.tsx` 삭제)·`/gu/[name]`에는 Suspense 경계를 두지 않는다 → `notFound()`가 스트리밍 전에 던져져 **HTTP 404**. 근거: OpenNext Cloudflare가 Node 런타임 `proxy.ts`를 "실험적·미지원"으로 명시한다(`@opennextjs/cloudflare` `bundle-node-middleware.ts`). 대가는 직접 진입 스켈레톤이 사라지는 것 — 목 데이터라 수 ms, Phase 6 Supabase에서도 한 쿼리다. 매치 안 되는 경로(`/test` 등)는 루트 `app/not-found.tsx`가 우리 빈 상태로 받는다.
- **재검토 조건**: OpenNext가 Node middleware를 정식 지원하고 스켈레톤 부재가 체감될 때.

## 2026-09-07 — `/gu/[name]`은 같은 지도 화면 + SSR 목록, 서울 25구만
- **결정(사용자 선택)**: 별도 랜딩이 아니라 `MapScreen`에 `initialGu`를 줘 그 구 가게로 지도를 맞춘다(0곳이면 구 중심·줌 13). 목록은 서버가 그 구 가게를 미리 채워 HTML에 상호가 들어간다(크롤러용) — 지도가 첫 idle을 보고하면 뷰포트 기준으로 자연히 전환된다. 헤더 "마포구 7곳"은 `areaLabel`이 만든다.
- **이름**: `lib/gu.ts`의 `SEOUL_GU` 25개(문자열 상수, `gu-boundaries.json` 이름과 일치하는지 테스트가 지킨다). 그 밖(김포시 등 비서울·오타)은 404. URL은 한글 그대로(`/gu/마포구`, 인코딩은 브라우저가 한다). 구 중심은 경계 박스 중앙(서버에서만, 60KB 동적 import).
- **구현 중 보정: 검색어를 그 구로 시작한다.** fitBounds만 하면 데스크탑의 넓은 지도(1040×900, 줌 13)에 이웃 구가 8개 넘게 들어와 헤더가 "서울 전체 23곳"으로 새고 목록도 마포구 밖 가게로 찬다. 검색어 = 구 이름이면 사용자가 "마포구"를 쳐서 얻는 화면과 같다 — 목록·마커가 그 구로 좁혀지고, 검색 바에 이유가 보이고, ✕ 한 번으로 풀린다. 새 상태·모드 없이 기존 필터 하나로 해결.
- **메타**: `lib/seo.ts` 순수 함수 — 제목 "마포구 새우구이 7곳", 설명 "마포구의 새우구이·생새우회 가게 7곳. 나라수산, …(3곳)". sitemap에 25구 전부(0곳 구 포함 — 런칭 글 "구별 카드 25장"의 자리).

## 2026-09-07 — OG 카드는 next/og로 **빌드 시 생성**, 폰트는 Pretendard subset woff
- **결정**: `opengraph-image.tsx` 파일 컨벤션 3종(루트·핀·구), 1200×630, 흰 바탕·잉크·좌하단 레드 점 + "새우맵", 카테고리색 마커 모티프. 사진은 넣지 않는다(외부 fetch 없음, 로고 에셋 대기). OpenNext 1.20이 `@vercel/og`를 edge 빌드로 재배선하고 `resvg.wasm`(1.4MB)을 동봉한다(`bundle-server.js`).
- **요청 시 렌더 → 빌드 시 생성으로 정정(같은 날, security-reviewer가 잡음)**: Workers **Free는 요청당 CPU 10ms**(초과 = Error 1102)인데 satori+resvg 한 장은 그보다 훨씬 길다. `generateStaticParams`(가게 전부 + 25구) + `dynamicParams = false`로 75장(가게 49 + 구 25 + 루트 1)을 빌드에 굽고, `open-next.config.ts`에 `staticAssetsIncrementalCache`(읽기 전용, 정적 에셋 `cdn-cgi/_next_cache/`)를 켠다 — 기본 dummy 캐시는 프리렌더 결과를 버려 요청마다 다시 그린다. 캐시 채우기(`.open-next/cache` → `assets/cdn-cgi/_next_cache`)는 `preview`·`upload`·`deploy` 명령이 하고 **`build`는 안 한다** — CI 스모크는 `populateCache local`을 한 번 더 부른다. 대가: 카드가 배포 시점 값이다 → **상대 시간("어제 확인")은 카드에서 뺐다**(배포 뒤 거짓이 된다), 가게 수·상호는 배포마다 갱신. **구 카드만 파일 컨벤션을 못 쓴다**: `gu/[name]/opengraph-image`의 프리렌더 키는 디코딩된 `/gu/강남구/…`인데 요청 경로는 퍼센트 인코딩이라 정적 서빙(workerd 실측)에서 not-found로 떨어졌다(핀 카드는 ASCII id라 정상). 그래서 `app/og/gu/[slug]/route.tsx`(로마자 슬러그 25개, `lib/gu.ts GU_SLUGS`)로 굽고 `guMeta`가 `openGraph.images`로 가리킨다. 재검증은 없다(지금 앱은 전부 요청 시 렌더라 필요 없음) — Phase 6에 ISR을 들이면 R2 캐시로.
- **폰트**: satori는 woff2를 못 읽고 Pretendard 전체 otf는 1.5MB다. `pretendard` 패키지의 **KS X 1001 subset woff**(Bold·Regular 각 350KB, SIL OFL)를 `public/fonts/og/`에 두고 `lib/og/font.ts`가 읽는다 — 빌드 프리렌더·`next dev`에선 `fs.readFile(public/…)`, 워커 컨텍스트가 있으면 `getCloudflareContext().env.ASSETS.fetch("http://assets.local/…")`(빌드 시 생성으로 바꾼 뒤엔 사실상 fs 경로만 탄다; ASSETS 경로는 요청 시 렌더 시절 workerd 실측으로 동작을 확인했다). 모듈 캐시로 한 번만. 폰트가 워커 번들에 들어가지 않아 500KB 번들 제한·3MB 워커 상한과 무관하다.
- **워커 크기**: 빌드 뒤 gzip 총량을 플랜 결과에 기록한다(Workers Free 상한 3MB, 이전 1.34MB). 넘으면 정적 PNG 카드로 후퇴.

## 2026-09-07 — vitest.setup의 matchMedia 스텁이 죽어 있었다 (하네스 발화 검증 규칙의 사례)
- **맥락**: `useMediaQuery`·`isDesktopViewport()`를 처음 쓰자 모든 화면 테스트가 `window.matchMedia is not a function`으로 죽었다. `vitest.setup.ts`의 `stub()`이 `key in target`으로 존재를 판정했는데, vitest의 jsdom 환경은 window의 모든 키를 globalThis에 **접근자**로 복사해 없는 API(matchMedia)도 `in`은 true인 채 undefined를 돌려준다. 스텁은 조용히 건너뛰어졌고, 그동안 아무 코드도 matchMedia를 부르지 않아 드러나지 않았다.
- **결정**: 판정을 `target[key] !== undefined`로. 하네스 요소(훅·CI·스텁)는 "파일이 있다"가 아니라 **실제 입력으로 발화를 확인**한 뒤에만 완료로 치는 규칙(CLAUDE.md 작업 방식)의 또 한 사례 — 스텁을 쓰는 테스트가 하나는 있어야 한다(`lib/__tests__/layout.test.ts`가 그 역할).

## 2026-09-07 — 사이트 URL은 서버 전용 `SITE_URL`
- **결정**: metadataBase·sitemap·robots가 쓰는 절대 URL은 t3-env `server`의 `SITE_URL`(없으면 `https://saeu-map.saeu-map.workers.dev`, http(s) 스킴만). `NEXT_PUBLIC_` 접두사를 쓰지 않는다 — 규칙 7 허용 목록(지도 Client ID·Supabase anon·카카오 JS 키) 밖이고 클라이언트가 필요하지 않다. 도메인(saeumap.kr)은 Phase 7에 값만 바꾼다.
- **런타임에 닿는 길(security-reviewer가 잡음)**: OpenNext 워커의 `process.env`는 Cloudflare 바인딩(wrangler `vars`·secrets)과 `.env*`에서만 채워진다 — CI 셸 env는 **빌드 때만** 보인다. 그래서 프로덕션은 `wrangler.jsonc` `vars.SITE_URL`, 프리뷰는 `pnpm run upload --var SITE_URL:https://preview-…`로 버전에 심는다. 정적으로 빌드되는 `robots.txt`만 빌드 env를 읽는데, 프리뷰 호스트(`preview-*`)면 `disallow: /` — 프리뷰가 프로덕션과 중복 색인되지 않게.

## 2026-09-07 — Lighthouse CI 예산은 모바일 LCP, workerd 스모크 뒤에
- **결정**: CI `check` 잡의 스모크가 띄운 `wrangler dev :8787`에 `npx @lhci/cli@0.15.1 autorun`(의존성 아님, 2025-06 릴리스) + `lighthouserc.json`. URL `/`·`/place/p018`, 3회 중앙값, 모바일 에뮬레이션(Lighthouse 기본). CI엔 NCP 키가 dummy라 지도는 에러 상태 — 우리 셸(HTML·CSS·JS)의 LCP를 재는 것이고 지도 SDK는 측정 밖이다.
- **실측(2026-09-07, 로컬 workerd, dummy 키, 3회 중앙값)**: `/` LCP **8.1~8.7s**(3회 8.2·8.7·12.8 — 편차 큼), `/place/p018` **4.2s**, performance 0.40~0.71. `/`의 LCP 요소는 지도 에러 상태의 안내 문장이고 Render Delay가 7~12s — 4× CPU 스로틀에서 앱 청크 부트업 1.9s·하이드레이션·SDK 초기화가 다 끝나야 그려진다. 네트워크는 스크립트 387KB·폰트 14조각 360KB·지도 타일 826KB(dummy 키여도 타일은 내려온다).
- **예산**: `largest-contentful-paint` **error 12,000ms**(측정 중앙값의 약 1.4배 — 새 500KB 스크립트 같은 큰 회귀만 막는 가드) + `categories:performance` **warn 0.5**. web.dev "good"(2.5s)은 지금 셸로는 목표가 아니라 백로그다(아래). 3회 중앙값(`aggregationMethod: median`)이라 한 번 튄 값엔 안 흔들린다.
- **같은 실측이 잡은 개선 1건**: 상세 첫 사진이 `loading="lazy"`인데 LCP 요소였다 — 하이드레이션 뒤에야 요청돼 Load Delay 1~3.6s. 첫 장만 `priority` → `/place/p018` LCP 중앙값 **8.0s → 4.2s**.
- **CI 첫 발화(PR #9)**: `/` performance 0.47 중앙값(warn 0.5 아래 — 경고만), LCP 예산 통과. 리포트 아티팩트는 `actions/upload-artifact`가 숨김 경로 `.lighthouseci`를 기본 제외해(`include-hidden-files` 기본 false, v4.4~) **비어 있었다** — `if-no-files-found: ignore`가 그걸 조용히 통과시켰다. 옵션을 켜고 `error`로 바꿈. 하네스는 파일 생성이 아니라 발화 검증이 완료 조건이라는 규칙의 사례 하나 더.
- **점수를 PR에서 바로 보기 — CI가 PR 코멘트를 남기는 방식으로, 다음 코드 PR에서(2026-09-07 사용자 결정)**: 지금은 점수가 아티팩트(리포트 HTML/JSON)와 잡 로그에만 있어 내려받아야 보인다. 세 가지 중 ① Lighthouse 스텝 뒤에 `.lighthouseci/manifest.json`·리포트 JSON을 jq로 읽어 URL별 performance·LCP 표를 `gh pr comment`로 올리고 재실행 땐 같은 코멘트를 갱신(GITHUB_TOKEN + `pull-requests: write`, 새 패키지 없음, 셸 15줄)을 택했다. ② LHCI 공식 상태 체크는 `filesystem` 타깃에서 동작하지 않고(문서 명시) `temporary-public-storage`로 바꿔야 해 리포트가 공개 스토리지에 올라간다. ③ `GITHUB_STEP_SUMMARY`는 PR 화면이 아니라 런 페이지다. Phase 5 PR #9에는 넣지 않는다(roadmap 백로그).
- **백로그(집는 시점: 런칭 전 실기기 LCP가 4s를 넘으면)**: 앱 청크 분할(지도 SDK·supercluster 지연 로드), Pretendard CSS preload, 폰트 조각 수. 목표 warn 4000ms를 error로 내리는 건 그때.

## 커스텀 에셋 필요 목록
- 현위치 표적(크로스헤어) 아이콘 — coolicons에 없어 `components/ui/icons/crosshair-icon.tsx` 인라인 SVG로 임시 대체
- ~~새우 로고 워드마크~~ — 2026-09-08 투입 완료(`public/wordmark.webp`, 데스크탑 브랜드 줄). OG·파비콘·홈 아이콘은 2026-09-07에 채웠다
- ~~카카오 심볼~~ — 2026-09-08 투입 완료(`public/kakao-symbol.png`). 아래 항목 참조.
- ~~까주기 테스트 유형 캐릭터 4장~~ — **2026-09-09 투입 완료**. `public/peel-test/{slug}.webp`(화면 320, 15KB) + `{slug}-og.png`(OG 400 — satori가 webp를 못 읽을 수 있어 두 벌). 장면: 집게로 굽는 새우 · 접시에 회를 놓는 새우 · 받아먹는 새우 · 초장에 찍는 새우. 쓰이는 곳 다섯: 결과 히어로 · 매트릭스 · 표지 미리보기 · 궁합 두 마리 · OG 카드
- **(선택) 문항 일러스트 6장** — `public/peel-test/q1.png`…`q6.png`, **512×512 PNG 투명**, 캐릭터와 같은 톤(플랫 채움·잉크 점 눈·그림자 없음). 문항 화면은 글자 두 줄 + 버튼 둘뿐이라 지금은 카드 안 옅은 새우 워터마크로 대신한다. **2026-09-09 문항을 다시 짜서 장면도 바뀌었다**:
  - q1 다 익은 새우 접시와 젓가락 두 짝 / q2 김 나는 구이 새우와 얼음 위 생새우 / q3 껍질을 까느라 지저분해진 손(집게발) / q4 술잔 옆에 놓인 새우 / q5 **소금 냄비 뚜껑이 들썩이는 장면**(이 음식의 상징) / q6 한 입 베어 문 새우
  - 없으면 지금 워터마크로 계속 간다 — 기능은 완결이다
## 2026-09-07 — Codex PR #9 코멘트 2건: P1 반영, P2 보류(실측으로 판단)
- **P1 `lib/env.ts` 반영**: `.env.example`의 `SITE_URL=`을 빈 채로 복사하면 `""`가 들어와 `z.url().optional()`이 거부하고 앱이 안 뜬다. t3-env `emptyStringAsUndefined: true`(문서가 새 프로젝트에 권하는 옵션) 한 줄 + `lib/__tests__/env.test.ts` 3건(빈 값=없음·있으면 그대로·`javascript:` 거부). 테스트는 jsdom에 window가 있어 t3-env가 클라이언트로 보므로 `vi.stubGlobal("window", undefined)`로 서버로 읽힌다.
- **P2 `use-map-screen.ts` 보류**: "1024 경계를 넘겨 리사이즈·회전하면 첫 위치 맞추기가 한 번뿐이라 지도가 옛 그릇 기준으로 남는다"는 지적은 사실이다(`initialPanDone`). 그러나 Playwright 실측(상세 열린 채 1023×768 ↔ 1200×768): 모바일→데스크탑은 SDK autoResize가 가로 중앙을 다시 잡고 선택 마커는 지도 컬럼 중앙보다 **80px 위**에 보이며, 데스크탑→모바일은 마커 y=304 < 시트 상단 498로 **시트 위에 남는다**. 가려지는 건 `/gu/[name]` 직접 진입 뒤 상호작용 없이 데스크탑→모바일로 넘긴 fitBounds의 아래쪽 핀뿐. 고치려면 매체 쿼리 변화에 `panTo(선택 가게 | 지금 중심, { screenY })`를 SDK 리사이즈 **뒤에** 걸어야 하는데 그 타이밍이 SDK 내부(throttle)라 지연 상수가 필요하다 — 값이 작은 데 비해 다음 리뷰 지적감이다. **집는 시점**: 태블릿 전용 배치를 들일 때(Phase 5 항목 "재검토 조건")와 같이 — 회전이 일상인 기기가 대상이 되면 그때 리사이즈 완료 신호(`map.getSize()`가 컨테이너와 같아질 때)를 기준으로 넣는다.


## 2026-09-07 — 새우 에셋 투입 (마커·카드 플레이스홀더 · 목 사진 · 루트 OG)
- **맥락**: 사용자가 GPT로 생성한 4장(새우 픽토그램·냄비 일러스트·소금구이 사진·생새우회 사진). 브랜치 `feat/assets-desktop`, 플랜 `docs/plans/shrimp-assets.md`. 데스크탑 리디자인은 이 뒤 별도 플랜.
- **크기 실측이 판단을 다 정했다** (sips로 축소 → 8배 확대해 육안):

  | 에셋 | 16px | 20px | 36px | 64px |
  |---|---|---|---|---|
  | 새우 원본 | 형체 없음 | 얼룩 | 읽힘 | 읽힘 |
  | 새우 **여백 크롭** | 뭉갬 | 얼룩 | **선명** | 선명 |
  | 냄비 일러스트 | — | 얼룩 | 읽힘(버너·불꽃 소실) | 읽힘 |
  | 새우 **단순화 벌**(수염·다리 뺌) | **읽힘**(눈까지) | 선명 | 선명 | 선명 |

- **핵심은 투명화가 아니라 여백 크롭이었다**: 원본은 새우가 프레임의 55%라 36px 마커에 넣으면 실제 새우가 20px가 된다. 처음에 "투명 PNG로 뽑아 달라"고 했던 건 어긋난 요구였다 — 마커는 `overflow:hidden` + 원형 클립이라 배경이 어차피 잘린다.
- **그래도 흰 배경은 알파로 뺐다**: `bg-sunken`이 지금은 항상 gray-100이지만 다크 팔레트가 들어오면 흰 사각이 뜬다. 알파 키잉은 `min(r,g,b)` 기준 235~250 사이를 부드럽게 깎는다(안티에일리어싱 테두리). 몸통 구분선의 흰 틈도 같이 뚫리는데, 회색 위에서 원래 의도대로 읽힌다.
- **카테고리 두 색이 필요 없다**: 마커 바깥 링(`--marker-color` 코랄/틸)이 이미 카테고리를 말한다 → 안쪽 그림은 **한 벌**이면 되고, 그래서 "검정 실루엣으로 뽑아 currentColor로 칠한다"는 계획도 불필요해졌다. 컬러 아트를 그대로 쓴다.
- **마커는 CSS 한 규칙만 바꾼다**: `.saeu-marker__dot`의 배경. `marker-icons.ts`의 innerHTML 문자열·아이콘 캐시 키·`safeAssetPath` 가드가 전부 그대로다(규칙 3의 방어선을 건드리지 않는다).
- **목 사진은 2장뿐이라 대표 사진만 카테고리에 맞춘다**: `photos[0] = thumbnailUrl`이 마커·카드에 뜨므로 회 전문점(p025 명가수산)은 photo-2(생새우회)가 첫 장이고, 나머지는 두 장을 번갈아 둔다 — 사진 뷰어 인덱스가 눈으로 확인된다. 머리버터구이·한 상 차림 2장을 받으면 슬롯만 늘린다.
- **WebP 변환은 `sips`가 못 한다**(`Error: Can't write format: org.webmproject.webp`) → `node_modules`에 이미 있는 `sharp`를 1회성 스크립트로 호출했다. **package.json 무변경**, 스크립트는 리포에 안 남긴다. 2.7MB PNG → 150KB WebP.
- **테스트의 `src` 단언을 경로 포함으로 바꾼 이유**: SVG는 next/image가 최적화를 자동으로 건너뛰지만(`dangerouslyAllowSVG` 기본 false) WebP는 아니다. vitest는 `next.config.ts`(`images.unoptimized`)를 읽지 않아 `/_next/image?url=…`이 잡혔다 — 설정에 기대지 않게 `toContain("thumb-1.webp")`로.
- **루트 OG만 아트를 넣는다**: 클러스터 모티프(레드 원 + 가게 수)는 왼쪽 브랜드 캡션("가게 N곳")과 중복이었다. 가게·구 카드는 카테고리색 모티프라 **구이 편향 그림이 생새우회 카드에 들어가면 틀린 그림**이 된다 — 그대로 둔다. satori는 외부 URL을 못 가져오므로 data URI 인라인(`lib/og/art.ts`), 루트 OG는 빌드 시 한 번 굽는 정적 라우트라 폰트와 달리 ASSETS 바인딩 경로가 필요 없다.
- **파비콘·홈 아이콘도 같이 넣었다(처음 판단 정정)**: "16px에서 뭉개니 보류"는 비교 대상을 잘못 잡은 것이었다 — 대안이 `app/icon.svg`의 **코랄 색점**이라 뭉개진 새우가 퇴행이 아니다. 게다가 레티나 탭은 16 CSS px을 **32 device px**로 그리는데 32px에선 새우가 명확히 읽힌다. `app/icon.png` 32(흰 라운드8 + 새우, 어두운 탭바 대비) · `app/apple-icon.png` 180(iOS가 스스로 스쿼클로 깎으므로 라운딩 없는 꽉 찬 사각 + 여백 26). `icon.svg` 삭제.
- **수염·다리를 뺀 단순화 벌로 전 크기를 통일한다(2차 정정)**: 처음엔 "작은 크기는 단순화 벌, 큰 크기는 디테일 벌"로 두 벌을 두려 했는데, 16·20·26·32px을 나란히 찍어보니 **단순화 벌이 전 크기에서 이겼다** — 16px에서도 눈과 머리→꼬리 흐름이 읽히고, 32px에서도 디테일 벌보다 깔끔하다. 180px에서도 밀리지 않는다: 얇은 수염이 오히려 생성물 티(design.md "AI가 만든 티가 나면 실패")를 내던 부분이었다. 옵티컬 사이징으로 두 벌을 관리할 값이 없어 **한 파일**로 간다.
- **제보 핀은 전용 에셋 없이 닫았다**: CSS 도형(`.saeu-report-pin`)이 이미 좌표를 가리키는 기하(바닥 끝 = 앵커)를 갖고 있어, 머리 안 8px 흰 점만 **18px 흰 새우**로 바꿨다 — `shrimp.webp`의 알파가 곧 실루엣이라 `mask`로 쓴다(아이콘 유틸과 같은 방식). 새우를 핀 대신 놓지 않은 이유 둘: ① 새우는 뾰족한 끝이 없어 어느 좌표인지 못 가리킨다 ② 제보 2단계에서 기존 가게 마커가 그대로 보이는데 그것들도 이제 새우라, 핀까지 새우면 뭘 끌고 있는지 모른다. 결과적으로 채운 레드 + 흰 새우(제보) ↔ 흰 원 + 주황 새우 + 카테고리 링(가게)으로 갈린다.
- **선택 마커의 잉크 링을 흰 링 + 진한 그림자로 바꿨다(사용자 지적)**: 원래 "확대 + 잉크 링"은 design.md 화면 1 v2에 적혀 있던 스펙인데 **근거가 기록돼 있지 않았고, 비교해서 고른 것도 아니었다**. 네 벌(잉크 / 브랜드 레드 / 링 없이 그림자만 / 흰 링 + 진한 그림자)을 실제 크기로 구워 보니 잉크는 링이 두 겹으로 보여 **카테고리 코랄 링을 죽이고**, 레드는 코랄과 인접색이라 뭉개지며 클러스터와도 겹친다. 흰 링은 팔레트 충돌이 없는 유일한 색이고(레드 = 클러스터, 코랄·틸 = 카테고리), 지도 위 요소의 문법이 원래 그림자다(design 공통 블록). 토큰 `--shadow-marker-selected: 0 2px 10px 0 rgb(0 0 0 / 0.24)` 신설 — 그림자 4종 → 5종. design.md 화면 1·2·6 "잉크 링" 3곳 정정.
- **현위치 마커는 파란 점으로 확정(2026-09-04 "최종은 새우 모양" 결정을 뒤집는다)**: 에셋 제약은 풀렸다 — 단순화 벌은 20px에서도 선명하다. 뒤집는 이유는 **전제가 바뀌었기 때문**이다. 그 결정 때 가게 마커는 카테고리 색점이었는데 지금은 마커도 새우다. 둘 다 새우면 지도에서 내 위치를 못 찾는다. 현위치는 "나"이고 마커는 "가게"라 다른 종류여야 하고, 파란 점은 구글·네이버·카카오가 공유하는 관습이라 학습 비용이 0이다. `.saeu-locator` 그대로 둔다. **커스텀 에셋 대기 목록에서 내린다.**
- **보류 0건**— 이번 작업으로 전부 처리했다. 커스텀 에셋 목록은 8줄 → **3줄**(크로스헤어·워드마크·카카오 심볼).
- **제보 완료 화면에 새우를 넣었다(사용자 결정, design 화면 3-5 정정)**: 원래 "체크 애니메이션 자리는 없다"였는데, 그 금지의 대상은 **체크마크 클리셰**였지 축하 자체가 아니다 — 제보 완료는 이 앱에서 유일하게 사용자가 기여를 끝낸 순간인데 화면에 보상이 없었다. **처음엔 왼쪽 정렬 64px 중립 새우**로 넣었는데 실기기 656에서 보니 축하가 아니라 그냥 아이콘이었다 — 가운데 정렬 + 제목·캡션도 가운데로 바꾸고 **168px 냄비 아트**로 키웠다(사용자 선택). 냄비가 구이 쪽 그림이라 생새우회 전용 가게 제보에도 같은 아트가 나오는 건 **알고 감수한다**: 완료 화면은 장면이 있어야 축하가 되고(김·불꽃), "지금 굽고 있다"가 제품 메시지("지금 되는 집")와도 맞는다. 카테고리별로 아트를 가르려면 `StepFrame`에 prop이 하나 늘고 회 전용 아트도 따로 필요해서, 값이 날 때 집는다.
- **별점 마크를 별 → 새우로(사용자 아이디어)**: `star-icon.tsx` 삭제, `shrimp-icon.tsx`(마스크 span) 신설. 세 곳이 같이 바뀐다 — 리뷰 행 5칸·평균 "4.7" 옆 1개·리뷰 폼 입력 36px. **컬러 아트로는 안 된다**: 채움/빔이 구분되지 않아 평점으로 안 읽힌다(실측). `shrimp.webp`의 알파를 마스크로 써서 `currentColor`로 칠하면 별과 **똑같은 문법**(색 하나로 갈림)이 된다. 색은 **브랜드 레드**(`brand-fg` = red-600): 잉크·코랄과 나란히 구워 보니 빔(gray-200)과의 대비가 가장 좋고 브랜드가 산다. 코랄은 카테고리 색이라 의미가 겹쳐 뺐고, "채운 레드 화면당 한 곳"은 **버튼** 규칙이라 걸리지 않는다(클러스터 레드 원이 이미 선례). 크기는 12 → 14 → **16px**(평균 옆은 18, 폼 입력은 36): 작은 크기에서 빈 새우가 뭉갠다 — 별보다 형태가 복잡한 값이다. 평균 "4.7" 옆 새우는 **1px 내린다**: 새우는 시각 무게가 위쪽(머리·몸통)이고 꼬리가 아래로 가늘어, 박스 가운데를 맞추면 숫자보다 떠 보인다. **접근 이름 "별점 N점"은 그대로** 둔다(사용자 결정) — "별점"이 일반명사화돼 있고 바뀐 건 UI뿐이다. 잃는 것도 적는다: 별은 만국 공통이라 0.1초에 읽히는데 새우는 한 번 생각하게 만든다. 앱 이름이 맥락을 준다고 보고 감수한다.
- **애니메이션은 CSS 한 벌로 — GIF도 Lottie도 아니다**: GIF는 256색 팔레트라 그라데이션이 뭉개지고 알파가 1비트라 테두리가 계단이 되며 용량이 WebP의 몇 배다. Lottie는 새 패키지 + After Effects 소스가 필요한데 우리에겐 정적 이미지 한 장뿐이다. 코드 최소주의 사다리 3칸("웹 표준으로 되나")에서 걸린다 — `@keyframes saeu-pop` 520ms + `cubic-bezier(0.34, 1.56, 0.64, 1)`(살짝 오버슈트) + `both`(첫 페인트에 큰 새우가 번쩍이지 않게). **파일도 패키지도 안 늘었다.** 기존 `saeu-shimmer`와 같은 문법이고 `prefers-reduced-motion` 블록에 옵트아웃을 추가했다.

## 2026-09-08 — 데스크탑 리디자인(화면 6~9 v3): 떠 있는 패널 · 칩만 지도 위 · 혼합 카드

사용자 판단으로 시작했다 — **"모바일 디자인을 그대로 패널에 넣은 그릇이라 밋밋하다."** 시안 4벌(A 정석 확장 / B 플로팅 / C 2열 매거진 / D 플로팅+매거진 요소)을 실제 크기로 구워 비교하고 **D**를 골랐다. 플랜·결과는 docs/plans/desktop-redesign.md.

- **붙은 패널 400 → 떠 있는 패널 420(2026-09-07 화면 6 v2를 뒤집는다)**: 지도가 뷰포트 전체를 채우고 패널이 그 위에 카드로 뜬다(여백 16, 라운드 20, shadow-panel). 뒤집는 이유는 **전제가 틀렸기 때문**이다 — v2는 "검색·칩을 전부 패널 안에 넣으면 지도가 깨끗해진다"고 봤는데, 실제로는 칩이 두 줄로 랩되며 패널 세로를 66px 먹었고 지도는 오른쪽 1040으로 잘려 보였다. 대가도 적는다: 패널 뒤 지도는 어차피 안 보이므로 **실효 지도 폭은 v2와 같다**(착시). 얻는 건 "지도 앱"으로 읽히는 인상과 접기의 여지다.
- **칩 행만 지도 위로, 검색 바는 패널 안(사용자 결정)**: 성격이 다르다 — **칩은 지도에 그려지는 것을 거르는 컨트롤**이라 누른 결과를 그 자리에서 보고, **검색은 목록을 만드는 입력**이라 결과가 나오는 표면(패널)에 붙어야 한다. 덤으로 자동완성 제안이 지도를 덮지 않고, 검색 바 끝에 달린 프로필 버튼이 지도 위로 떠오르지 않는다(로고·주 버튼을 지도에 두지 않는다는 공통 블록 규칙과 같은 결). 네이버지도·카카오맵 PC도 검색은 왼쪽 패널 최상단이다. **공통 블록의 "지도 위 두 층"은 이제 모바일 규칙**이고 데스크탑은 한 층이다.
- **패널 접기는 만들어 본 뒤 뺐다(사용자 결정, 같은 날 뒤집기)**: 요청대로 손잡이 + 접힘 입구([목록 N곳] pill·돋보기)까지 구현하고 1440에서 눌러 봤는데, **떠 있는 카드가 통째로 사라지는 게 어색했다**("플로팅 패널인데 숨기니까 이상해지네"). 붙은 패널이라면 접기가 자연스럽지만(패널이 지도 폭을 실제로 뺏으니까), 떠 있는 패널은 지도가 이미 그 뒤로 이어져 있어 **접어서 얻는 지도가 착시뿐**이다. 게다가 접힘 상태를 위해 두 번째 입구(pill·돋보기)를 따로 둬야 해서 컨트롤이 상태에 따라 이사 다닌다. 지도를 더 보고 싶으면 지도를 끌면 된다. 되돌린 것: `panelCollapsed` 상태·손잡이·접힘 입구·`MapHandle.panBy`(접었다 펼 때 되미는 용도였다).
- **지도 기하 보정이 새로 필요하다**: 패널이 지도 위에 겹치므로 `panTo`·`fitBounds`에 왼쪽 (패널 폭 + 여백) 보정을 준다. 안 하면 선택한 마커가 패널 뒤로 숨는다. 붙은 패널일 땐 지도 컬럼이 곧 가시 영역이라 없던 문제다.
- **카드는 사진 유무로 두 벌(2열 그리드는 채택하지 않았다)**: 사진 있는 집은 16:9 사진 카드, 없으면 콤팩트 행. 2열 매거진(시안 C)은 화보로는 가장 세지만 **목 50곳 중 사진이 3곳(6%)** 이라 지금 만들면 큰 회색 타일 40장이 되고, 패널 560이 지도를 880으로 깎는다. "사진은 어차피 채워진다"는 지적은 맞지만 **1열 사진 카드는 커버리지가 올라도 레이아웃 변경 없이 화보로 자란다** — 지금 좋고 나중에도 좋은 쪽을 골랐다. 2열 화보는 사진이 쌓인 뒤 `/gu/[name]`에서 살린다(백로그).
- **카드에 가격·역·평점·찜을 올린다**: 훑어보기 단계에서 고를 수 있어야 한다(지금은 상세에 들어가야 가격이 보인다). 평점은 리뷰 3개 이상일 때만(2026-09-02 결정 유지)이고, **집계는 lib/data.ts가 한다**(규칙 1 — Phase 6에서 이 함수만 Supabase 집계로 교체).
- **정렬은 데스크탑만 세그먼트**: 모바일은 헤더 "가까운순 ▾" 트리거 유지(세로 예산 656~702).
- **"이 지역에서 다시 검색"은 넣지 않는다**: 목록이 뷰포트로 자동 갱신된다(`use-map-screen.ts`의 `inView`). 그 pill은 자동 갱신을 안 하는 앱의 보완책이라 우리가 넣으면 같은 일을 두 번 시키는 버튼이 된다.
- **[＋ 제보]는 상단 고스트 + 목록 끝 채운 레드(2026-09-07 "브랜드 행 채운 레드 pill"을 뒤집는다)**: 워드마크가 텍스트에서 **이미지 에셋**으로 바뀌자 로고와 레드 버튼이 한 줄에서 서로 경쟁했다. 채운 레드는 목록을 다 훑고 "여긴 없네" 하는 순간(제보 동기가 가장 높다)으로 옮긴다. "채운 레드 화면당 한 곳"은 그대로 지킨다.
- **이벤트는 공지 행 → 틴트 배너 카드**: 레드 틴트 라운드 16 + 오른쪽에 새우 아트 92px를 -12° 기울여 걸치고 부제에 행동을 붙였다("20초면 끝나요"). 공지 행 문법은 정확했지만 밋밋했다 — 이벤트는 한시적으로 미는 것이라 리스트 카드와 같은 라운드 리듬을 갖되 색으로 구분되는 게 맞다. 모바일·데스크탑 공용.
- **여백·리듬 — "AI 티"의 정체**: 사용자가 "간격이 좁고 AI 티가 난다"고 했고, 원인은 **모든 간격이 균일하고 정보가 균등하게 나열된 것**이었다. 좌우 22, 세로 8/14/20 3단 리듬, 카드 사이 구분선 제거(여백으로 분리), 사진 카드 안쪽 여백 확대, 헤드라인 25 bold 자간 -3.5%.
- **워드마크 에셋 투입(커스텀 에셋 대기 3줄 → 2줄)**: 사용자가 만든 둥근 획 로고("새우"는 웜 브라운 #402018, "맵"은 레드, 위에 코랄 획 두 개). 딱딱했던 원인은 이름이 아니라 **본문 폰트(Pretendard semibold)로 쓴 이름**이었다. 로고는 폰트가 아니라 에셋이다 — 로고 한 줄 때문에 한글 웹폰트를 싣는 건 사다리 4칸(새 의존성)에서 걸린다. **로고의 레드는 브랜드 레드 #F04A28에 맞춘다**(옆의 [＋제보]·클러스터 마커와 나란히 보여 미세한 차이가 실수로 읽힌다). 브라운은 로고 고유색으로 유지 — 우리 잉크(#191F28 쿨)와 다른 건 의도로 읽힌다. 배경은 투명이어야 한다(원본은 흰 배경이 박혀 있어 선택 카드·다크 모드에서 흰 사각형이 드러난다).
- **버튼 커서가 전부 `default`였다(사용자 지적)**: 실측으로 확대·축소·내 위치·제보·검색·칩·정렬·카드·닫기 전부 `cursor: default`(링크와 지도 마커만 pointer). **Tailwind v4 Preflight가 버튼 커서를 브라우저 기본으로 두기 때문**이다. `globals.css` base 레이어에 `button:not(:disabled), [role="button"]:not(:disabled) { cursor: pointer }` 한 규칙으로 전부 잡는다. 앞으로 커서는 전수 검사로 확인한다(검증 항목에 추가).
- **hover 사진 프리뷰(사용자 선택)**: 마커 hover에 사진 있는 집은 사진 카드, 없으면 기존 2줄 텍스트 툴팁. 진입 150ms 지연·이탈 300ms 유예(프리뷰로 마우스를 옮기는 중 닫히면 잡을 수 없다)·가장자리 자동 플립·드래그/줌 시작 시 즉시 닫힘·클러스터 제외.
- **모바일도 사진 카드로 통일해 보고 판단한다(사용자 결정)**: "같은 컴포넌트" 원칙엔 통일이 맞지만 390×656에서 카드당 ~310px이라 1.3장만 보인다. 통일로 구현해 실기기 등가로 재고, 이상하면 모바일만 사진 비율을 낮춘다(16:9 → 2:1). 결과는 플랜 문서에 적는다.

### 구현 중 보정 (같은 날, Playwright 1440×900·1280×800·1024×768 + 390×702·656·320×480 + workerd :8787)

- **`hit-44`는 unlayered CSS라 `position: relative`가 `absolute` 유틸을 이긴다** — 이벤트 배너의 닫기 ✕가 카드 왼쪽 아래로 흘렀다. 자리는 래퍼가 잡고 버튼은 히트 영역만 갖게 나눴다. `.saeu-sheet`가 유틸을 이기던 것(2026-09-07)과 **같은 원인**이다: unlayered 규칙은 `@layer utilities`보다 세다.
- **칩 행은 DOM을 옮기지 않고 `lg:fixed`로 지도 위에 올렸다** — 모바일 두 층의 간격·시트 스냅 계산(`topStackRef`)을 건드리지 않으려고. fixed는 조상 `overflow: hidden`에 잘리지 않아 패널을 통과한다. **대신 패널은 transform을 가질 수 없다**(transform이 있으면 fixed의 기준이 그 패널이 된다) — 접기를 뺀 뒤 `transition-transform`도 함께 없앴다.
- **토스트는 그릇별로 나눠 단다** — 떠 있는 패널이 `absolute`라 그 안에 두면 "화면 아래 가운데"가 패널 기준이 된다. 모바일은 상단 스택의 마지막 층 그대로.
- **데스크탑 토스트를 데스크탑 문법으로 다시 그렸다(사용자 지적 "모바일 걸 그대로 갖다 써서 별로다")**: 모바일 토스트는 전폭 가운데 정렬 + 넉넉한 세로 패딩이라 1440에서는 화면 한복판에 검은 슬래브가 떨어졌고, **행동이 일어난 곳(왼쪽 패널)과 멀었다**. 세로 패딩을 줄이고 `shadow-card` + 6px 떠오르는 진입 모션(160ms, `prefers-reduced-motion` 옵트아웃)을 줬다. `Toast`는 `className` 슬롯만 받고 **자리·크기는 그릇이 정한다**.
  - **자리는 두 번 옮겼다**: 처음엔 구글맵식으로 지도 왼쪽 아래(패널 옆)에 뒀는데 사용자가 "맨 밑이라 잘 안 보일 수 있다"고 했다 — 맞다. 세 패턴(좌하단 스낵바 = 구글맵·Gmail / 우상단 스택 = Linear·Vercel / 상단 중앙 = 한국 서비스 다수)이 공유하는 원칙은 **행동 근처 + 콘텐츠를 안 가림**인데, 우리 안내는 대부분 **왼쪽 패널에서 한 행동의 결과**(찜 실패·로그인 넛지·로그아웃·제보 불가)라 지도 구석은 "안 가리지만 안 보이는" 자리였다. **패널 안 바닥**(Gmail의 실행취소 바 자리)으로 옮겨 지금 보고 있는 곳에 뜨게 했다 — 마지막 카드가 2초 가려지는 건 감수한다.
  - **패널 밖(셸 루트)에 두고 좌표만 패널과 맞춘다** — 패널이 `overflow-hidden`이라 안에 넣으면 그림자가 잘린다.
- **평점은 쓰기 응답에도 붙인다** — `checkIn`이 평점 없는 Place를 돌려주면 낙관 갱신(`patchPlace`)이 카드의 평점을 지운다. `getPlaceDetail`도 같은 계약으로 맞췄다(security-reviewer 지적).
- **찜 토글에 세션 가드**(security-reviewer 낮음 1) — 토글 중 로그아웃·승계·탈퇴가 끼면 이전 사용자의 찜 목록이 새 세션 화면에 앉는다. 목은 마이크로태스크라 창이 0에 가깝지만 Phase 6 왕복에선 실제 창이다. CLAUDE.md "비동기 결과는 아직 그 화면인지 확인" 규칙에서 이 쓰기만 빠져 있었다.
- **선택된 핀에는 hover 프리뷰를 그리지 않는다** — 패널이 이미 그 상세다. 다른 핀의 프리뷰는 그대로 뜬다(상세를 열어 둔 채 옆 가게를 훑는 길).
- **문서의 시안 픽셀값을 토큰 언어로 고쳤다** — 브랜드 줄 44→52, 검색 44→48(모바일과 같은 컴포넌트), CTA 46→48(`size="xl"`), 하트 28→32, 프리뷰 250→248(`w-62`), 헤드라인 25/-3.5%→`title-m-bold`, 좌우 22→20(안전영역 토큰과 같은 값), 카드 타이포는 px 대신 텍스트 스타일 이름. 시안은 임의값으로 그렸지만 **구현은 토큰 12종 안에서만** 한다(임의값 금지). 정렬 셋째 칸은 design v3 초고의 "이름순"이 아니라 spec 4.1의 **"확인 많은 순"**이 맞다. "자동완성 제안" 문장은 그런 기능이 없어 지웠다.
- **하트는 사진 카드에만(콤팩트 행에서 뺐다, 사용자 지적)**: 처음엔 두 벌 모두에 달았는데 콤팩트 행에서 **오른쪽 열에 성격이 다른 둘**(위 "3일 전 확인" = 상태 / 가운데 하트 = 액션)이 서로 다른 높이로 걸려 시선이 두 번 멈췄고, 긴 상호는 하트에 가렸다(자리를 안 비워 둔 내 실수). 사진 카드는 **사진 모서리라는 앵커**가 있어 하트가 그 위에 얹히는 게 자연스럽지만 콤팩트 행엔 그 앵커가 없다. 찜은 목록의 주 액션이 아니라 상세에 있다 — design v3 초안도 사진 카드 하트만 적었다.
- **카드 메타 줄에 거리는 하나만(사용자 지적 "다른 요소도 애매하다")**: `10m · 양천구 · 새우구이 │ ⑤ 오목교역 180m`은 **뜻이 다른 숫자 둘**("나까지"·"역까지")이 나란히 서서 읽을 때마다 구분해야 했다. 역까지의 거리는 상세에 있으니 카드에선 **역 이름만** 남기고, 세로 헤어라인도 빼 한 줄로 흐르게 했다.
- **스펙 밖으로 더 들어간 것 2개(의도)**: 평점 옆 리뷰 수 `(3)`(3개 이상만 보이는 값이라 개수가 신뢰도를 말한다), 빈 상태에서는 목록 끝 CTA 대신 빈 상태의 [제보] 아웃라인.
- **찜을 낙관적 업데이트로 바꿨다(사용자 결정)**: `toggleBookmark`에 다른 쓰기와 같은 계약(지연 400ms·10% 실패)을 주고, 하트는 **누르는 즉시** 바뀌고 실패하면 되돌아오며 토스트가 뜬다("쓰기는 상태 변화까지" — UI 완성 기준). 곁들여 지킨 세 가지: ① **검증은 지연 전에** — 빈 id·없는 가게를 400ms 뒤에 거부하면 롤백이 그만큼 늦는다 ② **진행 중 항목을 ref에 들고 재로드 결과에 덮어씌운다** — 세션이 바뀌며 찜을 다시 읽을 때 낙관 상태가 되살아나거나 지워지지 않게(성공·실패 **양쪽에서** 표식을 지운다) ③ **연타 방향은 진행 중 낙관 상태 기준** — 화면 목록만 보면 같은 틱의 두 번째 클릭이 방향을 잘못 잡는다. (CLAUDE.md 낙관 업데이트 규칙 2026-09-05를 그대로 적용)
- **워드마크 에셋 투입 — 원본 색 그대로(사용자 결정 정정)**: "로고 레드를 브랜드 레드에 맞춘다"는 앞의 결정을 **뒤집는다**. 맞춰 보니 그림이 죽었다(사용자 판단). 로고는 사실 표기라 팔레트 예외를 갖는다 — 브라운 #402018 · 자체 레드 · 코랄 획 그대로 쓴다. 옆 [＋ 제보]가 아웃라인이 되면서 두 레드가 나란히 서는 문제도 사라졌다.
  - **처리**: 원본 1774×887(흰 배경, 여백 큼) → 글자 bbox (359,191)-(1411,652) 크롭 → **흰 배경을 알파로**(픽셀을 "팔레트색이 흰 위에 a로 얹힌 것"으로 보고 되돌린다 — 임계값 컷은 가장자리가 계단이 된다) → 표시 26px의 ~2.3배인 **137×60 WebP 4KB**. `public/wordmark.webp`, 브랜드 줄의 `<h1>` 안 `<Image alt="새우맵">`(모바일은 h1이 sr-only라 alt가 곧 이름이다).
- **제보 2단계에서 클러스터를 눌러도 핀이 옮겨진다(사용자 버그 제보)**: "데스크탑에서 지도를 클릭해도 핀이 안 꽂힌다" — 원인은 **클러스터 마커가 지도의 상당 부분을 덮고 있는데**(1440 격자 탐침에서 13칸) 2단계 클러스터 클릭이 `return`으로 빠져 아무 일도 안 했기 때문이다. 2단계에선 클러스터도 "지도의 그 자리"로 보고 핀을 옮긴다(개별 마커는 그대로 중복 의심 후보 — design 화면 3 변형 (a)). 빈 지도를 누를 땐 원래도 동작했다(실측 6/6 이동).
- **지도 위 칩 행의 빈 자리가 지도 클릭·드래그를 삼켰다**: 행 컨테이너가 `pointer-events: auto`라 칩 사이 간격과 줄 끝 여백이 지도를 가렸다. 행은 통과시키고 **칩만** 받게 했다(상단 스택이 쓰는 것과 같은 문법).
- **가격 입력은 십만 원대(6자리)까지(사용자 제안)**: 9자리(억 단위)를 받을 이유가 없다 — 새우 한 판·1kg이 백만 원을 넘지 않는다. 오타로 0을 하나 더 쳐도 그 자리에서 막힌다. `PRICE_MAX_DIGITS = 6`이 화면 입력과 zod 스키마(`max(999_999)`)를 함께 정한다 — **UI 제한만으론 검증이 아니다**.
- **테스트 깜빡임 하나를 잡았다(Stop 훅이 발화)**: `report-flow`의 "한국 밖(바다) 핀은 막힌다"가 부하 걸린 머신에서만 실패했다. 원인은 구 경계 파일(서울 60KB + 전국 180KB)이 **첫 사용 때 동적 import**되는데 그 비용이 `findBy`의 기본 1초 창 안에 들어온 것 — 검사할 건 판정 결과지 모듈 로딩이 아니다. 스위트 시작 전에 두 파일을 한 번 데운다(모듈 캐시라 한 번뿐). 같은 종류가 또 나오면 `findBy` 타임아웃을 늘리지 말고 **비용을 창 밖으로 빼는** 쪽으로 고친다.
- **제보 4단계에 "네이버 지도 링크(선택)"를 받는다(사용자 제안)**: 규칙 2가 막는 건 **API 응답을 저장하는 것**이지 사용자가 붙여넣은 문자열이 아니다(카카오 공식 답변도 "직접 입력한 값"을 예외로 든다). 네이버 지도 앱의 [공유 → 링크 복사]는 `naver.me/…` 형식인데 **`isAllowedNaverPlaceUrl`의 허용 호스트에 이미 들어 있어** 검증 코드를 그대로 재사용했다(https + m.place/place/map.naver.com/naver.me). 값이 있으면 상세의 "네이버에서 사진 보기"가 살아난다 — **사진이 6%뿐인 지금 업로드보다 문턱이 훨씬 낮은 우회로**다. 검증은 **붙여넣는 즉시** 하고 틀리면 CTA를 막는다(등록까지 갔다가 실패하면 어디가 틀렸는지 모른다). 대가: 다른 가게 링크를 붙여도 우리가 확인할 방법이 없다(링크를 열어 대조하는 건 또 크롤이다) — 상세 [정보 수정 제안]과 Phase 6 관리자 큐가 받는다. `naver.me` 리다이렉트 대상까지는 검증하지 않는다(네이버 소유 도메인 + `noopener noreferrer`).
- **1단계 가게명 자동완성을 풀었다(사용자 지적 "너무 빡빡하다")**: ① 최소 2자 → **1자** ② 띄어쓴 토큰은 **순서 무관**("목동 홍초장" → 홍초장 목동점) ③ **초성 검색**("ㅍㅍㅅㅅ" → 팔팔수산, 초성만 친 토큰에만 적용해 오검색을 늘리지 않는다). 초성 질의는 정렬도 초성끼리 비교한다(한글 이름과 직접 비교하면 유사도가 전부 0이라 순서가 뜻을 잃는다). **중복 판정(`same_place`)은 다른 함수라 그대로 엄격**하다 — 검색은 느슨해도 되고 중복 판정은 아니어야 한다.
- **2단계 주소 검색은 우리가 조인 게 아니라 API가 자동완성이 아니다**: 지오코더 실측(2026-09-08) — "마" 0건 / "마포" 1건 / **"마포대로" 0건** / "마포대로 3" 1건 / "목동로" 0건. 입력 중 자동 검색(디바운스)을 붙여도 대부분 "결과 없음"만 깜빡인다. 그래서 **빈 결과 문구에 어떻게 쳐야 하는지를 넣었다**("건물번호까지 넣어보세요 (예: 마포대로 3)")와 placeholder 정정. 진짜 자동완성이 필요하면 **행안부 도로명주소 검색 API(juso.go.kr)** 로 갈아타야 하는데, 승인키를 숨기려면 서버가 필요하고 그 API는 결과 저장도 허용되므로 **주소 자동 채움(A안)과 같은 작업으로 묶어 Phase 6**에 한다.
- **남긴 것**: 새 사진 렌더 3곳(`place-card` 2곳·`map-view` 프리뷰)은 `safeAssetPath`를 다시 지나지 않는다 — 데이터 계층이 보장하지만 Phase 6에서 스토리지 URL이 들어오면 방어선이 한 겹뿐이다(마커 아이콘은 `safeThumbnailUrl`로 재검증한다).

## 2026-09-08 — Codex PR #10 코멘트 3건: 전부 반영 + 규칙 승격 1건

세 건 다 유효했다(P2 3). 하나는 **이 PR에서 내가 만든 회귀**다.

- **#1 쓰기가 세션을 지연 뒤에 읽었다(내 회귀)**: 찜에 `simulateWrite()`(400ms)를 넣으면서 `await` **다음에** `currentSession.userId`를 읽게 됐다 — 그 사이 로그아웃하면 이전 사용자의 요청이 **새 익명 사용자의 찜을 건드린다**. 훅의 세션 가드는 응답만 버릴 뿐 이미 일어난 쓰기는 못 되돌린다. 행위자를 지연 **전에** 잡는다. 테스트: 쓰기 도중 `signOut()`을 끼워 새 사용자의 찜이 비어 있는지 본다.
- **#2 연타가 직렬화되지 않았다**: 같은 하트를 400ms 안에 두 번 누르면 첫 응답이 **두 번째 클릭이 소유한 pending 표식을 지우고** 중간 상태를 앉혔고, 첫 요청이 실패·두 번째가 성공하면 서버가 마지막 의도와 **반대로** 끝났다. 근본 원인은 API가 **토글(상대값)** 이라는 것 — `toggleBookmark(id)` → **`setBookmark(id, wanted)`(멱등)** 으로 바꾸고, 가게별 요청 순번으로 늦게 온 응답을 버린다. Phase 6의 insert/delete와도 같은 모양이다.
- **#3 선택해도 이미 떠 있는 프리뷰가 안 닫혔다**: hover 가드는 앞으로의 진입만 막았다. 마우스를 안 움직인 채 마커를 클릭하면 `mouseout`이 없고 `panTo`는 `dragstart`·`zoom_changed`를 내지 않아 프리뷰가 상세 위에 남았다(design "선택된 핀은 프리뷰를 그리지 않는다"와 어긋남). **마커 클릭 핸들러에서** 프리뷰를 닫는다 — `selectedId` effect로 하면 "effect 안 setState 금지" 린트에 걸리고, 트리거는 상태가 아니라 사용자의 행동이다. 새 테스트가 고침을 되돌리면 실패하는 것까지 확인했다.
- **규칙 승격(같은 종류 2회째 — security-reviewer의 찜 세션 가드 + 이번 #1)**: CLAUDE.md 컨벤션과 AGENTS.md 체크리스트에 **"쓰기 함수는 지연·await 전에 행위자를 잡고, 상대값이 아니라 원하는 상태를 받는다"** 를 넣었다. Phase 6에서 서버 쓰기가 늘면 같은 실수가 반복될 자리다.

## 2026-09-08 — 막힌 입구 전부 열기: 상세 제안·신고·사장님 · 사진 실반영
- **맥락**: 사용자 지적 "상세에서 수정 같은 거 누르면 준비 중이에요가 많다". gap-sweeper 전수 조사로 **막힌 입구(A) 8곳 · 화면 없음(B) 2곳 · 반쪽 동작(C) 4곳**을 확인했다. 여태 미룬 근거는 decisions 2026-09-02 "미구현 플로우 입구 8곳"과 design.md의 같은 문단이었고, 이유는 도착지가 사용자 화면이 아니라 관리자 수정 제안 큐(spec 4.2 "승인 큐 경유", 4.5)였다는 것. **그 판단을 여기서 끝낸다** — 이미 두 번 앞으로 당겨졌고(사진 신고 2026-09-03, 정보 수정 제안 사유 시트 2026-09-05) 남은 것도 같은 성격이다. 플랜 docs/plans/pending-flows.md.
- **범위**: 상세 7곳(사진·영업시간·주소·메뉴·사이드·신고·사장님) + 이벤트 배너 404 + 제보·리뷰 사진 실반영 + 스켈레톤 2건. **`/admin` 4탭은 다음 브랜치**(PR은 함께) — admin 표의 열은 큐에 무엇이 쌓이는지로 정해지므로 이 브랜치가 먼저다. `/test` 까주기 테스트는 Phase 7.
- **상세의 쓰기 표면은 세 문법뿐**(design 화면 2에 같은 표): 사유 시트(탭이 곧 제출) / 값 폼 시트(입력 + 바닥 CTA) / 파일 선택기 직행. 네 번째를 만들지 않는다.
- ~~**제안에는 낙관적 업데이트를 쓰지 않는다**(승인 큐 경유라 화면 값이 그대로다)~~ → **같은 날 뒤집었다**. 아래 "수정은 즉시 반영" 항목 참조.
- **가게 신고 사유 4개**: 새우집이 아니에요 / 허위·광고성 등록 / 중복 등록이에요 / 기타. **[문 닫았어요]는 넣지 않았다** — 정보 수정 제안 사유와 겹친다. 경계는 "이 등록 자체가 잘못됐다"(신고 → 3회 자동 숨김) vs "값이 틀렸다"(수정 제안 → 큐).
- **사장님이신가요? = 요청 폼 시트**(요청 종류 2 + 연락처 필수 + 내용 선택). 이메일 안내로 하지 않은 이유: saeumap.kr 도메인이 아직 등록 전이고(spec 6), spec 5의 "24시간 내 처리"는 회신할 연락처가 있어야 성립한다.
- **주소 제안에 자동완성을 붙이지 않는다**. 지오코더 응답은 절대 규칙 2로 저장할 수 없고, **사용자가 직접 친 값만** 남길 수 있다(`naverPlaceUrl`과 같은 판례, 같은 날 앞 항목). 제보 2단계 주소 검색과 생김새가 다른 건 그래서다.
- **blob URL은 `lib/data.ts`가 자기 것을 만들어 보관한다**. `PhotoPicker`는 자기 목록의 objectURL을 언마운트 때 revoke하므로 그 URL을 그대로 `Place.photos`에 넣으면 **시트가 닫히는 순간 사진이 깨진다**. 데이터 쪽에서 `URL.createObjectURL`을 다시 불러 세션 동안 들고 있고(revoke 안 함), 서버(SSR·OG 빌드)엔 이 API가 없으니 존재를 확인하고 없으면 사진 없이 진행한다.
- **이벤트 배너의 `href`는 선택값이 됐다**. 없으면 링크가 아닌 안내 카드(닫기만). 지금 목 설정값이 가리키던 `/test`는 Phase 7이라 라우트가 없어 **카드를 누르면 404로 떨어지고 있었다**. 링크할 곳이 없다는 사실을 데이터로 표현한다 — Phase 7에 `/test`가 생기면 설정값에 href를 다시 넣는다.
- **`PlaceDetailSkeleton`은 삭제하지도, 연결하지도 않는다.** 붙일 자리를 찾다가 **그 상태가 목 단계에 존재할 수 없다**는 걸 확인했다: `mode`가 `detailPlace`에서 파생되므로(`use-map-screen.ts`) "상세인데 가게가 없는" 상태 자체가 만들어지지 않고, 딥링크는 서버가 `initialDetail`까지 실어 보내며, `/place/[id]`의 로딩 경계는 진짜 404 상태 코드를 위해 일부러 뺐다(2026-09-07). 억지로 도달 가능하게 만들면 목록에 없는 id에서 영영 안 끝나는 스켈레톤이 된다. 컴포넌트는 비동기 상세가 생기는 **Phase 6에서 살아난다** — 그 사실을 컴포넌트 JSDoc과 design 변형 (d)에 적었다.
- **문서 정정**: design.md의 "아직 없는 플로우의 입구" 문단(149줄)을 **쓰기 표면 블록으로 교체**, 화면 2-9의 "신고·사장님은 그대로 준비 중 토스트" 삭제, 화면 3 완료 화면의 "리뷰도 남겨볼래요? — Phase 4까지 준비 중"이 Phase 4에서 연결된 잔재라 정정. design.md에 "준비 중이에요"는 0건이 됐다.
- **Lighthouse 점수 PR 코멘트를 백로그에서 당겨왔다** — 조건이 "다음 코드 PR에 커밋 하나로"였고(2026-09-07) 이 PR이 그것이다. `check` 잡과 **별도 잡**으로 나눴다: 그 잡은 npx로 Lighthouse 전이 의존성을 받으므로 `pull-requests: write` 토큰을 같은 환경에 두지 않는다(`persist-credentials: false`와 같은 이유). 예산에 걸려 실패했을 때가 점수를 가장 보고 싶은 때라 `always()`로 돈다.
- **뒤집은 결정 — PR 코멘트 갱신은 `gh pr comment --edit-last`가 아니라 표식(첫 줄 HTML 주석) 조회다.** 프리뷰 URL 코멘트가 쓰던 방식인데, 한 PR에 봇 코멘트가 **둘**이 되는 순간 "마지막 것"이 서로를 덮어쓴다. Lighthouse 코멘트를 들이면서 드러났고, 프리뷰 쪽도 같이 고쳤다 — 둘 다 `scripts/pr-upsert-comment.sh`를 쓴다. `head -1`을 파이프로 물리면 `pipefail` + SIGPIPE로 잡이 죽어서 변수에 받아 자른다.
- **뒤집은 결정 — 메뉴 수정 시트는 제보 폼(구이/회 2줄)을 재사용하지 않는다.** 사용자 지적으로 드러났다: 크롤 가게는 메뉴가 **중앙값 3줄·최대 5줄**(50곳 중 64%가 3줄 이상)이고 화면(화면 2-5)은 그 줄을 다 그리는데, 폼만 2줄이라 앞 두 줄만 채워지고 나머지가 사라지는 것처럼 읽혔다. 2줄 모델은 원래 **제보용**이다(spec 4.3 "대표 메뉴 한 줄이면 돼요" — 이탈을 막으려 낮춘 문턱이라 그대로 둔다).
  - **고른 형태**: 시트가 지금 있는 줄을 다 보여주고 **가격만** 고친다 + 줄마다 [없어졌어요] + 맨 아래 [메뉴 추가] 한 줄. 흔한 의도 셋(가격 올랐다·없어졌다·빠졌다)을 한 화면에서 받는다.
  - **메뉴명·단위를 안 받는 이유**: 크롤 이름 잔재("새우머리튀김 0", "(가을 한정 판매)", 이름에 박힌 가격)는 **우리 파싱 문제**지 사용자가 고칠 일이 아니고, 익명 사용자가 남의 가게 메뉴명을 갈아엎는 걸 승인 큐가 다 받게 할 이유도 없다.
  - **미채택**: 제보 필드를 늘리는 안(제보로 생긴 가게는 메뉴가 1~2줄이라 애초에 안 어긋난다 — 문제를 안 푼다), 크롤 메뉴를 2줄로 자르는 안(64%가 3줄 이상이라 실제 가격을 버린다).
- **크롤 메뉴 정제는 Phase 6 임포트에서.** 지금 시드는 `raw`→`name` 분리는 됐지만(150줄 중 144줄) spec 2의 "이모지·프로모션·옵션 제거"가 덜 됐고 **단위가 74%(111/150) 미파싱**이다. 목 50곳만 고쳐도 452곳 임포트 때 덮이므로 `convert_seed.py` 쪽에서 한 번에 한다.
- **뒤집은 결정 — 영업시간·주소·메뉴·사이드는 승인 큐가 아니라 즉시 반영이다** (사용자 판단). 근거는 **네 스펙 안에 이미 있던 판례**다: 제보는 가게 하나를 통째로 만드는 더 큰 쓰기인데도 "즉시 노출 + 운영자 24시간 내 사후 확인"(spec 5)이다. 가격 한 줄 고치는 게 큐에서 대기할 이유가 없고, **1인 운영에서 큐는 밀린다 — 밀리면 아무도 두 번 고쳐주지 않는다**(제안 기능이 죽는다).
  - **전제는 되돌리기다.** 제보는 잘못돼도 그 핀만 지우면 되지만 수정은 기존 값을 덮어써서 지우는 걸로 못 되돌린다. 그래서 `PlaceEdit`(이전 값 통째로 + actor + 시각)를 남기고, `/admin` **수정 이력 탭**에 [되돌리기]를 둔다. 이력 없이 즉시 반영은 하지 않는다.
  - **익명 허용은 유지**(spec 5 경계 그대로). "익명 = 무기명"이 아니다 — Supabase anonymous auth로 모든 활동이 익명 id에 기록되므로 되돌리기·섀도 밴·속도 제한이 다 된다. 리뷰만 로그인인 이유는 **별점이 평균에 들어가는 집계**라서고(조작 방어), 수정은 되돌릴 수 있는 사실이라 성격이 다르다.
  - **자유 텍스트(영업시간·주소·새 메뉴명)가 익명으로 바로 뜨는 위험**은 spec 5 스팸 4겹 3(URL 패턴 거부·욕설 블록리스트)이 Phase 6에서 받는다.
  - **`tags`(구이/회)는 제안으로 바뀌지 않는다** — 추가된 줄이 회인지는 운영자가 사후 확인에서 정한다.
  - 화면: 시트 카피가 "확인 후 반영돼요" → **"바로 반영되고 운영자가 확인해요"**(CTA 위), 토스트는 "고쳐주셔서 고마워요". 낙관적 업데이트는 여전히 안 쓴다 — 폼은 실패 시 되돌릴 **입력**이 남아서 리뷰 폼처럼 시트를 열어 둔 채 오류를 보이는 게 맞다(사진은 되돌릴 게 없어 낙관이 맞았다).
  - **미채택**: 로그인 필수로 승격(참여 문턱), 익명은 큐·로그인은 즉시(같은 입구가 사람마다 다르게 작동해 카피로 설명할 수 없고 큐가 다시 생긴다).
- **로그인 공급자는 카카오만 유지**(spec 5 재확인, 사용자 판단). 이 앱에서 **로그인이 필요한 건 리뷰 하나뿐**이고(나머지는 전부 익명 가능) 카카오는 공유(카카오 JS 키)에 이미 쓰고 있어 앱 등록이 전제다. 공급자를 늘리면 **계정 중복**이 생긴다 — 같은 사람이 카카오·구글로 각각 가입하면 리뷰·찜이 흩어지고, `linkIdentity`(익명→카카오 승계) 위에 계정 병합까지 얹어야 한다. **자체 회원가입은 하지 않는다**(비밀번호·이메일 인증·재설정·스팸 가입 방어가 1인 운영에 순수 부담이고 얻는 게 없다).
  - **조건부 백로그**: 런칭 후 "로그인 시트를 띄웠는데 카카오로 안 넘어간 비율"이 실제로 높으면 구글 추가를 재검토한다. 그건 런칭 후에나 보이는 숫자다.
- **카카오 심볼 투입 완료 — 공식 버튼 이미지에서 떼어냈다.** 카카오싱크 디자인 가이드가 배포하는 건 **버튼 전체 이미지(PNG·PSD)**뿐이고 심볼 단독 SVG가 없다. 직접 그리거나 커뮤니티 아이콘 셋(`simple-icons`)을 쓰지 않은 이유: 가이드가 **"심볼 형태·비율·색상 변경 불가"**, **"카카오톡 심볼이나 카카오 CI 사용 불가"**로 못 박았고 로그인 권한 심사 항목이다(`simple-icons:kakaotalk`은 카카오**톡** 앱 아이콘이라 애초에 금지 대상).
  - **추출 방법**: `complete/ko/kakao_login_large_wide.png`(600×90)에서 심볼 bbox를 픽셀 스캔으로 재고(x28 y28, 36×34) 잘라낸 뒤, 합성식을 역산해 알파를 복원했다 — 배경이 정확히 #FEE500(254,229,0)이고 잉크가 (25,22,0)이라 **가이드의 "심볼 #000000 90% 투명도"와 계산이 맞아떨어졌다**(0.1×254=25.4). 660바이트.
  - **크기는 공식 버튼 비율 그대로**: 90px 버튼에 34px 심볼 → 48px 버튼에 18px(19×18). PSD도 버튼이 1x라 **공식 래스터는 36×34가 최대**다. 2x 커버리지라 3x 기기에서 약간 무를 수 있다 — 실기기에서 거슬리면 완성형 버튼 이미지를 통째로 쓰는 쪽으로 바꾼다(그 경우 "로그인 중…" 상태를 버튼 밖으로 빼야 한다).
  - **가이드 대조 결과 두 곳이 어긋나 있었다**: (1) 레이블이 `text-fg`(우리 잉크 #191F28)였는데 가이드는 **#191919**다. (2) 더 나쁜 건 `fg`가 Semantic이라 **다크 모드에서 흰색으로 뒤집혀 노란 버튼에 흰 글자**가 됐다. `--color-kakao-900: #191919` Primitive를 `kakao-500` 옆에 두고 버튼만 쓴다 — 지하철 노선색·카카오 옐로와 같은 "사실 표기" 결이다.
  - 나머지는 이미 맞았다: 문구 "카카오로 시작하기"(가이드 허용 완성형), 컨테이너 #FEE500, 라운드 12px(6dp), 심볼+레이블 가운데 정렬.
- **재검토 조건**: 사진 ＋ 타일 직행이 실수 업로드를 부르면 미리보기 시트를 한 겹 넣는다. 사장님 요청의 본인 확인 문턱은 실제 요청이 들어와 본 뒤에. Lighthouse 코멘트는 **이 PR에서 실제 발화를 확인해야 완료**다(하네스 규칙).

## 2026-09-08 — hover 프리뷰가 마커에서 어긋나 있었다 (사용자 지적)
- **증상**: 데스크탑에서 마커에 hover하면 사진 프리뷰가 마커 위가 아니라 엉뚱한 곳에 떴다. 1440에서 **가로로 218px** 어긋났고, 지도를 끌수록 더 벌어졌다.
- **원인**: `projection.fromCoordToOffset()`이 주는 건 컨테이너 좌표가 아니라 **오버레이 pane 좌표**다. 그 pane은 지도를 끌 때마다 translate되므로(실측: 마커 `style.left` 1382px인데 화면 x는 1222 — 조상 pane이 −160만큼 밀려 있었다) 그 값을 그대로 `left/top`에 쓰면 pane 이동분만큼 틀어진다. 처음 화면에서 세로가 맞아 보인 건 우연이었다.
- **고침**: **두 offset의 차**로 바꿨다 — 중심의 offset을 빼면 pane 이동분이 상쇄되고, 컨테이너 중앙(`size/2`)에 더하면 컨테이너 픽셀이 된다. 같은 파일의 `panTo`가 이미 이 식(중앙 기준 offset 차)을 쓰고 있었다 — **정답이 옆줄에 있었는데 프리뷰만 값을 그대로 썼다.**
- **검증**: 초기·지도 이동 후 모두 프리뷰 중심 = 마커 중심(dx 0), 마커 위 8px(`PREVIEW_GAP`).
- **배운 것**: 지도 SDK의 "offset"은 컨테이너 픽셀이 아닐 수 있다. 같은 파일 안에 이미 쓰이는 식이 있으면 그 식을 따르거나, 왜 다른지 적어야 한다.

## 2026-09-08 — /admin 운영 화면: 표 5탭 (design 화면 10 신설)
- **맥락**: 앞 브랜치가 사용자 화면의 막힌 입구를 전부 열었는데 **그것들이 만드는 일감이 도착할 곳이 없다**. `/admin`은 라우트조차 없었다(spec 4.5). 브랜치 `feat/admin`(`feat/pending-flows` 위에 쌓음, PR은 함께), 플랜 docs/plans/admin.md.
- **조사에서 나온 것 — 플랜을 바꾼 사실 셋**:
  1. **신고·요청이 아무 데도 저장되지 않는다.** `flagPlace`·`reportPlace`·`reportPhoto`·`submitOwnerRequest`가 zod 검증 + 400ms 지연만 하고 버린다. admin에 띄울 행이 없어 **저장소부터 만들어야 한다**(`getPlaceEdits()`는 앞 브랜치에서 만들어 뒀다).
  2. **`Place`에 운영 상태 필드가 없다** — spec 4.4의 "검증 전/확인됨", spec 5의 "신고 3회 자동 숨김(복구 가능)"을 담을 자리가 없다 → `verifiedAt`·`hiddenAt`.
  3. **사후 확인은 폰에서 해야 한다.** spec 5가 정의한 행동이 "텔레그램 알림 → 24시간 내 **플레이스 링크 30초 체크**"라 알림을 받은 자리에서 끝나야 한다. spec 4.5의 "데스크탑 표"를 그대로 읽으면 폰에서 못 쓰는 화면이 된다 → **사후 확인 탭만 모바일 카드**, 나머지는 표.
- **탭은 4 → 5.** 통계를 더하고(사용자 선택), "신고·숨김"을 **"신고·요청"**으로 넓혔다 — 가게 신고·사진 신고·정보 수정 제안·사장님 요청이 갈 곳이 없었는데 넷 다 "사용자가 알려온 일감"이고 흐름이 같다(열림 → 처리함/무시함). 종류 칩으로 거른다.
- **[확인]은 배지(`verifiedAt`)만 찍는다** — 카드·마커의 "새로 제보됨"은 7일 타이머(`isNew`)이고 "새로 생겼다"는 정보지 "검증했다"가 아니다. 둘을 묶으면 진짜 새 가게인데도 사용자가 신규를 알 길이 없어진다.
- **신고 3회는 사람 기준 중복 제거 후 센다** — 혼자 세 번 눌러 남의 가게를 내리는 걸 막는다(spec 5 "악성 신고자 섀도 밴"과 같은 결).
- **삭제는 소프트**(`hiddenAt` + `removedByOwner`) — spec 5의 "재제보 시 경고 표시"에 기록이 필요하다. 관리자 화면에서 **유일하게 확인 모달을 쓰는 동작**이다. 되돌리기는 파괴적이지 않고 대칭이라(되돌린 것도 이력에 남아 다시 되돌린다) 확인 없이 즉시 + 토스트.
- **비관리자는 `notFound()`** — 로그인 유도도 "권한 없음"도 아니다(spec 4.5 "404 위장"). 관리자 화면이 있다는 사실 자체를 알리지 않는다. 목 판정은 `Session.isAdmin` + dev 전용 토글이고 **URL 쿼리로는 켜지 않는다**(프로덕션에서 열리면 안 된다). 실제 판정은 Phase 6 `profiles.is_admin` + 서버 검증.
- **표는 장식을 쓰지 않는다** — 띠·줄무늬·카드 그림자 없음. 밀도가 값인 화면이라 장식이 읽기를 방해한다. 대신 토큰·공용 컴포넌트(`Segmented`·`Chip`·`Button`·`Skeleton`·`EmptyState`·`ErrorState`·`ModalSheet`)는 사용자 화면 것을 그대로 쓴다 — 관리자라고 새 팔레트를 만들지 않는다.
- ~~**방문자 분석은 Cloudflare Web Analytics, GA4는 미채택**~~ → **같은 날 뒤집었다: 둘 다 쓴다**(아래 항목). 원래 근거: 스크립트 한 줄·무료·**쿠키리스라 동의 배너가 필요 없고** 이미 CF Workers에 배포 중이라 설정이 가장 짧다. GA4는 ~50KB가 LCP 예산(error 12s, 2026-09-07)을 먹고 쿠키 때문에 첫 화면에 동의 배너가 뜬다 — **지도를 가리는 배너는 최악**이다. 이벤트 퍼널이 필요할 만큼 커지면 재검토. **네이버 서치어드바이저는 분석 도구가 아니라 검색 색인·노출 관리**라 목적이 달라 둘 다 한다. 붙이는 시점은 런칭 전이고 지금은 spec 6에 기록만.

## 2026-09-08 — 뒤집음: GA4도 붙인다 (CF Web Analytics와 함께)
- **사용자 판단**. 같은 날 "GA4 미채택"으로 적었다가 뒤집었다. 근거가 바뀐 게 아니라 **저울의 반대편을 과소평가**했다: CF Web Analytics는 "몇 명이 왔나"까지고, 우리가 실제로 판단해야 하는 건 **퍼널**이다 — "제보를 시작한 사람 중 몇 %가 끝냈나", "로그인 시트를 본 사람 중 몇 %가 카카오로 넘어갔나". 이번 세션에서 미룬 결정(익명 유지 vs 로그인 필수, 구글 공급자 추가, 새로 들어온 집 필터 되살리기)이 전부 그 숫자를 조건으로 걸고 있다.
- **원래 반대 근거는 그대로 유효하고, 그래서 배선으로 막았다**:
  - LCP 예산(error 12s) → `next/script` **`lazyOnload`**. 분석 스크립트는 첫 화면이 그려진 뒤에 와도 되는 종류다.
  - 수집 시점 → **`NEXT_PUBLIC_GA_ID`가 없으면 스크립트를 아예 안 붙인다.** dev·프리뷰에서 수치가 섞이지 않고 런칭 전까지 수집이 0이다. 규칙 7 허용 목록에 측정 ID를 추가했다(공개 값이다).
  - 동의 배너 → **아직 안 만든다.** GA4는 쿠키를 쓰므로 개인정보처리방침·동의 배너를 **측정 ID를 넣기 전에** 정한다(roadmap Phase 7). 지금은 배선만 있고 켜지지 않은 상태다.
- **CF Web Analytics는 그대로 둔다** — 쿠키 없이 방문자·유입을 보는 값은 GA4가 대체하지 못한다(동의 거부한 사용자도 세어진다).
- **`@next/third-parties`를 쓰지 않았다**: 새 의존성 없이 `next/script` 두 줄이면 되고, 그래야 로딩 전략(`lazyOnload`)을 우리가 쥔다(사다리 4칸에서 걸린다).

## 2026-09-08 — Codex PR #11·#12 코멘트 7건: 전부 반영
사용자가 전부 고치기로 선별. 7건 다 사실이었고, 세 부류였다.

**내가 문서에 약속하고 안 지킨 것 (3건)**
- **`NEXT_PUBLIC_GA_ID`가 허용 목록 밖이었다**(P1). CLAUDE.md만 고치고 **AGENTS.md를 빠뜨렸다** — "결정이 바뀌면 CLAUDE.md·AGENTS.md에서 grep해 정정"을 내가 어겼다.
- **사장님 "게재 삭제" 요청을 [처리함]으로 닫으면 가게가 안 내려갔다**(P1). 상태만 `done`이 되고 `removedByOwner`가 영영 안 찍혀 spec 5의 "재제보 시 경고"가 죽어 있었다. 확인해보니 `byOwner=true`를 넘기는 호출처가 코드에 **하나도 없었다**. 버튼 라벨도 [내리고 처리]로 바꿔 무엇을 하는지 말하게 했다.
- **`openReportCount`가 아무 데서도 안 쓰였다**(P2). 자동 숨김을 철회하며 "대신 눈에 띄게 표시한다"고 spec·design에 적어놓고 그 표시를 안 만들었다. 신고 3건이 넘는 가게에 틴트 배지를 붙였고, **게이트 없는 집계 함수를 따로 여는 대신 이미 게이트를 지난 목록에서 센다**(security-reviewer가 지적한 "게이트 없는 읽기" 모양을 새로 만들지 않는다).

**같은 파일에 정답이 있는데 새 코드만 빠뜨린 것 (3건)**
- **시트를 닫으면 반영이 유실됐다**(P2). 보내고 400ms 안에 닫으면 쓰기는 커밋됐는데 화면은 옛 값이고, 다시 열면 stale 값으로 덮어썼다. `usePhotoUpload`는 이미 "쓰기는 이미 일어났고 진실은 부모다"로 alive 가드를 안 뒀는데 **시트만 달랐다** → 부모 갱신은 무조건, 닫기·토스트만 "아직 열려 있을 때".
- **수정하면 별점이 사라졌다**(P2). `checkIn`은 `withRating`을 얹어 돌려주는데 `submitSuggestion`·`addPlacePhotos`는 raw place를 그대로 반환해, 호출자가 통째로 갈아끼우며 파생 평점을 날렸다.
- **내린 사진이 KST 자정에 되살아났다**(P2). 숨김은 `placeOps`로 캐시 밖에 뒀으면서 `deletePlacePhoto`는 `dataset(now)`만 고쳤다 — **같은 종류 2회째**라 규칙으로 올린다(아래).

**검증이 빠진 것 (1건)**
- **`resolveReport`에 zod가 없었다**(P1). `as Report` 단언이라 모르는 상태가 저장될 수 있었고, 그런 행은 열린 큐에서 빠지는데 어떤 종결 상태도 아니라 **영영 안 보인다**. 다른 쓰기와 같이 첫 await 앞에서 파싱한다.

**규칙 승격(같은 종류 2회째)**: 목 단계에서 **날짜 캐시(`dataset`)를 넘어야 하는 상태는 캐시 밖 맵·Set에 둔다**. `dataset()`은 KST 날짜가 바뀌면 `rawPlaces`에서 다시 만들기 때문에 캐시 안 객체에만 찍으면 자정에 풀린다. 지금 그런 상태는 `deletedReviewIds`·`placeOps`·`removedPhotoIds` 셋이고, 새로 만들 때도 같은 자리에 둔다.

**테스트 497 → 504개.**

## 2026-09-09 — 전국 줌아웃 + 지역 라벨 사다리

- **맥락**: `MIN_ZOOM = 10`이 바닥이라 최대로 줄여도 수도권까지만 보였다(폰 390px에서 47×85km). 그런데 제보는 2026-09-04에 **전국 허용**으로 갔고 `"김포시(경기)"` 라벨까지 있다 — 부산에 제보한 사람이 자기 핀을 보려면 325km를 7화면 팬해야 했다. 팬 제한(`maxBounds`)은 원래 없었으므로 막혀 있던 건 "전국을 한 화면에 담는 것"뿐이다.
- **결정 1 — `MIN_ZOOM` 10 → 7.** 줌 7 = 970m/px이라 폰(390px)에서 378×681km, 남한 전체가 한 화면이다. 데스크탑(1440px)은 주변국까지 들어오지만 **모바일 퍼스트라 폰 기준으로 잡는다**(줌 8로 잡으면 데스크탑은 전국이어도 폰에서 부산이 안 보여 어중간하다). 그 아래(6)는 동아시아 바다이고 네이버 타일은 해외가 빈약해 얻는 게 없다.
- **결정 2 — `areaLabel` 사다리를 시도 한 단 위로.** 구가 8개 넘거나 60%가 보이면 무조건 `"서울 전체"`였다: 전국으로 줄이면 서울 밖 가게가 섞여도 "서울 전체 452곳"이라고 말했다. 사다리는 이제 시군구 → 한 시도 → 여러 시도 3단이고 **`"일대"` 문법을 한 단 위에서 재사용**한다(`마포구 일대` ↔ `서울 일대`). 최상위는 `"전국"`.
- **"전국"만 뷰포트 크기로 판정한다 — 첫 설계를 화면에서 뒤집었다.** 플랜은 "분포만 보면 전국까지 줄였을 때 전부가 보여 비율이 100%가 되니 자연히 `전국`이 뜬다, 줌·뷰포트라는 새 입력이 필요 없다"고 했는데 **실제로 돌려 보니 기본 화면(데스크탑 줌 12)부터 `전국 38곳`이 떴다**. 김포 한 곳만 섞여도 시도가 2개가 되고 서울 가게의 76%가 보여 60% 문턱을 넘기 때문이다. 원인은 문턱값이 아니라 전제다: **시드의 93%가 서울에 몰려 있어 수도권 뷰와 전국 뷰는 보이는 가게가 거의 같다** — 분포로는 원리적으로 구별되지 않는다. 그래서 `"전국"`은 뷰포트 가로·세로가 둘 다 3°를 넘을 때로 정한다(폰 줌 7 = 4.3°×6.1°, 데스크탑 줌 8 = 7.9°×3.9°가 걸리고 폰 줌 8·데스크탑 줌 9는 한 축이 모자라 안 걸린다). **나머지 칸은 분포 그대로** — 줌만 보면 같은 줌에서 강남을 보든 강원을 보든 같은 라벨이 나온다.
- **곁다리 시도는 최다 시도 80% 규칙으로 걸러낸다**: 서울 37 + 김포 1이면 `"서울 전체"`, 서울 20 + 경기 18이면 `"서울 일대"`. 이게 없으면 서울 밖 핀 하나가 라벨을 통째로 한 단 올린다.
- **`"전국"`은 "전국에 골고루 있다"는 주장이 아니다** — 지금 보는 범위다. 서울에 뭉친 클러스터는 마커가 이미 보여주므로 라벨이 그걸 반박할 필요가 없다. 카피는 `전국`(`전국 전체`는 겹말).
- **서울 밖은 시도 괄호를 뗀다**(사용자 선택): 헤더는 `김포시 1곳`. 저장값·카드·상세는 원문 그대로다. (여기서 예상한 "동구·남구가 어디인지 모른다"는 한계는 **같은 날 아래 항목에서 해결했다** — 재검토 조건이 "실제 제보가 들어오면"이었는데, 그 전에 화면에서 먼저 드러났다.)
- **한계 2**: 3° 문턱은 화면 전체 기준이라 **바텀시트가 덮는 아래쪽을 안 뺀다**. 줌 7 폰에서 시트가 half면 보이는 띠가 약 350km라 부산·제주가 시트 아래에 있고, 시트를 내리면(collapsed) 인천·속초~목포·여수·통영까지 본토가 거의 다 든다. 라벨이 `전국`인데 화면엔 중부만 보이는 구간이 있다는 뜻이라 감수한다 — 지도를 보려면 어차피 시트를 내린다. **재검토 조건**: 세로 예산을 다시 잴 일이 생기면 `visibleStripCenterY`처럼 시트를 뺀 높이로 문턱을 계산한다.
- **안 한 것 — 줌아웃 안내 문구.** 줌 10 아래에서 "지금은 서울 위주예요 · 제보하기"를 띄우는 안은 검토했으나 **넣지 않는다**(사용자 선택): 헤더가 `전국 452곳`이라고 말하는 것으로 이미 답이 되고, spec 4.1에 없는 새 요소다.
- **부작용(감수)**: 전국 뷰에서 카드 거리가 `203km`로 뜨고 "가까운순"이 사실상 무의미해진다. 형식은 정상이고(`formatDistance`가 10km↑는 정수 km) 그 줌에서만 그렇다. 클러스터는 그리드라 452곳이 원 1~2개로 뭉친다 — 성능 영향 없음.
- **0곳이면 "이 지역"이 아니라 지도 중심의 지역을 부른다**(같은 브랜치 후속, 사용자 지적). 줌을 열기 전엔 빈 지역까지 갈 방법이 사실상 없어(47km 창으로 325km 팬) "이 지역"이 거의 안 보이는 값이었다 — 전국 줌아웃이 흔하게 만든 케이스라 플랜의 사각지대였다. 중심 좌표를 `lib/gu.ts`의 `guOfPoint`로 판정한다(제보 2단계가 쓰던 그 경계 폴리곤. 외부 지오코딩이 아니라 규칙 2와 무관하다).
- **입도는 뷰포트 폭이 정한다**(사용자 선택): 가로·세로가 둘 다 0.5°를 넘으면 시도(`경남`), 아니면 시군구(`창원시 마산합포구`). 시군구 하나가 대략 0.1~0.3°라 0.5°면 "한 동네보다 넓게 보고 있다"는 뜻이다. 바다·한국 밖은 `guOfPoint`가 null이라 `이 지역`으로 떨어진다 — 서울 밖 경계는 단순화본이라 해안에서 종종 그렇게 된다(2026-09-04 "전국 정밀도는 낮다").
- **가게가 있으면 중심을 안 쓴다.** 분포로 정하는 게 맞다 — 중심을 쓰면 마포 12곳을 보는 화면이 중심이 조금 넘어갔다는 이유로 "서대문구"가 된다. 경계 파일(서울 60KB + 서울 밖 180KB)도 빈 화면에서만 받게 된다.
- **`react-hooks/set-state-in-effect`에 걸렸다**(2026-09-08에 기록한 그 규칙). effect 본문에서 동기로 `setCenterGu(null)` 리셋을 하려다 잡혔고, **결과를 판정에 쓴 중심 좌표와 함께 저장해 현재 중심과 일치할 때만 쓰는** 쪽으로 고쳤다. 리셋이 필요 없어지고 팬 도중 이전 지역 이름이 남는 것도 같이 막힌다 — seq(늦은 응답 폐기)와 겹치지 않는 다른 층의 가드다.
- **중복 시군구 이름에는 시도를 앞에 붙인다**(사용자 지적 — 광주가 "서구"·"남구"로만 떴다). 경계 파일 251개(서울 25 + 전국 226)를 전수로 세니 **7종 30개(12%)**가 이름만으로는 구별이 안 된다: 중구 6(**서울 포함**)·동구 6·남구 5·서구 5·북구 4·**강서구 2(서울/부산)**·고성군 2(강원/경남). 목 50곳 중 6곳이 있는 서울 강서구가 부산 강서구와 겹치므로 서울도 남 일이 아니다.
- **표기는 "광주 서구"**(앞 접두어), 괄호가 아니다 — 뉴스·주소·배달앱이 쓰는 순서이고 "어느 도시의 어느 구"라는 읽기 순서와 맞는다. **서울은 접두어를 생략한다**: `Place.gu`가 이미 "괄호 없으면 서울"이라 화면도 "접두어 없으면 서울"이 되어 문법이 하나가 되고, 서울 우선 제품에서 "서울 강서구"는 과하다. 모호한 30개만 붙고 나머지 221개는 그대로다 — 대안(서울 밖 전부 접두)은 "경남 창원시 마산합포구"(12자)까지 길어지고, 대안(모호하면 시도만)은 `고성군`처럼 군 이름이 더 유용한 경우를 버린다.
- **적용은 0곳 라벨만이 아니다.** 부산에 제보가 쌓이면 가게가 있는 라벨도 "서구 3곳"·"서구 일대"가 된다 — `sigunguLabel()` 한 곳을 두 경로가 같이 쓴다.
- **상수 + 경계 파일 대조 테스트**(`SEOUL_GU`가 쓰는 그 패턴): 라벨 한 줄 만들자고 240KB를 읽지 않되, 행정구역이 개편돼 상수가 어긋나면 `places.test.ts`가 깨진다.
- **다른 엣지는 확인 결과 문제 없다**: 긴 2단계 이름 31개(최장 "창원시 마산합포구" 9자)는 헤더 `min-w-0 truncate`로 잘림 없이 들어가고, 세종(`세종시`/`세종`)·제주(`제주시`·`서귀포시`)도 자연스럽다. 바다·한국 밖은 `guOfPoint`가 null이라 `이 지역`으로 떨어진다.
- **플랜·결과**: docs/plans/nationwide-zoom.md.

## 2026-09-09 — 신규 핀: 마커 NEW 배지 + 시즌 카운터가 켜는 필터

- **맥락**: 신규 신호가 `.saeu-marker--new { border-style: dashed }` 한 줄뿐이었다. 36px 원의 2px 카테고리색 링이 실선→점선으로 바뀌는 것뿐이라 실기기에서 거의 안 보인다. 더 큰 구멍은 **시트 캡션이 "새로 들어온 집 N곳"이라고 숫자를 약속하는데 그 N곳으로 가는 길이 없었던 것** — [새로 들어온 집] 칩은 2026-09-05에 빠졌다.
- **마커 위 「NEW」 레드 pill 배지**(사용자 선택). `caption-l-semibold`(12px, 스케일에 10px semibold가 없다) 흰 글자 + `shadow-marker`. **`pointer-events: none`** — 신규 핀이 이웃 핀의 클릭을 뺏지 않는다. `.saeu-marker`가 썸네일을 원으로 자르려고 `overflow: hidden`이라 **`isNew`일 때만 36px 래퍼**를 씌우고 배지만 위로 넘긴다(래퍼가 마커와 같은 크기라 `size`·`anchor`가 안 바뀐다). 400개 전부에 빈 div를 더하지 않는다.
- **점선 링은 뺐다** — 배지가 같은 말을 더 크게 하므로 링은 카테고리 색(구이 코랄 / 회 틸)으로 돌려준다. 정보 하나를 되찾는 셈이다.
- **애니메이션 없음**: 깜빡임·펄스는 design "신규는 *정보*로 보이고 *참여 요구*로는 보이지 않는다"(2026-09-05)를 넘는다.
- **채운 레드는 규칙 위반이 아니다** — design 26줄이 별점 새우를 넣으며 정리한 그대로 "채운 레드 화면당 한 곳"은 **버튼** 규칙이고, 클러스터 레드 원·별점과 같은 결이다.

### fitBounds만으로는 안 됐다 — 계획을 화면에서 뒤집었다

플랜은 캡션 탭을 "필터를 켜지 않고 지도만 맞춘다"로 잡았다. 돌려 보니 **아무것도 해결되지 않았다**: 목 신규 3곳이 성수·강남·구로로 **16km에 흩어져** 있어 fitBounds가 줌 11이 되고 거기서 다시 클러스터로 묶여 **배지가 0개**였다. 게다가 목록은 뷰포트 기준이라 그대로 42곳이다 — 눌렀는데 그 3곳이 어디인지 여전히 모른다.

- **그래서 필터를 켠다.** 목록이 3곳으로 좁혀지면 클러스터에 묶인 나머지도 **카드가 경로를 준다**(카드 탭 → 그 핀). roadmap 백로그의 "[새로 들어온 집] 필터 되살리기"를 **칩이 아니라 시즌 카운터 자리**로 앞당긴 것이다 — spec 4.1의 칩 5개 상한을 안 건드린다.
- **입구와 상태를 분리한다**(사용자 지적). 처음엔 캡션 조각 자체를 토글(눌린 상태 = 틴트)로 만들었는데, **캡션은 상태 줄**(오늘 N건·이번 주 N곳)이라 거기 컨트롤이 섞이고, 켜져 있어도 회색 캡션 안 작은 틴트뿐이라 "왜 3곳이지?"의 답이 안 보이며 끄는 길도 그 조각뿐이었다. 지금은 **캡션 = 입구(멱등한 "켜기", 눌린 상태 없음) / 칩 행 = 상태·해제**다.
- **적용 필터 칩은 맨 앞이라야 한다**(실측): 뒤에 붙였더니 칩 행이 가로 스크롤이라 **화면 밖으로 나가** 상태가 안 보였다. `[전체 ▾]` 바로 옆으로 옮겨 390·1440 모두 화면 안임을 확인했다.
- **캡션 셋째 조각의 `lg:` 제한을 풀었다.** "모바일에선 캡션이 두 줄이 된다"(design 화면 6 v3)가 근거였는데 실측하니 **320·390·430 전부 한 줄**(높이 17 = 한 줄)이다. 320에서만 첫 조각이 기존 `truncate`로 살짝 잘린다 — 모바일 퍼스트 제품에서 입구가 데스크탑에만 있을 수는 없다.
- **목 데이터는 안 건드렸다.** 원본 JSON에는 `createdAt`이 3곳뿐(2026-08-27)이라 "신규 0곳"으로 보이지만, `lib/data.ts`의 `dataset()`이 `shift = today - MOCK_LATEST_DAY`로 날짜를 밀어 실행 중에는 늘 7일 이내다.

### 랭킹은 따로 판단한다 (미결)

사용자가 "확인 많은 순을 빼서 랭킹으로 따로 두면?"을 물었다. **정렬과 필터는 다른 축**이라 세그먼트에 신규를 넣는 안은 접었다(넣으면 42곳이 그대로 남고 3곳이 위로 올라올 뿐이다). 랭킹 자체는 **"조작 표적"이 지금 단계에선 약한 논거**임을 인정한다 — MAU가 없고, 문제가 생기면 로그인 필수화 같은 대응이 있다. 남는 반대 근거는 둘이다: ① **사장님 관계** — 크롤로 만든 순위표는 하위에 놓인 가게를 만들고, "왜 우리가 아래냐"는 spec 3의 "1회 처리로 다투지 않는다"로 안 끝난다(수익 모델의 최대 자산인 사장님 클레임이 그 관계 위에 선다). ② **표본 없음** — 확인 수가 0~몇 회라 지금 만들면 "확인 3회 1위"가 된다. **조건부 보류**: 확인 수가 쌓이면 다시 본다. 그때도 누적 순위보다 **"이번 주 많이 확인된 곳"**처럼 시간 창을 좁힌 형태를 먼저 본다(누적 조작이 안 쌓이고 시즌 제품에 맞는다).

### Codex PR #13 코멘트 2건 — 1건 반영, 1건 근거 대고 무시

- **#1(P1) 반영 — 경계 판정을 `lib/data.ts` 경유로.** `use-map-screen.ts`가 `@/lib/gu`의 `guOfPoint`를 직접 불렀다. **같은 파일 25줄에 `getGuOfPoint`를 이미 import해 두고 있었는데도** 새 import를 판 것이고(`lib/data.ts:414`, 제보 2단계가 쓰던 그 이음매, map-screen 테스트가 이미 목한다), 절대 규칙 1 위반이다 — Phase 6에서 `lib/data.ts`만 갈아끼울 때 이 호출만 남는다.
- **#2(P2) 무시 — `<p>` 안의 `<button>`은 유효하다.** "브라우저 파서가 `<p>`를 먼저 닫아 하이드레이션 불일치가 난다"는 지적인데 **전제가 틀렸다**: `<p>`의 콘텐츠 모델은 phrasing content이고 `<button>`은 phrasing content다. 파서가 `</p>`를 암묵적으로 닫는 건 `div`·`ul`·`p` 같은 non-phrasing flow content가 올 때다. 실측으로도 확인했다 — 버튼의 부모가 그대로 `<p>`(`closest('p')` → `aria-label="시즌 카운터"`)이고 SSR 라우트 `/gu/마포구`(신규 있음)에서 **콘솔 에러·경고 0**이다.
- **규칙 승격 — `no-restricted-imports`로 `guOfPoint`·`guCenter`를 `app/**`·`components/**`에서 금지.** `eslint-plugin-boundaries`가 못 잡은 이유는 `components → lib`을 통째로 허용하고 데이터 차단이 `lib/mock`에만 걸려 있어서다(`lib/gu.ts`는 그냥 `lib` 타입). **모듈이 아니라 이름 단위로 막는다** — `lib/gu.ts`에서 경계 JSON을 읽는 건 이 둘뿐이고 나머지(`SEOUL_GU`·`isSeoulGu`·`GU_SLUGS`·`guSlug`·`guFromSlug`)는 순수 상수·매핑이라 `app/gu/[name]`·`app/og/gu/[slug]`가 직접 쓰는 게 맞다. **발화 확인 완료**: 위반을 일부러 되돌리면 error, 원복하면 통과(하네스는 파일 생성이 아니라 발화가 완료 조건).
- **플랜·결과**: docs/plans/new-pin-badge.md.


## 2026-09-09 — 까주기 테스트 `/test`: 6문항 2축 4유형 + 가게 추천 + 궁합 (Phase 7 → 지금)

- **맥락**: roadmap Phase 7 항목이었으나 **Phase 6(백엔드 교체) 앞으로 당긴다**(사용자 결정). 목 데이터로 만들 수 있는 마지막 사용자 화면이고, 새우철(9~11월) 런칭 바이럴 장치라 시즌 안에 서야 한다. 라우트가 없어 이벤트 배너 `href`를 `null`로 비워 둔 상태(2026-09-08)도 이 작업이 되돌린다.
- **포맷을 먼저 비교했다**(사용자 지시로 국내 사례 조사 후 6안 비교): ① 까주기 유형 ② 나랑 맞는 새우집(매칭) ③ 새우력 모의고사(퀴즈·등급) ④ 새우 종족 테스트 ⑤ 불판 앞 역할 8종 ⑥ 사이즈 판독기. **①+②를 고른다**(사용자 선택) — 퍼진 테스트의 공통점이 셋인데(결과가 캐릭터 / 결과가 브랜드 서비스로 이어짐 / 링크가 다시 테스트를 부름), ①이 공유 훅을 ②가 전환을 맡아 둘을 다 만족한다. **스페이스클라우드**(유형 → "내 능력이 빛나는 공간" 추천 → 예약)가 그대로 우리 구조다. ③은 결과가 지도로 흐르지 않고, ④⑤는 아트가 6~8장 필요하며, ⑥은 spec 8장 "런칭 후"이고 서버·모델이 있어야 한다.
- **조사 근거**: 문항 수는 **6~8개**가 관행이고("질문은 길어도 되나 답변 후보는 짧아야 한다" — SNAP APP), 푸망은 9문항 = 3문항 × 3성향, 툴앤조이는 유형 이름을 "든든한 한 끼형"처럼 **형용구 + 명사 + 형**으로 짓고 결과에 조심할 점·잘 맞는 유형을 둔다. 스낵팟 SPTI는 5일 만에 860만(최종 980만)이었고 그 핵은 **캐릭터 결과**였다.
- **6문항 2축 4유형.** 6은 배너 카피 "20초면 끝나요"가 정한 상한이다(문항당 3초). 축 A 까준다↔받는다(배너가 약속한 축) · 축 B 구이↔회(우리 카테고리와 1:1이라 추천이 억지가 아니다). **축당 3문항이라 동점이 없다.** 8유형(3축)은 20초를 넘기고 카피·OG·아트가 8벌이 된다. 문항은 A·B **교차 배치** — 한 축을 연달아 물으면 채점이 티 난다.
- **궁합은 조합 카피 16벌 대신 축에서 도출한다**(사용자가 궁합 모드 채택). 두 유형의 축을 비교하면 관계가 4종으로 떨어진다: **R1** 축 A 다름·축 B 같음(궁합 100) · **R2** 축 A 다름·축 B 다름(80) · **R3** 둘 다 까주는 쪽(65) · **R4** 둘 다 받는 쪽(40). 억지 궁합이 아니라 테스트 자체의 논리이고, 카피가 16벌 → 4벌이 된다.
- **R2에서 궁합이 쓸모를 낳는다**: 취향이 갈리면 **구이·회를 둘 다 하는 집**을 뽑는다(목 50곳 중 15곳, 실데이터에도 흔하다). 나머지는 공통 카테고리 상위 3곳.
- **공유 버튼이 둘이다.** [결과 공유하기]는 `/test/<유형>`(자랑), [친구와 궁합 보기]는 **초대 링크** `/test/with/<유형>`을 보낸다 — 친구가 열면 "OO형이 궁합을 신청했어요"가 뜨고 6문항을 풀면 `/test/<친구>/<나>`로 떨어진다. **링크 하나가 다음 테스트를 부르는** 유일한 장치라 얹었다.
- **라우트 4개 · OG 25장, 전부 빌드 시 생성**(구별 카드와 같은 이유 — Workers Free는 요청당 CPU 10ms, 2026-09-07). `generateStaticParams` + `dynamicParams = false`라 슬러그 밖은 **진짜 404**. `/test`와 결과 4개만 sitemap에 넣고 **초대·궁합 20개는 noindex** — 공유 링크로만 사는 얇은 페이지라 색인 가치가 없다. 정적 세그먼트 `with`가 `[type]`보다 우선하므로 충돌하지 않는다.
- **정정(구현 중)**: 페이지 자체는 **둘만 정적**이다 — `/test`와 `/test/with/[type]` 4장. **결과·궁합은 `connection()`으로 요청 시 렌더**한다: 추천 카드가 "어제 확인" 같은 상대 시간을 그리므로 배포 시각에 얼면 거짓이 된다(`/gu/[name]`과 같은 이유). 빌드 시 굽는 건 **OG 25장**이고, 그게 Workers CPU 10ms 제약이 걸리는 유일한 자리다. 슬러그 밖 404는 `generateStaticParams`가 아니라 **라우트 안의 `notFound()`**가 보장한다.
- **슬러그는 ASCII 로마자**(`jipge`·`sonjil`·`wansik`·`chojang`) — 구 슬러그와 같은 문법이다. 퍼센트 인코딩된 한글 URL은 공유 링크에서 깨져 보인다.
- **그릇은 독립 풀페이지**(사용자 선택) — 지도 없음. 공유 링크로 처음 오는 사람이 주 진입이라 지도가 먼저 뜨면 안 된다. 데스크탑도 중앙 480 한 컬럼이다(design 화면 11 신설).
- **참여자 수·유형별 비율은 넣지 않는다**. 국내 관행이지만 서버가 있어야 진짜 수치가 된다(Phase 6). 규칙 4로 localStorage도 못 쓴다 — **가짜 숫자를 박느니 자리를 비워 둔다**.
- **콘텐츠는 `lib/mock/peel-test.json` 한 파일**(이벤트 카드와 같은 설정값 문법) — 카피를 다듬는 게 코드 수정이 아니다. 채점·궁합 판정은 `lib/peel-test.ts` 순수 함수, 추천 3곳은 `lib/data.ts` 경유(규칙 1).
- **결과 아트는 자리만 잡고 간다**(사용자가 제작 중): 지금은 `shrimp.webp` 변형으로 그리고, 캐릭터 4장이 오면 `TYPE_ART` 상수 4줄만 바꾼다. **폴백 코드는 두지 않는다** — 파일이 없으면 조용히 다른 그림이 나가는 게 더 나쁘다.
- **화면을 이벤트 톤으로 다시 썼다**(같은 날, 사용자 판단 "심리테스트인데 밋밋하다"). 처음 구현은 앱 본체와 같은 정보형(흰 바탕·회색 원·모션 없음)이었는데, **바이럴 장치는 정보 화면과 결이 달라야 한다**. 바꾼 것: 바탕을 브랜드 틴트 + 배경 새우 실루엣(`shrimp-mask`를 `red-50`으로 — 별점 마크와 같은 마스크 문법이라 새 에셋이 없다), 내용은 흰 카드(`rounded-20`·`shadow-card`), 아트 원을 회색 → 틴트, 표지에 유형 미리보기 4개, 문항에 번호 원·화살표·워터마크·"질문 N / 6", 결과에 해시태그 pill 3개와 **2×2 유형 매트릭스**, 궁합에 채운 레드 점수 원 + 카운트업. **새 색·새 컴포넌트 없이 기존 토큰만** 썼고 그라데이션·이모지 금지는 그대로다.
- **모션은 `globals.css`에 세 개**: `.saeu-rise`(등장, delay 3단) · `.saeu-pop`(아트, 제보 완료와 공용) · `.saeu-bob`("결과 만드는 중"만 반복). 지연 값을 컴포넌트에 흩뿌리지 않는다. `prefers-reduced-motion`이면 전부 꺼지고, 궁합 카운트업도 최종값으로 바로 간다.
- **해시태그·짧은 이름은 콘텐츠**다(`tags`·`shortName`) — 화면이 아니라 JSON에 있다. 캡처 한 장에 성격이 담기는 게 국내 테스트의 공통 문법이라 결과 카드에 넣었다.
- **텍스트 토큰 3종을 새로 만들었다**(사용자 허가 "이거 전용으로 새로 만들어도 된다"): `display-l` 32/800 · `display-m` 26/700 · `read-l` 16/400 **행간 1.7**. 기본 12종은 정보형 화면 스케일이라 히어로가 24에서 멈추고 문단 행간이 1.4다 — 표지·유형 이름이 안 서고 결과 문단이 안 읽혔다. **화면 11 밖에서는 쓰지 않는다**(지도·상세·관리자는 12종 그대로). CLAUDE.md·design 토큰 표에 예외로 명시.
- **[이전]을 아웃라인 pill로 키웠다** — 12px 회색 텍스트라 안 보이고 안 눌렸다. 실수했을 때 유일한 출구라 히트 영역이 값이다. "20초면 끝나요"도 회색 한 줄 → **배지**(짧다는 게 시작의 이유라 눈에 걸려야 한다).
- **문항을 다시 짰다**(같은 날, 사용자 지적 "질문이 무슨 말인지 모르겠다"). 첫 판의 축 B 세 문항이 **가게 문화를 아는 사람만 답할 수 있었다** — "소금 위에 올릴까 얼음 위에 올릴까"(소금구이↔회), "마무리는 매운탕 한 그릇"(회 코스의 끝). **테스트는 첫 방문자가 푸는 화면**이라 전제 지식이 있으면 거기서 이탈한다. 축 B를 도메인 지식 없이 답하는 세 각도로 바꿨다: 방식(뜨겁게 구워서↔차갑게 날로) · 상황(술안주) · 맛(불맛↔단맛). 축 A도 장면을 구체화했다("아무도 집게를 안 든다" → "다들 먹느라 바쁜데 불판이 비었다"). 축 매핑·교차 배치·채점은 그대로다.
- **문항 2차 정정 — 소금구이의 실제 조리를 틀리게 썼다**(사용자 지적). 새우 소금구이는 **직원이 냄비에 굵은 소금을 깔고 새우를 담아 와 뚜껑을 덮어 익힌다**. 그래서 ① 손님이 불판에 새우를 더 올리는 장면은 **없고**(5번 "불판이 비었다"), ② 뚜껑을 덮는 조리라 **불맛이 날 수 없다**(6번 "불맛↔단맛"). 5번은 **"새우가 튀어서 냄비 뚜껑이 들썩인다"**(이 음식에서 가장 상징적인 장면), 6번은 맛·식감(**짭조름·고소 ↔ 달고 쫀득**)으로 고쳤다. 잔재도 함께: "불판 앞" → "불 앞", `#불판지킴이` → `#뚜껑담당`, R2 궁합 설명. **교훈**: 도메인 카피는 조사한 관행(테스트 문법)만으로는 안 되고 **음식 자체의 사실**을 확인해야 한다.
- **플랜·결과**: docs/plans/peel-test.md.
